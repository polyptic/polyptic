# dependencies
node_modules/
.pnp.*
bun.lockb.bak

# builds
dist/
build/
out/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example
*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
.claude/

# agent artefacts
*.deb
profiles/

# runtime media (dev MEDIA_DIR default = packages/server/media) + uploaded files
packages/server/media/
/media/

# runtime fleet logs (dev LOG_DIR default = ./logs, POL-187) — NDJSON per machine per UTC day
packages/server/logs/
/logs/
deploy/dist/test-depot/

# Agent-Native local plan bridge token (never commit)
.plan-url
comments.json

# The SITE LAYER (POL-190): an organisation's own endpoint/compliance bundle. Never committed — it
# names internal repos and hosts, and a careless copy of the boot medium's site.conf into it would put
# a bearer credential in git. `deploy/site.example/` is the committed template.
deploy/site/
