# Polyptic — Decisions Log

ADR-lite. Newest at the bottom. Don't re-litigate accepted decisions in passing — if one needs to change, add a new entry that supersedes the old and say why.

| # | Date | Decision | Status | Why |
|---|------|----------|--------|-----|
| D1 | 2026-06-29 | **Generic, vendor-neutral product** — no coupling to any specific dashboard, identity, or content stack. | Accepted | Reusable across any wall; integrations live behind adapters so the core never depends on a vendor. |
| D2 | 2026-06-29 | **Name: Polyptic.** | Accepted | A polyptic is a multi-panel painting whose panels compose one image — the exact screens/scenes/mosaic metaphor, with a clean, distinct namespace. |
| D3 | 2026-06-29 | **Architecture: control plane + thin reconciling agents + per-screen player.** Desired-state, Kubernetes-controller style. | Accepted | One global layout, swarm-consistent; matches a model the team already thinks in. |
| D4 | 2026-06-29 | **Screens are first-class & named; machines are plumbing.** Ident mode maps panels → screens. | Accepted | User's core mental model; makes config + onboarding intuitive. |
| D5 | 2026-06-29 | **Instant updates over WebSocket, DOM-diff in the player, no reload (< ~150ms).** | Accepted | Hard requirement — snappy enough to impress stakeholders; slowness is not acceptable. |
| D6 | 2026-06-29 | **TypeScript everywhere**, ESM, **bun** workspaces (switched from pnpm). | Accepted | Team already uses bun; one tool for install + run + the shipped agent binary; bun runs TS natively, so we drop pnpm AND tsx. |
| D7 | 2026-06-29 | **Agent = Bun single binary**; controls host via IPC sockets + child processes; stays unprivileged with a minimal privileged helper. | Accepted | Trivial edge deploy (one file); no runtime to install; small blast radius. |
| D8 | 2026-06-29 | **Player & Admin UI = SolidJS + Vite.** | Accepted | Lean, fast, good for a page that runs unattended for days. |
| D9 | 2026-06-29 | **Generic display backend**: `DisplayBackend` interface with `wayland-sway` (default) + `x11-i3` (fallback), runtime auto-detected. | Accepted | Must be generic; Wayland best for Intel/AMD, X11 for NVIDIA/edge cases. |
| D10 | 2026-06-29 | **Shared contract in `@polyptic/protocol` (zod).** All cross-process messages defined + validated there. | Accepted | Single source of truth for wire types; enables parallel package builds. |
| D11 | 2026-06-29 | **Auth: anonymous-first, login-page acceptable initially**, but build the per-source auth-strategy seam (`public`/`anonymous`/`reverse-proxy`/`oidc`) from day one. | Accepted | Get moving fast; some content needs real auth "very soon after" — must be a config change, not a rewrite. |
| D12 | 2026-06-29 | **Outbound-only agents** (dial control plane over WSS); enrollment via one-time bootstrap token → claim → durable mTLS cert. | Accepted | No inbound ports/NAT holes into the client LAN; cold boot = reconnect; secure enrollment. |
| D13 | 2026-06-29 | **Two WS channels**: agent channel (device/placement/lifecycle) and player channel (content). Content goes server → player directly, never via the agent. | Accepted | Keeps content path fast (instant) and the agent simple. |
| D14 | 2026-06-29 | **Build order: vertical slice first** (server+agent+player proving instant on one screen), then breadth. | Accepted | De-risks the spine + the headline "instant" property early; demo-able sooner. |
| D15 | 2026-06-29 | **Docs live in the repo — the single durable source of truth** (no separate notes vault). | Accepted | Keeping docs in-repo means one versioned source of truth that travels with the code and never desyncs. |
| D16 | 2026-06-29 | **Dev runtime = Bun** for server & agent (`bun --watch`, no tsx); added a **`dev-open` display backend** (opens player URLs in the host browser; auto-detect defaults to it in Phase 1). | Accepted | Bun runs TS natively; lets Phase 1 run on any dev machine (incl. macOS, no compositor). Consistent with D6/D7 — the shipped agent is a Bun single binary (Phase 4 packaging). |
| D17 | 2026-06-29 | **Auth = two buckets** (clarification of D11): (A) content auth is website/browser-level — Polyptic only points tiles at the right URL/proxy; (B) Polyptic's own admin UI/API auth is OIDC, our layer. | Accepted | Sharpens what Polyptic does vs doesn't implement; see ARCHITECTURE.md. |
| D18 | 2026-06-29 | **Storage = PostgreSQL from the outset**, behind a `Store` interface (`PostgresStore` default via `postgres` client + SQL migrations; `MemoryStore` test double). Dev Postgres via `deploy/docker-compose.yml`. | Accepted | Prod parity / easiest port to prod (user priority — "as easy to port to prod as possible"). The interface keeps tests fast (MemoryStore) without compromising the real storage. Supersedes the earlier bun:sqlite idea. |
| D19 | 2026-06-29 | **Phase 2 split: 2a** = persistent Postgres registry + multi-machine + minimal Admin UI + ident mode; **2b** = device enrollment/claim + mTLS identity. | Accepted | Keep 2a tight + demoable (the visible "screens + ident" magic); enrollment/mTLS is plumbing, deferred. Roadmap updated. |
| D20 | 2026-06-29 | **Spatial composition is the core UI concept ("murals").** The Admin UI's primary workspace is a canvas where operators **drag + snap screens** into spatial arrangements; **screens are independent of their host machine** (do NOT organise the UI by machine). Content can span adjacent screens; scenes capture the whole composition. Canvas may be large/zoomable/possibly infinite. | Accepted | It's the product's namesake and was the user's key correction. **Reshapes Phase 3:** a Screen needs a spatial position/size; introduce a **Mural/Wall** entity (a named, positioned set of screens) distinct from content. See [[polyptic-spatial-canvas]] memory; to be written into DESIGN.md + the Phase 3 data model. |
| D21 | 2026-06-29 | **Mural = first-class, multiple, switchable; Screens are placed onto it.** A deployment has several named **Murals** (switchable, e.g. "Reception" / "Atrium"). A Screen is **unplaced** (in a tray) or **placed** on exactly one mural at `{x,y,w,h}`. Enrolment (2b) ≠ placement — an approved screen still has to be dragged onto a mural. | Accepted | Adopted from the Console v2 design. Concretises D20: murals are top-level entities you switch between; placement is its own operator step. |
| D22 | 2026-06-29 | **Combined surfaces (video walls).** Adjacent placed screens can be **Combined** into one `Surface` treated as a single logical screen — content **spans** across it (bezel seams shown); **Split** undoes it. A surface has its own combined resolution, content assignment, and "ident all". | Accepted | Adopted from Console v2 (the user's "combining screens"). Refines D20's "content spans" into an explicit grouped entity with combine/split ops. |
| D23 | 2026-06-29 | **Content Library = reusable `ContentSource` entities.** Content is not ad-hoc URLs but managed sources (name, kind = web\|dashboard\|image\|video, address/config, **auth strategy** per Bucket A/D11) you drag onto a screen or a combined surface. | Accepted | Adopted from Console v2's content library. Promotes the "content adapters" idea (D11/ARCHITECTURE) into first-class library items. |
| D24 | 2026-06-29 | **A Scene snapshots content + layout + grouping** (not just content): for a mural it captures each screen's content, every screen's position/size, and which screens are combined. Switching a scene restores the whole composition atomically. | Accepted | Adopted from Console v2 ("captures every screen & surface's content and layout"). Extends the earlier Scenes concept. |
| D25 | 2026-06-29 | **Activity feed** — the server emits an event stream (machine unreachable, content changed, scene activated, screen approved, …) surfaced as a live feed in the console. | Accepted | Adopted from Console v2. An affordance over data we already broadcast (`admin/state` + lifecycle events); not a core model change. |
| D26 | 2026-06-29 | **On-device delivery = an installable PACKAGE, not a mandatory image.** `apt install polyptic-agent` (a `.deb`) whose postinst wires greetd autologin → sway → systemd-supervised agent + Chromium-per-output. The setup logic lives **in the agent binary** (`polyptic-agent setup`), **distro-aware** (apt/dnf/pacman) so it's generic across any systemd Linux; native `.deb` first (Ubuntu/Debian = the hardware), `.rpm`/others later. A prebuilt **image** and **cloud-init/Ansible** are OPTIONAL wrappers around the same package. Per-box config (control-plane URL + bootstrap token) via debconf or `/etc/polyptic/agent.toml` → then it enrols (2b) → approve in the console. | Accepted | "Just install it." Generic, one source of truth; the image stays allowed for big fleets but isn't required. Splits cleanly: **apt = the box is a display; console = what it shows.** |
| D27 | 2026-06-29 | **Start from Ubuntu Server-minimal; add only the thin graphical layer; browser = `.deb` Chromium (not the snap).** A "server" is CLI-by-*default*, not CLI-only — the kernel DRM/KMS already drives the panel; we add a **compositor (sway)** + a **browser**, no desktop environment. Use a **`.deb` Chromium** (Ubuntu's snap-Chromium is confined, slow to cold-start, awkward profiles); **`cog`/WPE WebKit** is the documented fallback for low-power clients. | Accepted | Lean, fast-booting kiosk; no GNOME/GDM to fight; avoids the snap-Chromium kiosk pitfalls. |
| D28 | 2026-06-29 | **Operator console = Vue 3 + Vite + Vue Router + Pinia + Vue Flow** (new `packages/console`). **Supersedes D8 (SolidJS) for the console.** Vue Flow drives the murals canvas (node drag/snap/zoom). The **player** migrates SolidJS→Vue at **3b** (when combined surfaces touches it anyway); the old SolidJS `packages/admin` is retired once the console covers its machines/approve UI (3e). | Accepted | The user's familiar/preferred stack; Vue Flow is purpose-built for the spatial canvas. |
| D29 | 2026-06-29 | **Operator auth = LOCAL accounts, done securely.** Argon2id password hashing, HTTP-only secure session cookies (or signed short-lived tokens), login rate-limiting/lockout, no plaintext anywhere. OIDC stays a later **configurable add-on** on the same seam (Bucket B / D11). | Accepted | Self-hosted product; matches the design's email/password sign-in; user emphasised security. OIDC needs an IdP, so local-first unblocks dev. |
| D30 | 2026-06-29 | **Combined surfaces = `VideoWall` + `Surface.span`.** Adjacent placed screens combine into a `VideoWall` (≥2 members); content SPANS via the **union bounding box** of the members' placements — each member gets one web surface with `span {contentW,contentH,offsetX,offsetY}` (offsets = member.{x,y} − unionMin) that its player renders (content sized contentW×contentH, translate −offset). Unplacing a member, or deleting its mural, **DISSOLVES** the whole wall and clears each member's slice (no stale fragment left rendering). The **player is now Vue 3** (migrated from SolidJS per D28), preserving the INSTANT in-place swap (keyed `v-for` + reactive `:src`, no remount). | Accepted | Realises D22 (combined surfaces / video walls). Server-side span keeps the player dumb; dissolve-on-break keeps state honest. |
| D31 | 2026-06-29 | **Local-auth GATE, secure-by-default.** A Fastify preHandler gates all `/api/v1/**` (except `/auth/login,/logout,/me`) + the `/admin` WS upgrade; device channels (`/agent`,`/player`) + `/healthz`,`/metrics`,`/media` stay public. argon2id via **Bun.password**; opaque server-side sessions in a **signed httpOnly sameSite=lax** cookie (secure-when-HTTPS); **dual-key** login rate-limit (per-email AND per-email+ip) → 429; anti-CSWSH **Origin allowlist** on the /admin upgrade. `AUTH_ENABLED` defaults **true**; `false` (tests/dev only) skips the gate with a loud warning. Seed admin from `POLYPTIC_ADMIN_*` or a dev default. | Accepted | Implements D29. Secure-by-default; tests opt out like `STORE=memory`. |
| D32 | 2026-06-29 | **Live preview via agent capture; ops endpoints ungated.** Server CaptureCoordinator requests `server/capture` on an interval/on-demand; agents reply `agent/thumbnail` (grim/scrot); bounded LRU store; `GET /api/v1/screens/:id/thumbnail` (gated, mime-whitelist+nosniff). `GET /healthz` + Prometheus `GET /metrics` are **top-level, NOT `/api/v1`**, so unauthenticated scrapers/liveness work. | Accepted | The "impress stakeholders" preview + standard ops surface. |
| D33 | 2026-06-29 | **Media on a DISK VOLUME, not S3/MinIO.** Uploads (`POST /api/v1/media`, gated, multipart, image/* + video/* only, size-capped, streamed) land in `MEDIA_DIR` (random id, traversal-safe) and become a `ContentSource`. `GET /media/:id` is **top-level UNGATED** (players load it without a session) with HTTP **Range** support (video seeking). Deleting the source unlinks the file. Helm PVC / compose volume. | Accepted | User's call (disk volumes); ungated serve mirrors how the player loads any content URL. |
| D34 | 2026-06-29 | **Distribution = one Docker image + a .deb agent; npm optional.** The server serves the built **console (/) + player (/player)** via @fastify/static when `CONSOLE_DIR`/`PLAYER_DIR` are set (multi-stage Dockerfile builds them) → a single same-origin image (no CORS). GitHub Actions: `ci` (typecheck+test on push), `release` (tag `v*` → GHCR image + nfpm `.deb`/`.rpm` to the Release; npm-publish hard-gated), `pages` (docs site). **Self-hosted npm = GitHub Packages** (recommended, optional — the product ships as the image/.deb) or Verdaccio. | Accepted | Lowest-friction self-hosting; nothing auto-publishes. |
| D35 | 2026-06-30 | **The control plane is the edge DEPOT — zero-touch, air-gappable provisioning.** The server serves `GET /install` (a `curl\|sh` script with the control-plane URL injected from the request Host), `GET /dist/agent/<arch>` (the agent binary, baked into the image), and `GET /dist/deps/<distro>/<arch>/…` (the bundled substrate `.deb`s for a supported distro). An edge box reaches **only the server** — no internet. The agent install is fully air-gapped; `--kiosk` provisions sway/greetd/Chromium **offline-first** from the server bundle, falling back to the distro package manager **only** on an un-bundled distro with internet, else failing clearly. | Accepted | Air-gapped edge is a hard requirement. Extends D26 (installable package): the server distributes the substrate too, so the box needs nothing but the LAN. Pre-baked images stay an option for big fleets. |
| D36 | 2026-07-01 | **No guessed screen before a compositor exists; server prunes only UNUSED stragglers (POL-9).** A REAL backend (`wayland-sway`/`x11-i3`) with **no** explicit `POLYPTIC_OUTPUTS`/`POLYPTIC_CONNECTOR` override whose compositor reports nothing advertises **zero** outputs — never the old guessed `DEFAULT_CONNECTOR="HDMI-1"`. `dev-open` and explicit overrides keep the configured/default connector, unchanged. On re-advertise, the control plane prunes a machine's screens for connectors it no longer advertises, but **only** when (a) it advertised a **non-empty** set (empty = compositor-not-up ≠ "gone", so never wipe) and (b) the screen is **unused** (never placed, not in a wall, no surfaces, no library source). Output resolution moved to `packages/agent/src/outputs.ts` for unit testing; `index.ts` main() now guarded by `import.meta.main`. | Accepted | A guessed `HDMI-1` was actively harmful: wrong name → placement fails ("HDMI-1 is not a known sway output"), and it lingered next to the real panels once the kiosk agent advertised them (seen live on wall2, enrolled during Stage A before Stage B started sway). "Unused-only + non-empty-advertise" makes the prune safe — it clears phantoms/hotplug stragglers without ever destroying operator layout on a transient flap, so it needs no cascading deletes. |
| D37 | 2026-07-01 | **Media URLs are re-homed player-side onto the origin the player reaches the server at (POL-5).** An upload's `ContentSource.src` is baked ABSOLUTE as `${MEDIA_PUBLIC_BASE}/media/<id>` (the protocol validates `src` as `z.string().url()`, so a relative path isn't an option), which defaults to `http://localhost:8080`. A remote wall box then can't fetch it — `localhost` is the box itself → broken-image glyph (iframes were fine: real external URLs). The player already knows a reachable server origin (the same one it dials the /player WS on, derived from `window.location`), so `resolveMediaSrc` re-homes any **loopback/unspecified-host** media URL onto it (host + port), leaving genuine external URLs untouched. | Accepted | Fixes already-uploaded media with no re-upload/migration and no dependence on `MEDIA_PUBLIC_BASE` being set correctly; loopback hosts are unreachable-by-construction from a remote box, so rewriting them is always safe (a no-op when co-located). Reuses the player's existing WS-origin derivation so the two can't disagree. |
| D38 | 2026-07-01 | **Removal = FORGET, distinct from reject/revoke and unplace (POL-14).** Two idempotent DELETEs: `DELETE /api/v1/machines/:id` forgets a machine — deletes it, all its screens, their placements + content, and its off-band credential, and CLOSES its live agent socket; `DELETE /api/v1/screens/:id` forgets one screen — dissolves any combined surface it belonged to (clearing surviving members' slices) and clears its player. The store keeps single-responsibility deletes (`deleteScreen`/`deleteContent`/`deletePlacement` per D36's prune path); `removeScreen` composes them, `deleteMachine` cascades a whole box in one op. Both routes push the cleared `server/render`s (instant path) + a fresh admin/state. A removed **machine** must re-enrol to return (gated: unknown credential → rejected, bootstrap token → fresh pending; open: re-registers brand-new); a removed **screen** reappears on its machine's next `agent/hello` if that output is still driven — so it targets STALE screens or clearing out ahead of removing the whole machine. Console surfaces "Remove" on every machine card (pending/approved/rejected) and per screen row, each behind a `window.confirm`. | Accepted | POL-14 — operators need to decommission retired boxes and clear ghost outputs. Reject/revoke only parks a machine (remembered forever) and unplace only returns a screen to the tray; neither actually forgets. |
| D39 | 2026-07-01 | **Idle splash replaces the black screen when a player has no content (POL-27).** When a screen's slice carries **zero surfaces** — both pre-first-render and an explicitly empty slice — the player renders a calm, dark "No Content Assigned" board (the canonical Polyptic mark + wordmark, the screen id in a bottom-left pill, and the build version bottom-right) instead of a bare black stage. The status dot reflects `connState` (live green / connecting amber / offline red) so an idle-but-connected panel reads differently from one that never reached the control plane. New scoped-style component `IdleSplash.vue`; **no protocol change** — the screen id is the `?screen=` the player already has. Geist / Geist Mono load via the same Google-Fonts `<link>` the console uses, with a `system-ui` / `ui-monospace` fallback so an **offline** wall box still renders cleanly; the version is injected from `package.json` via a Vite `define` (`__APP_VERSION__`). **Dark-only** for now — the design's light variant is deferred; the boot-progress splash (POL-7, the design's other turn) is a separate ticket. | Accepted | A black screen reads as broken; a branded board reads as intentional and surfaces the panel's identity + version for on-glass diagnosis (esp. in prod, where the dev badge is hidden). Reuses the existing `connState` and the canonical mark (kept in sync with console `Logo.vue`); graceful font fallback honours the self-hostable/air-gapped non-negotiable. |
| D40 | 2026-07-01 | **Boot splash = a generated, LIVE Plymouth `script` theme (POL-7), not a static image.** `polyptic-agent setup` generates + installs a `polyptic` Plymouth theme (the same way it generates greetd/sway configs — the agent binary carries it as source, so the `curl\|sh` depot path (D35) needs no extra assets), sets it default (`plymouth-set-default-theme -R`), and adds `quiet splash plymouth.ignore-serial-consoles` to the kernel cmdline (`/etc/default/grub`→`update-grub`, or `cmdline.txt` on Pi). It is **live**, not a frozen picture: the script wires `SetUpdateStatusFunction`/`SetBootProgressFunction`/`SetMessageFunction`, so the splash shows real systemd boot status + a progress bar and the agent can push lines via `plymouth message` (answering Alex's "can it be live?" — yes; **version + hostname** are baked into a vector stamp from the build/host, and are refreshable live). The **logo is a swappable SVG** rasterised to PNG at install with `rsvg-convert` (Plymouth renders PNG, not SVG) so it scales to any panel and the final designed asset is a drop-in replacement. Clean hand-off via `plymouth quit --retain-splash` (a plymouth-quit drop-in + the compositor launcher) so sway paints over the last frame with no console flash. It also covers **shutdown/reboot** (setup enables the shipped `plymouth-{poweroff,reboot,halt,kexec}.service` units; the script keys off `Plymouth.GetMode()` to show "Shutting down"/"Restarting" instead of "Starting up", and hides the boot progress bar) — so there's no raw console text at *either* end. The agent **version is baked into the compiled binary** at build (`bun build --define POLYPTIC_BUILD_VERSION`), fixing the standalone binary previously reporting `0.0.0`. `--no-splash` opts out; `setup uninstall` restores the prior theme + cmdline. | Accepted | POL-7. A branded, informative boot *and shutdown* in place of kernel console text — no vendor hard-wiring, no mandatory image; the live status line makes the wall self-diagnosing while it comes up and goes down. |
| D41 | 2026-07-01 | **The agent ships ONLY via the zero-touch depot — the standalone `.deb`/`.rpm`/apt path is removed. Supersedes the agent-packaging parts of D26 and D34.** The only supported way to put an agent on a box is the control-plane one-liner `curl -sfL http://SERVER:8080/install \| sh -` (`… \| POLYPTIC_TOKEN=… sh -s -- --kiosk` for the visual substrate), served by the depot (D35): the server bakes the agent single binary into its image and serves it at `GET /dist/agent/<arch>`; air-gapped boxes pull the substrate from the server's bundled `.deb`s. Removed: `deploy/nfpm.yaml`, the agent `.deb`/`.rpm` packages, the `apt install ./polyptic-agent*.deb` / `dnf install` path, the first-boot systemd oneshot + `.deb` maintainer scripts (`deploy/agent/`), and `deploy/cloud-init.example.yaml`; the release CI now attaches the raw agent **binaries** (not packages). **Kept** (still correct): the kiosk **browser** `.deb` that `setup` installs (D27), the **server** Docker image + Helm chart (control plane, D34), `deploy/build-agent.sh` (now builds only the binary — to seed a depot), and the substrate `.deb` bundle the depot serves for air-gap. The version is baked into the served binary in `server.Dockerfile` (`--define POLYPTIC_BUILD_VERSION`), so the depot binary reports a real version (not `0.0.0`) on the splash (D40) + `agent/hello`. | Accepted | Alex's call: one delivery path, one thing to keep working. D26's "just `apt install`" and D34's ".deb agent" were about *reach*; the depot (D35) already delivers to air-gapped edges with nothing but the LAN, so a second packaged path was dead weight + drift risk (two provisioning entry points, the never-wired `--no-pkg`, a first-boot oneshot). NB: an OTA self-update over the agent channel is the tracked follow-up (POL-28). |
| D42 | 2026-07-01 | **The `--kiosk` installer auto-reboots when it finishes, so the box cold-boots straight into content (POL-30).** After Stage B (substrate + `polyptic-agent setup` + retiring the Stage-A service), the one-liner now reboots the box itself instead of ending on a "run `sudo reboot`" hint. A short, `Ctrl-C`-able countdown (`POLYPTIC_REBOOT_DELAY`, default `5`s; `0` = immediate) precedes it; `--no-reboot`/`POLYPTIC_NO_REBOOT=1` opts out (reboot on your own schedule). **Agent-only installs (no `--kiosk`) never reboot** — the agent is already running + enrolled, and rebooting a headless box would be surprising. `reboot_now` degrades gracefully across `systemctl reboot` → `reboot` → `shutdown -r now`, and warns (doesn't hard-fail) if none exist. Mirrored in the built-in fallback `packages/server/src/install.default.sh` so both served scripts behave identically. The UTM/VM walkthrough deliberately passes `--no-reboot` to set the virtio-gpu `WLR_NO_HARDWARE_CURSORS` tweak before the first cold boot. | Accepted | Non-negotiable #4 (zero-click cold boot to content): a fresh kiosk box only renders after a cold boot, so making the operator remember `sudo reboot` left a manual step in a flow sold as "no further on-device steps, ever". The countdown + `--no-reboot` keep it safe at a console or in a VM; scoping it to `--kiosk` keeps headless enrolments untouched. |
| D43 | 2026-07-01 | **Boot-splash theme selection is `plymouthd.conf` + dracut-first, NOT the `plymouth-set-default-theme` helper (POL-7 fix). Refines D40.** `setup` now writes `/etc/plymouth/plymouthd.conf` `[Daemon] Theme=polyptic` — the portable selector that both plymouth AND **dracut** read — instead of relying on `plymouth-set-default-theme`, which **does not exist on Ubuntu 26.04** (dracut-based); its absence made `applyPlymouthTheme` bail out entirely, so the stock Ubuntu splash showed. The initramfs rebuild is **dracut-first** (`dracut -f` before `update-initramfs -u`): 26.04 ships an `update-initramfs` compat shim that left the real dracut image untouched, so the theme never got embedded (`lsinitramfs … \| grep polyptic` = 0). After the rebuild, setup **verifies** the theme is actually in the initramfs (`lsinitramfs`/`lsinitrd`) and warns if not. The compositor launcher now redirects its + the compositor's stdout/stderr to a logfile (`/tmp/polyptic-compositor.log`) so no VT text is drawn over Plymouth's retained splash frame during hand-off (`console=tty0`). `setup uninstall` restores the backed-up `plymouthd.conf` and rebuilds dracut-first. | Accepted | First real VM boot (Ubuntu 26.04/arm64, `wall1`) showed the STOCK Ubuntu splash plus leaking console text — D40 was only unit-tested, never booted. D40's helper-based selection is an initramfs-tools-only mechanism absent on dracut, and the rebuild order preferred a no-op shim. `plymouthd.conf` is the single selector both initramfs builders honour, so it works on 24.04 (initramfs-tools) and 26.04 (dracut) alike. Pre-existing duplicate D42 row (POL-30 landed it twice) removed here. |
| D44 | 2026-07-01 | **The player labels itself with the screen's FRIENDLY NAME, carried on every `server/render` (POL-29).** The idle splash (D39) + status badge showed the raw `?screen=` id (`screen-10`), so a screen renamed to "Left" in the console still read `screen-10` on the wall. Fix: `ServerToPlayerRender` gains a `friendlyName` field, **stamped from the registry at send time** (both send sites — `pushRender` in rest.ts and the `player/hello` handler in ws.ts) — NOT baked into the stored `ScreenSlice`, which stays pure render data (see `renameScreen`, which still does not bump the revision). The rename route now **re-pushes the current slice** at the *same* revision so the player relabels live (no reload, no "behind" ack). The player threads `msg.friendlyName` into reactive state (falling back to the id until the first render lands) and passes it to `IdleSplash` (prop `screenId`→`name`) and the badge (the POL-6 toggleable status badge — now shows the name, not the id). | Accepted | POL-29 — "screens are first-class and named" (non-negotiable #1): the wall must show the operator's name, exactly as typed, not the internal id/slug. Stamping at send (vs. on the slice) keeps the name as registry metadata and confines the change to two chokepoints; re-pushing on rename honours the instant-propagation non-negotiable. |
| D45 | 2026-07-01 | **POL-7 splash MADE TO ACTUALLY WORK on Ubuntu 26.04 — completes D43/D40, VM-confirmed on `wall1`.** After D43 selected the theme, the box still didn't show it, then crashed — diagnosed live over SSH by reproducing the crash on demand with `plymouthd --debug` (freeing the DRM from greetd). Two further causes: **(1) Theme not embedded.** dracut's `plymouth-populate-initrd` only bundles the *default* theme's plugin and 26.04 resolves no default, so `plymouthd.conf`'s `Theme=` didn't get our theme + `script.so` into the initramfs. Fix: an `/etc/dracut.conf.d/polyptic-splash.conf` `install_items` drop-in force-including the theme files + resolved `script.so`; setup then verifies both the theme AND the plugin actually landed (`lsinitramfs`). **(2) THE crash — missing text renderer.** Our `script` theme draws text via `Image.Text`; plymouth renders that through a **separate label plugin** which wasn't installed → text rendering disabled → console viewer never created → the script plugin dereferenced that NULL viewer in `ply_console_viewer_hide` during sprite refresh → **SIGSEGV every boot** (`plymouth-start.service: Failed with result 'signal'`). Fix: add **`plymouth-label`** (apt) / `plymouth-plugin-label` (dnf) to the substrate deps + air-gap bundle (Arch's `plymouth` bundles it); once present, `plymouth-populate-initrd` auto-bundles the whole text-render closure (pango/cairo/fontconfig + fonts) into the initramfs. Hardening (from adversarial multi-agent review): the script never holds an image-less sprite (lazy message sprite); the dracut drop-in only names files that EXIST so a missing rasterised PNG can't wedge every future `dracut -f`; a failed initramfs rebuild is surfaced to `needsVerification`, not swallowed; `setup uninstall` deletes a freshly-created `plymouthd.conf`. The live status line + progress bar are preserved (Alex's requirement — no pivot to a static `two-step` theme). | Accepted | D43 was unit-tested, never booted; the real root cause (a genuine plymouth NULL-console-viewer deref, which the label package sidesteps by enabling text rendering) was only visible on real 26.04 hardware. Keeping the live `script` theme was an explicit requirement, and the fix is a one-package dependency + deterministic initramfs bundling — no feature loss. |
| D46 | 2026-07-01 | **Netboot: a bare box HTTP-boots a live Polyptic image into RAM (no OS install), owned BY KEY not by who-answered-first (POL-33). The control plane is also the BOOT DEPOT.** Extends the depot (D35) with ungated top-level routes in `provision.ts`: `GET /boot.ipxe` returns an iPXE chain script with the control-plane base baked from the request Host (like `/install`) and, in GATED mode, the current enrolment token baked from the **live `Enrollment` instance** (new `Enrollment.currentToken`, the same value the agent WS accepts, so a regenerate re-keys the fleet with no drift); `GET /dist/image/:arch/{vmlinuz,initrd,squashfs}` streams the live-image artifacts with real **HTTP Range** (206/416, the squashfs is streamed into RAM); `GET /dist/ipxe/:file` streams the **tokenless** prebuilt boot medium (`polyptic-boot-<arch>.{efi,img}`), ungated like `/dist/agent` because it carries no secret (it only chains `/boot.ipxe`). One operator-facing route is GATED under `/api/v1`: `GET /api/v1/settings/netboot` → secret-free `NetbootInfo{baseUrl,mode,bootIpxeUrl,bootMediumUrl}` driving a **Console ▸ Settings ▸ Netboot** card. **Ownership = the boot key:** a box belongs to the server whose enrolment token its boot chain carries, so Polyptic never runs DHCP and multiple instances coexist for free. **The whole diskless fix is a STABLE identity, with ZERO server/protocol change:** a live image regenerates `/etc/machine-id` randomly each boot, so `deploy/live/…/derive-machine-id.sh` derives `POLYPTIC_MACHINE_ID` from `/sys/class/dmi/id/product_uuid` (MAC-hash fallback) and `parse-cmdline.sh` lifts `polyptic.server_url`/`polyptic.token` off `/proc/cmdline` into `/run/polyptic/agent.env` (a root oneshot `polyptic-agent-env.service`, `Before=greetd`), which the agent's existing `POLYPTIC_MACHINE_ID` override + `enroll.ts` case-4 re-attach turn into a silent re-enrol every boot. The **boot medium** offers **dongle** (fully diskless) or **offload** (`/boot.ipxe?offload=1` → the live image's `polyptic-offload.service` drops the loader into the *existing* ESP + adds a `efibootmgr` UEFI entry, never repartitions/wipes; the OS still streams into RAM every boot). `deploy/build-live-image.sh` (casper `netboot=http fetch=`, reuses `polyptic-agent setup`) + `deploy/build-ipxe.sh` are **Linux-build-host-only** (can't build on macOS); the pure identity layer IS unit-tested here (`bun test` + `deploy/live/test/identity.test.sh`). **amd64 first** (`${buildarch}` selects arch, arm64 is a drop-in follow-up). | Accepted | POL-33, the "zero-touch" story hid a manual OS install per box; netboot makes a box disposable (swap like a lightbulb) and answers "who owns a booting box on a shared LAN?" with the key it carries, not a DHCP race. Reuses the already-verified `POLYPTIC_MACHINE_ID` override + `enroll.ts` case-4, so the diskless box needs no new server/protocol surface. Ungated `/boot.ipxe` serving the token is acceptable: a valid token on a NEW box lands PENDING (case 1), so a human still approves, the same trust model as the `curl\|sh` one-liner. Secure Boot must be OFF (unsigned self-built iPXE), documented once for both modes; proxyDHCP-for-bare-metal stays an optional companion (Polyptic will not become a DHCP server). **Note (2026-07-02): the loader choice (unsigned self-built iPXE, Secure Boot off) is superseded by D47; the depot, identity, and ownership-by-key decisions stand.** |
| D47 | 2026-07-02 | **Netboot first stage = Ubuntu's SIGNED shim + network GRUB; Secure Boot stays ON (POL-33 pivot). Supersedes D46's loader choice (unsigned self-built iPXE); every other D46 decision (depot, stable identity, ownership by key) stands.** The chain: the firmware `db` (Microsoft UEFI CA 2011) verifies **shim 15.8**; shim's embedded Canonical certificate verifies **grubnet** (GRUB 2.12); GRUB's **shim_lock** verifier checks the Canonical-signed kernel **on the loaded buffer** (transport-agnostic, HTTP included) before executing it. We sign **nothing**, manage **no keys**, and redistribute exactly the binaries Canonical ships for its own netboot installer, extracted from **pinned noble pool debs** (deb + payload sha256 both pinned; the pins exist because of **SBAT revocation**: the GA noble grub build and shim's `.signed.previous` are already revoked, so pins are bumped deliberately, never floated). `initrd` + squashfs stay verification-exempt **by design** (`GRUB_VERIFY_FLAGS_SKIP_VERIFICATION`, the standard shim model; Ubuntu documents that initrds aren't validated); their trust is the LAN + control plane, unchanged, and a UKI is the tracked path to full-chain coverage. Surface: `GET /boot.ipxe` → **`GET /boot/grub.cfg`** (per-request GRUB menu, base + gated token baked in; aliased at `/grub/grub.cfg` + per-arch paths because an HTTP-booted grubnet resolves its baked `/grub` prefix at the **server root**); `GET /dist/ipxe/:file` → **`GET /dist/boot/:file`** (one **universal** dd-able `polyptic-boot.img`, both arches on one FAT32 stick, plus the four signed `.efi` loaders for offload + UEFI HTTP Boot); Fastify gains `ignoreDuplicateSlashes` (shim requests its second stage as `<dir>//grubx64.efi`); `NetbootInfo.bootIpxeUrl` → `bootConfigUrl`; `IPXE_DIST_DIR` → `BOOT_DIST_DIR`. The boot menu moves **server-side** (updatable without reflashing dongles); the medium is built by `deploy/build-boot-medium.sh` (downloads + text + mtools, **no compiler, builds on macOS**; replaces `build-ipxe.sh`); offload now drops the signed shim+GRUB pair into the existing ESP and never clobbers a foreign `/grub/grub.cfg` (marker-checked, add-only). **Also corrects a latent D46 bug:** casper has NO `netboot=http fetch=` mode (verified in casper source, 20.04 through 26.04); the real mechanism is **`iso-url=<url ending .iso>`**, casper wgets the whole ISO into a RAM tmpfs and loop-mounts the squashfs inside, so `build-live-image.sh` now emits a `polyptic.iso` wrapper (xorriso) and guards that the packaged kernel is Canonical-signed (grep of `sbverify --list` output, which always exits 0). The depot stays **plain HTTP by contract** (GRUB + busybox wget: no TLS, no redirects, no chunked; direct 200s with Content-Length), kept on the boot LAN. **Note (2026-07-09): the casper `iso-url=` payload mechanism is superseded by D55 (dracut `root=live:<bare squashfs>`, self-built initrd); the signed chain, the depot, and the plain-HTTP contract all stand unchanged.** | Accepted | iPXE is unsigned, so netboot forced Secure Boot OFF per box, which blocks Cyber-Essentials-style baselines and undercut the disposable-panel story on any hardened fleet. Ubuntu's shim + grubnet is the industry-standard signed netboot chain, already trusted by stock firmware, and the pivot removes the iPXE compile (the medium now builds anywhere) while moving the menu server-side. The `fetch=` cmdline would have failed on first real boot; catching it in research and pivoting to `iso-url=` in the same change avoided shipping a boot that never worked. |
| D48 | 2026-07-08 | **POL-38: the live image is verified END TO END in a hypervisor (UTM/arm64) — auto-boot → sway → gated enrol → approve → live player — and the four bugs that only a real watched boot could surface are fixed.** **(1) The VM test medium is a first-class build artifact:** `deploy/build-vm-test-iso.sh` (macOS or Linux, xorriso only, no root) remasters a stock casper BASE_ISO with the netboot payload's squashfs + a baked `polyptic.server_url`/`polyptic.token` GRUB cmdline, laying the boot metadata out the way Ubuntu ships it (`-append_partition 2 <ESP-GUID> esp.img` + El Torito `-e --interval:appended_partition_2:all::`) and **self-verifying** that the El Torito UEFI image and the appended GPT ESP are the same byte-identical region. This exists because the ad-hoc repack it replaces carried over only the FIRST 2048-byte sector of the ~5 MiB ESP (the post-20.10 "appended partition" remaster pitfall), so the firmware found no `\EFI\BOOT\BOOTAA64.EFI`, skipped the CD, and dropped to the UEFI shell — even the manual `FS0:` workaround was impossible against that image. **(2) The live rootfs now manages its own network:** the ubuntu-server-minimal squashfs ships NO netplan, so a live box left every NIC DOWN/unmanaged forever — netboot masked it because the initramfs `ip=dhcp` only configures the link long enough to fetch the image; after switch-root the agent could never reach the control plane. Fix: overlay netplan (`en*`/`eth*`, DHCP4+6, `optional: true`) + a `systemd-networkd-wait-online --any --timeout=15` drop-in so boot never blocks ~2 min on "ALL interfaces online". **(3) `setup` without `--server-url` now writes a REAL `agent.toml`** (display config; empty server_url/token are env-overridden per boot, and an existing agent.toml is never clobbered): previously only `.example` was written, so `POLYPTIC_BACKEND` stayed unset and the agent silently fell back to `dev-open` — sway up, screens "placed" in logs, **no browser ever launched**. **(4) surf on wayland-sway gets its X11 plumbing:** the browser installer adds `xwayland` alongside surf (sway starts Xwayland lazily but only if the binary exists), and the sway config imports `DISPLAY` into the systemd user environment (surf died with `Can't open default display` — WAYLAND_DISPLAY alone is useless to an X11-only client). Diagnosed live over a QMP socket added to the UTM VM (send-key + host screencapture; `systemd.debug-shell=1` for a root tty9). UTM operational notes now in NETBOOT.md: `virtio-gpu-gl-pci` + HVF on; the empirical render `auto` mode lands on hardware GL in UTM; VT-switching away pauses the seat's DRM so sway reports ZERO outputs while you debug on another VT (a diagnostic trap, not a bug). | Accepted | The OS half had only ever booted headless, where "boots" meant "casper mounted the squashfs" — every one of these four failures sat invisibly downstream of that. Watching one real boot end-to-end (the POL-38 acceptance) caught them all before the netboot-hardware pass (POL-39) would have chased them blind, and the self-verifying build script makes the broken-ESP class of repack unrepeatable. |
| D49 | 2026-07-08 | **POL-38 follow-through: the live image is a DOWNLOADABLE, SPLASH-BOOTING artifact.** **(1) The console serves the bootable ISO:** `/dist/image/:arch/polyptic-vm-test.iso` joins the image-depot whitelist (Range-streamed), `NetbootInfo.vmTestIsos` lists per-arch ISOs that exist on disk, and **Console ▸ Settings ▸ Netboot** shows "Download VM test ISO" next to the boot-medium download — netboot dongle and self-contained ISO are independent operator choices (some sites want no netboot). **(2) The live boot shows the Polyptic Plymouth splash** (quiet + branded logo/progress/live-status instead of scrolling text), which took THREE independent fixes, each dug out live in the UTM VM with `plymouthd --debug`: **(a) the theme must ride INSIDE the casper initrd** — plymouthd starts from the initramfs long before the squashfs is mounted, and the stock initrd ships text-only plugins; `build-live-image.sh` now runs Ubuntu's own initramfs-tools plymouth hook in the chroot (gated on `FRAMEBUFFER=y`; `DESTDIR/etc` pre-created to dodge the hook's cp-into-missing-dir bug that silently drops the font closure) and appends the harvested closure (theme, script.so, label-pango.so, fonts, fontconfig) to the shipped initrd as an extra zstd cpio segment — the Canonical initrd bytes stay untouched (D47) and the kernel's later-segment-wins unpacking applies our files; **(b) the hook resolves the theme via the `default.plymouth` update-alternatives entry, NOT plymouthd.conf** — `setup` now registers/pins the alternative when `plymouth-set-default-theme` is absent (26.04), which also fixes initramfs-tools *installed* boxes; **(c) `plymouth.ignore-serial-consoles` on every live cmdline** (netboot `/boot/grub.cfg` + the VM ISO) — arm64 VMs get a serial console implicitly from the devicetree, and ANY serial console makes plymouth assume a headless server and never paint the local display; a wall renders on its panel by definition. Verified end to end on a cold boot of the downloadable ISO: GRUB → branded splash with live status line → sway → silent re-attach → wall content, zero manual steps. | Accepted | "Boot to content" without the splash still LOOKS broken to a stakeholder watching a panel (non-negotiable #3/#4 spirit), and the ISO existing only as a hand-built file on one laptop is not a deliverable. The three splash causes were undiscoverable from code review — only a watched boot with plymouthd tracing exposed the initramfs timing, the alternatives-vs-conf split, and the serial-console suppression. Serving the token-bearing ISO is confined to the image depot with the same leaked-token-still-lands-pending trust model as `/boot/grub.cfg` (D46). |
| D50 | 2026-07-08 | **POL-39: the FULL netboot chain runs end to end in a UTM VM — all three provisioning flows verified: PXE-served loaders, USB dongle, and offload-to-disk.** The blocker chain, each found live: **(1) `computeBaseUrl` trusted a portless `Host:` header** — GRUB's http client sends `Host: <ip>` with NO port, so the menu we served over :8080 baked port-80 URLs into every follow-up fetch (kernel/initrd/iso-url/agent WS) and the box died with "connection refused" right after the menu; curl masks it because it always sends the port. Fixed by restoring the listener's port (from the fastify server address — Bun's http sockets don't reliably expose `localPort`) when the header has none; raw-socket regression test (fetch() cannot send a portless Host). **(2) UTM/QEMU's bundled EDK2 has NO HTTP-Boot driver** (only "UEFI PXEv4" is enumerated), so in that firmware the first stage is classic PXE — QEMU user-net's built-in DHCP+TFTP (`-netdev user,tftp=<root>,bootfile=shimaa64.efi` + `bootindex` on the NIC) serves shim+grubnet, and everything after GRUB is the identical HTTP flow; documented as the firmware-support caveat on the HTTP-Boot page. **(3) The dongle's firmware-NIC dependency is confirmed and has a clean recipe**: with the NIC merely PRESENT in the firmware boot order (bootindex), EDK2 connects the network stack even when booting from USB, and dongle-GRUB is online — the POL-39 `net_ls_cards`-empty finding reproduced and resolved. **(4) offload was a half-install:** ubuntu-server-minimal ships NO `efibootmgr`, so `offload.sh` (set -eu) installed the loaders on the ESP and then died before registering the UEFI boot entry — looks done, never self-boots. The image build now installs efibootmgr and offload.sh pre-flights it LOUDLY before touching the ESP. Also: two user-mode NICs get identical 10.0.2.0/24 subnets and GRUB's routes become ambiguous — one NIC for VM netboot tests. Measured, cold power-on to wall content: PXE ~60s, dongle ~30s, offloaded-disk ~70s (incl. the ~1.4G image streamed to RAM). NETBOOT.md gains the verified UTM lab walkthrough + the dongle/HTTP-Boot caveats. | Accepted | POL-39's whole premise ("GRUB sees no network card") dissolved into four precise, separately-fixable causes once the chain ran under observation — two of them (`computeBaseUrl`, missing efibootmgr) were real product bugs that would have hit every non-curl client and every offload on real hardware. The three verified flows map one-to-one onto the operator choices the console now offers (live ISO / dongle / offload), leaving real-hardware UEFI HTTP Boot as the only untested variant, deliberately deferred to the on-hardware pass. |
| D51 | 2026-07-08 | **POL-41: the baked image gets a fully automated update loop — scheduled server-side refresh + a 5-minute box-side poll; "update" = "rebuilt image + reboot" because a diskless box re-pulls its whole OS at every boot.** **Server half:** Console ▸ Settings ▸ **Image updates** runs a rebuild hook on an operator schedule (default 01:00 server-local; also a Rebuild-now button and an **URGENT** roll-out switch). The hook is a COMMAND by contract (`IMAGE_REBUILD_CMD`) because the control plane may live on a laptop or an unprivileged container: the ready-made `deploy/rebuild-image-docker.sh` runs the refresh in a privileged Linux container on any host. `deploy/refresh-live-image.sh` does the real work: unsquash the CURRENT image, chroot `apt-get upgrade` (kernel stays HELD — the D47 signed-kernel/initrd invariant; kernel CVEs need a full rebuild from a newer base ISO, documented), re-wrap, stamp a new image id. **Nothing to upgrade → artifacts untouched** (no id churn, no pointless fleet reboots; FORCE=1 overrides). **Identity:** every build/refresh bakes `/etc/polyptic/image-id` into the squashfs and publishes `image-id.txt`; the server serves `GET /dist/image/<arch>/manifest.json` `{imageId, builtAt, sha256, urgent}` — ungated and secret-free like the artifacts themselves (a leaked urgency flag only makes boxes reboot into the image this same depot serves). **Box half:** `polyptic-update-poll.timer` (5 min) compares the booted id against the manifest; mismatch + urgent → reboot NOW splayed 0–4 min per box (hash of the stable machine id, no thundering herd); mismatch + not urgent → reboot only in the 03:00–04:59 window, so a 01:00 refresh rolls the fleet the same night invisibly. **Netboot-only by guard:** the poll exits unless `iso-url=` is on the cmdline — an ISO/USB-booted box would re-boot the same stale medium (loop risk), those refresh by re-flashing. Store gains a persisted `image_rollout` row (schedule, urgency, last hook run with log tail); protocol gains `ImageManifest`/`ImageUpdateInfo`; the manual rebuild endpoint is non-blocking (persists "running", spawns, returns — a build takes ~15 min). | Accepted | The immutable-image model's one real cost is staleness, and both existing options were bad: hand-rebuilding (never happens) or unattended-upgrades inside the live overlay (lost at reboot, mutates the "immutable" box, and drifts from the served image). Rebuilding at the SOURCE keeps images reproducible and boxes identical, the no-change short-circuit keeps walls stable, and the urgent switch gives a one-click critical-CVE story measured in minutes. The hook-command contract keeps the server honest about privilege (it never chroots anything itself). |
| D52 | 2026-07-09 | **POL-43: the control plane runs on Kubernetes (Helm chart brought up to the netboot line) + a SECOND update cycle closes the kernel-CVE gap.** **Two cycles (server-side, both in Console ▸ Settings ▸ Image updates):** the nightly in-place apt refresh (POL-41, kernel held per D47, default 01:00, `IMAGE_REBUILD_CMD`) is joined by a **weekly FULL rebuild from the base ISO** (default Sundays 02:00, `IMAGE_FULL_REBUILD_CMD`, day+time+enable persisted; a same-minute collision resolves full-wins because full supersedes refresh). The daily cycle CANNOT roll kernel CVEs — the kernel is pinned to the base ISO's Canonical-signed build — so the weekly rebuild (new `deploy/full-rebuild-image-docker.sh` locally: caches the base ISO under `deploy/dist/cache/`, builds the agent if missing, runs `build-live-image.sh` in a privileged container) is what keeps the kernel current, and any Canonical-signed kernel keeps Secure Boot working. `POST /api/v1/settings/image/rebuild` takes `{kind: refresh\|full}`; `lastBuild.kind` surfaces which cycle ran. **Kubernetes (chart `deploy/helm/polyptic`, updated from its pre-POL-33 state):** the depot (live image + signed loaders) moves to a **PVC** (`netboot.persistence`, keep-on-uninstall) served via `IMAGE_DIST_DIR`/`BOOT_DIST_DIR`; the **hook contract stays a command** — in-cluster it becomes `bun deploy/k8s-run-job.ts refresh\|full`, a dependency-free bun script that POSTs a chart-rendered Job template (ConfigMap `-rebuild-jobs`, mounted at `/etc/polyptic/jobs`) to the API with the pod's ServiceAccount, waits, and relays the pod-log tail into the Settings card (RBAC: jobs create/get, pods list, pods/log get — nothing else). The Jobs run **privileged** (chroot + loop mounts) in plain `ubuntu:24.04` on a Linux node of `imageUpdates.arch`, take their scripts from an **initContainer running the server image** (version-locked, zero chart-side duplication — `server.Dockerfile` now ships `refresh-live-image.sh`/`build-live-image.sh`/`build-live-iso.sh`/`k8s-run-job.ts`/`deploy/live/`), write to the shared depot PVC, scratch in an emptyDir at `/var/tmp`, and carry a **required pod-affinity to the server's node** because the depot PVC is RWO. Day-0 in-cluster: the depot starts empty; "Full rebuild now" downloads the base ISO onto the PVC (cached at `cache/`) and publishes the first image — the cluster's Linux nodes make the image build work even when the operator's machine is macOS. GRUB/casper speak plain HTTP, so netbooting boxes use an http LoadBalancer/NodePort, never the HTTPS Ingress (documented in the chart README). **Dev workflow (README + `values-dev.yaml`):** the fast inner loop stays `bun run dev` on the host; chart iteration is `helm upgrade --install -f values-dev.yaml` (seconds, no image); code-into-cluster is a rare local `docker build` + upgrade (`pullPolicy: Never`, OrbStack/Docker Desktop share the daemon; kind needs `kind load`). | Accepted | The user runs Kubernetes and wants the control plane on the cluster ("alleviates the endpoint challenges — it can run as a job"); kernel CVEs were previously a MANUAL rebuild (i.e. silently ignored); k8s Jobs are the natural incarnation of the D51 hook contract and delete the macOS-can't-build-ISOs problem in dev too. Verified live on OrbStack k8s (arm64): chart installs, pod healthy, both hooks configured, refresh Job correctly fails "no payload" on an empty depot with the log tail relayed to the console, and the full-rebuild Job bootstraps the depot from the official Ubuntu 26.04 ISO. |
| D53 | 2026-07-09 | **Content auth (POL-24) = credential profiles: OAuth CLIENT-CREDENTIALS at the operator's IdP, token stamped into the URL at SEND time.** First real strategy through the D11/D17 Bucket-A seam. A **CredentialProfile** is a new managed entity (`{name, strategy:"oauth-client-credentials", tokenEndpoint, clientId, clientSecret, scope?, audience?, tokenParam:"auth_token"}`): the server exchanges it for short-lived JWT access tokens (background refresh at ~75% of `expires_in`, backoff on failure) and **stamps the current token into web/dashboard surface URLs at the two `server/render` send sites** (`?auth_token=<jwt>` — Grafana `[auth.jwt]` `url_login` style, validated against the IdP's JWKS). Stored slices + the DB always keep the CLEAN url (same stamp-at-send pattern as `friendlyName`, D44), so no token is ever persisted, a cold-booting/reconnecting screen always loads with a live token, and a routine refresh never rewrites (= reloads) a live iframe — the running page is carried by the target app's own session. The token-usable EDGE (first fetch / recovery after a fixed secret) re-pushes renders so a screen stuck on a login page heals itself. `ContentSource` gains `credentialProfileId` (many sources → one profile; many screens → one source = the "authenticate once, share everywhere" requirement). Secret is WRITE-ONLY (never in `admin/state`, REST responses, or logs); delete is REFUSED (409) while in use. Console: "Access credentials" section + per-source Authentication picker on the Content page (the design's deferred slot). Alternatives parked on the same seam: `persisted-session` (apps we can't configure), `reverse-proxy-header-injection`, device grant (wall-as-person). IdP-side changes live in ACS (PR #668 `serviceAccountsEnabled` passthrough + Grafana 13 bump PR #670) + per-deployment values. Grafana gotcha: `[auth.jwt]` refuses a plain-http `jwk_set_url` unless app-mode=development (exemption absent in 10.0.1, present in 12.x/13.x) — https deployments unaffected. NB originally built 2026-07-09 but lost UNCOMMITTED in a branch switch; rebuilt from the design note + surviving tokens.ts/test. Full design note incl. Grafana + IdP config walkthroughs: see POL-24. | Accepted | Grafana behind OAuth is the top content blocker; the auth-code flow humans use is deliberately un-automatable, while client credentials is the SAME IdP's machine grant — central, unattended, revocable, vendor-neutral (endpoint+id+secret+scope/audience are the four knobs every compliant IdP exposes). No proxy to deploy, nothing vendor-named in core paths. |
| D54 | 2026-07-09 | **POL-45: Settings rebuilt to the design, and the image depot keeps a BUILD HISTORY whose active member is operator-chosen — so rollback is free.** **Depot layout** (per arch, under `IMAGE_DIST_DIR`): `builds/<imageId>/{polyptic.iso,vmlinuz,initrd,SHA256SUMS[,polyptic-live.iso]}` holds every retained build, and the arch ROOT holds the ACTIVE one. The root is byte-for-byte what it always was, so the whole boot chain (`grub.cfg`'s `iso-url=`, `/dist/image/<arch>/…`, every already-written USB dongle) is untouched. **Activating a build = relink the root + rewrite `image-id.txt`**, and since every netbooted box already compares `manifest.json`'s `imageId` against its own `/etc/polyptic/image-id` every 5 minutes (D51), that IS the fleet rollback — urgent → minutes, else the nightly window. No new fleet mechanism, no agent change, no protocol change on the box side. **Sharing rule:** the two multi-GB ISOs are HARDLINKED between root and build dir (the active build costs no extra bytes); `vmlinuz`/`initrd`/`SHA256SUMS` are COPIED, because `refresh-live-image.sh` writes SHA256SUMS with shell `>` and the kernel artifacts with `cp` — both truncate the inode IN PLACE, which through a hardlink would silently rewrite a retained build's artifacts. Retention is `IMAGE_RETAIN_BUILDS` (chart: `imageUpdates.retainBuilds`, default 3, ~2GB each → depot PVC default raised 15Gi→30Gi); prune drops oldest-first and NEVER the active build, even when the operator has rolled back onto the oldest. A pre-POL-45 depot is adopted into `builds/` on boot. New surface: `ImageBuild`/`ImageUpdateInfo.builds`, `POST /api/v1/settings/image/activate`, and ungated `GET /dist/image/:arch/builds/:imageId/:file` (id whitelisted to `^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$`, so `..` can never escape, plus `safeResolve`). **Console:** the section rail is gone; Netboot becomes **Onboard Screens** — the auto-updating bootloader as the one primary download, a "Make a bootable USB" modal (Etcher, three steps), Recent builds with per-row Activate + live-ISO download, and a ⋯ menu carrying Build update / Full rebuild / Deploy latest to fleet immediately (the old `urgent` switch, renamed to what it does, behind a confirm). Image updates becomes **Update schedule** (schedules only; the rollout-window note points at the ⋯ menu). UEFI HTTP Boot / DHCP option-67 survive as secondary info behind a "Boot without a USB stick" disclosure. | Accepted | The old Settings buried a fleet-wide reboot next to a theme toggle, and "Recent builds" in the new design implied history the depot did not keep. Retention was the honest way to render it — and once three builds exist on disk, making one ACTIVE costs nothing extra and turns the existing 5-minute poll into a rollback path, which is the capability an operator actually wants after a bad image reaches a wall. Hardlinking only the artifacts the build scripts replace atomically is the one non-obvious constraint; it is pinned by unit tests because getting it wrong corrupts history silently rather than loudly. |
| D55 | 2026-07-09 | **POL-35: the netboot root image is built UP from `ubuntu-base` and shipped as a BARE squashfs booted by dracut — 1.41 GiB → 491.8 MiB (−66%), and the netboot RAM floor halves from ~4 GB to ~2 GB. The D47 signed chain is byte-for-byte unchanged.** Two changes, composed. **(A) Build up, not down.** `deploy/build-live-image.sh` no longer unsquashes a live-server ISO's rootfs (`BASE_ISO` is gone, and with it `xorriso`): it unpacks the `ubuntu-base` 26.04 tarball and apt-installs the kernel, dracut, a curated firmware set, and the same `polyptic-agent setup` substrate. **The one non-obvious trap: `linux-image-generic` *Depends* on `linux-firmware`** (which Depends on all eighteen per-vendor packages, ~600 MB) — a hard dependency `--no-install-recommends` cannot decline — so the build resolves and installs the **concrete** `linux-image-<abi>-generic`, whose deps are only kmod/linux-base/linux-modules. Firmware becomes `linux-firmware-minimal` + a coarse GPU/NIC allowlist, with `FULL_FIRMWARE=1` as the escape hatch for unanticipated hardware. Docs/man/locales are excluded at unpack time via a `dpkg.cfg.d` `path-exclude` (not deleted afterwards, so a nightly refresh cannot reinstate them), and `/boot` is stripped before `mksquashfs` because kernel+initrd reach the box over the boot chain, never inside the root image. **(B) casper → dracut.** The cmdline moves from `boot=casper iso-url=<…>.iso` to **`root=live:<…>/rootfs.squashfs rd.overlay=1 ip=dhcp rd.neednet=1`**; the ISO wrapper, `layerfs-path=`, `filesystem.size`, the `casper-uuid` matching dance and the Ubuntu `---` terminator all disappear. `livenet` fetches with **curl** (DNS names, redirects, retries — the D47 busybox-wget caveat is retired) and `dmsquash-live` auto-detects the flat squashfs (`/usr` present → overlayfs required). **The initrd is now OURS**, built by `dracut --no-hostonly` *inside* the chroot: kernel, `/lib/modules` and initrd come from one apt transaction, so the `apt-mark hold` gymnastics and the byte-identical-initrd constraint — the #1 netboot footgun, a standing constraint on every future package change — are **structurally gone rather than defended against**. It also deletes the POL-38 Plymouth machinery (initramfs-tools hook harvest + appended zstd cpio segment): dracut's own `plymouth` module bundles the theme named in the `/etc/dracut.conf.d/polyptic-splash.conf` that `setup` already writes (D45), and the initramfs-tools multipath no-op overrides give way to `--omit multipath`. The POL-46 tmpfs fix survives as a first-class dracut module (`50polyptic-live`): remount `/` to 90% before livenet runs, and warn below ~1.5 GB naming the live ISO. **`rd.live.ram=1` must never be set** (it `dd`s a second full copy into RAM); an e2e test asserts the menu never emits it. **Secure Boot is untouched**, and this was the load-bearing risk: apt's `linux-image-<abi>-generic` is the *same* Canonical-signed PE the ISO carried (both from the `linux-signed` source), so the existing `sbverify` guard — repointed at the chroot's `/boot/vmlinuz-<kver>` — passes unchanged, verified on the arm64 build. shim 15.8 → grubnet → shim_lock-verified kernel; the unsigned dracut initrd is exactly as legitimate to shim as the unsigned casper initrd it replaced. **Blast radius:** depot artifact `polyptic.iso` → `rootfs.squashfs` (whitelist, build history, `SHAREABLE` hardlink set, manifest checksum lookup, boot summary); the box-side update poll's netboot guard moves from `iso-url=` to `root=live:http` (an ISO boot reads `root=live:CDLABEL=POLYPTIC`); `build-live-iso.sh` lays down our own kernel/initrd/`LiveOS/squashfs.img` and keeps `BASE_ISO` **only** for its signed ESP + GRUB prefix, so its release need no longer match the payload's; the Helm rebuild Jobs and both Docker hooks drop `xorriso`/`cpio`. A pre-POL-35 depot's `polyptic.iso` builds drop out of Recent builds rather than offering an image the cmdline cannot boot; the next full rebuild re-bootstraps it. **Verified:** the real arm64 artifacts boot under QEMU from a bare HTTP squashfs — livenet fetch → dmsquash-live overlayfs → systemd multi-user → `polyptic-agent-env` — and a 2 GB box boots. Real-hardware pass (amd64, Secure Boot ON, curated firmware against actual GPUs/NICs) is the outstanding risk and rides the existing hardware pass. | Accepted | Field data (2026-07-09, three real boxes): the 1.41 GiB image put netboot's floor at ~4 GB of RAM and killed two of three kiosk boxes outright, because casper's `iso-url=` wgets into a tmpfs the kernel caps at half of RAM — the image cost was paid twice. Every GiB shaved off the squashfs removes a GiB from that floor, since it lives in RAM for the whole session; ~500 MiB puts netboot within reach of the 2 GB class of hardware actually on the wall. The investigation (POL-35 comments) measured that the size win lives entirely in (A) and that (B) alone saves <1 MiB — but (B) is what makes (A) clean: a from-scratch rootfs needs its own initrd anyway, and once you are building one, dracut's `root=live:` removes the ISO wrapper, the uuid dance and the footgun for free. Distro switch, EROFS, xz+BCJ and zstd-22 were assessed and rejected (same size band for weeks of cost, or measured as marginal/arm64-hostile); they are recorded here so they are not re-litigated. |
| D56 | 2026-07-10 | **POL-56: both web apps ship a favicon, sourced from `brand/` and COMMITTED into each app's Vite `public/` dir.** The set is the modern minimum: `favicon.svg` (a single file whose holder/panel fills flip on `prefers-color-scheme`, so one icon reads on a light *and* a dark tab strip), `favicon.ico` (16/32/48, the fallback for browsers that ignore SVG icons — Safari — and the answer to the bare `/favicon.ico` every browser requests anyway), plus `apple-touch-icon.png` on the console only. The `.ico` carries the CANONICAL mark (dark holder, light panels): it cannot adapt, and a light-on-dark mark stays legible on either strip, whereas the inverted variant would vanish into a light one. `brand/generate.sh` is extended to pack the `.ico` (`brand/make-ico.ts`, PNG-in-ICO, hand-rolled so librsvg stays the only binary dep) and to copy the set into `packages/{console,player}/public` — the copies are committed because `vite build` runs inside the Docker image, where there is no librsvg. Paths in `index.html` are ROOT-ABSOLUTE, which Vite rewrites to the player's `/player/` base at build. | Accepted | The console had no favicon at all — browsers showed a blank page glyph and logged a `/favicon.ico` 404 on every load. `brand/` already existed and its README already named favicons as its purpose, so this is wiring, not new design. Pointing Vite's `publicDir` across packages at `brand/` was rejected: `deploy/server.Dockerfile.dockerignore` already trims `docs`/`README.md` from the build context, so a future line adding `brand` would silently ship icon-less images. Relative hrefs were rejected too — the player's history fallback serves the same `index.html` at `/player/screen/<id>`, where `favicon.ico` would resolve one level too deep. |
| D57 | 2026-07-10 | **The console's enrolment story leads with the netboot bootloader (POL-48).** The cold-start wizard's STEP 1 and the Machines enrolment card still described the D41 world: "run this `curl … \| sh -` on a fresh Ubuntu box, here is your bootstrap token". That stopped being how a machine joins the fleet at D46/D47 (netboot) and D54 (bootloader download in Settings ▸ Onboard Screens). STEP 1 is now: **Download bootloader** (the same `bootMediumUrl` Settings serves) → flash to a USB stick → boot the machine Secure-Boot-on, whereupon it streams the live image into RAM and dials in with the control-plane address and token that came down with its boot menu. The token is no longer printed in the wizard, because a netbooted machine never needs it typed. When no medium is bundled, the wizard points at Settings ▸ Onboard Screens rather than dead-ending. Copy plus one download button: no new endpoints, no protocol change. | Accepted | The wizard is the first thing a new operator reads, and it was teaching a provisioning path the product had moved off. |
| D58 | 2026-07-10 | **`GET /install` and the substrate-bundle depot are REMOVED. Netboot is the only supported provisioning path. Supersedes D41.** Deleted: the `/install` route + `deploy/install.sh` + the built-in `install.default.sh` fallback template (and `INSTALL_SCRIPT_PATH`), the `GET /dist/deps/:distro/:arch/{manifest.json,:file}` routes + `DEPS_DIST_DIR` + `deploy/bundle-deps.sh` + the Dockerfile's `BUNDLE_DEPS` arg, and `packages/e2e/install.e2e.test.ts`. **Kept:** `GET /dist/agent/:arch` and `AGENT_DIST_DIR` — no boot path fetches them (`build-live-image.sh` reads the binary off local disk and apt-installs the substrate in its chroot), but the route is where agent OTA (POL-28) lands, and the binaries must stay in the server image for in-cluster rebuild Jobs. `deploy/build-agent.sh` stays: it feeds the image build. Verified after the cut: `/install` and `/dist/deps/**` 404, `/boot/grub.cfg` + `/dist/boot/*` + `/dist/agent/:arch` unchanged, 247/247 tests green. | Accepted | Alex's call. D41 kept the one-liner as "the ONE delivery path" for a box that already had an OS; netboot (D46/D47/D55) removed the need for the box to have an OS at all, so the one-liner became a second, unsupported provisioning entry point that drifted (no image updates, no rollback, its own air-gap depot to keep working). One path, one thing to keep working — the same reasoning D41 used to delete the `.deb`, applied to D41. |
| D59 | 2026-07-10 | **POL-55: an operator can reboot ONE named machine from the console, and the agent's escalation to do it is `touch`, not `sudo`.** New surface: `POST /api/v1/machines/:id/reboot {reason?}` → `server/reboot` down that machine's live agent socket, answered by `agent/reboot-ack {accepted, reason?}`; a Reboot button on Console ▸ Machines, shown only while the machine is **online** (the reboot rides the socket). 404 unknown, **409** when the machine is not approved or has no live socket — an undelivered reboot must never read as a delivered one. This is the OTHER DIRECTION from D51's fleet roll-out: there, each box polls the image manifest and reboots *itself*; here the control plane pushes at one box that has wedged (compositor dead, browser hung) but still holds an agent socket. **The privileged half:** the agent runs unprivileged as the kiosk user, and the D55 image ships neither `sudo` nor polkit — so logind refuses an unprivileged `systemctl reboot` outright. Rather than install either (a general-purpose escalation on a kiosk, for one action), `polyptic-agent setup` writes a root-owned systemd pair whose ONLY capability is rebooting: `polyptic-reboot.path` watches `PathExists=/run/polyptic/requests/reboot`, and `polyptic-reboot.service` deletes that file (so a failed reboot cannot re-arm the watcher) and runs `systemctl --no-block reboot`. A `tmpfiles.d` drop-in creates the request dir `0770 root:<kiosk>` each boot — **only** that sub-directory, never `/run/polyptic` itself, which holds `agent.env` and its bootstrap token. The agent therefore escalates by creating an empty file: no command string, no argument, nothing to smuggle, and `/run` being tmpfs means a request cannot outlive the reboot it caused. A box with no helper (a dev Linux host, a hand-rolled image) falls back to plain `systemctl reboot`. **Refusals are first-class:** `dev-open` (the laptop backend) and any non-Linux host decline and say why, so a stray reboot can never power-cycle a developer's desktop; the reason rides back on the ack into the Live Activity feed, because a refused reboot leaves the box up and the operator's click otherwise did nothing visible. | Accepted | Alex asked whether the mechanism was already there. It half was: D51 gave boxes a reason to reboot themselves, not the control plane a way to reboot one of them — and "the wall is frozen" is exactly the case where the image is fine and the box is not. The agent-channel push (`agent/ident`, `agent/capture`) was the obvious carrier; the only real design question was privilege, and a systemd path unit answers it with strictly less attack surface than a sudoers line, no new package in a 492 MiB image, and a helper that is *incapable* of doing anything but reboot. |
| D60 | 2026-07-10 | **POL-58: installing the bootloader is CONFIRMED, VERIFIED, and REPORTED — never silently assumed.** Three changes, all downstream of one bug class. **(a) Verified, not assumed:** `offload.sh` re-reads the UEFI boot variables after writing them and asserts `Polyptic Netboot` both exists and *leads* `BootOrder`; nothing is stamped, said, or reported as installed until the firmware itself agrees. The unit's `SuccessExitStatus=0 1` is gone, so a failed install is a failed unit. **(b) Chosen, not guessed:** the target ESP is filtered (removable media excluded outright) then scored by `PARTUUID` against the firmware's existing boot entries; a genuine tie aborts and names the `polyptic.offload_disk=` override rather than picking one. Stale `Polyptic Netboot` entries are pruned and re-created against the ESP actually written, and `BootOrder` is set explicitly. **(c) Reported, not journalled:** every outcome goes to the boot splash *and* to `POST /boot/report` → one Live Activity line (`BootReportBody`/`BootReportCode` in `protocol`; token-gated when the fleet is gated; throttled). The GRUB menu gains a **confirmation submenu defaulting to "No"** whose entry titles state plainly that nothing is erased. The removable-media fallback path `EFI/BOOT/BOOT<arch>.EFI` is claimed **only when empty**. `deploy/live/test/offload.test.sh` drives the whole tree against stubs (no root, no disks, macOS/Linux/CI). | Accepted | A real box with Ubuntu on it "claimed to offload" and then booted straight back into the old Ubuntu. Every failure path in the script exited quietly into a journal on a RAM disk that is discarded at reboot, the unit laundered exit 1 into success, and GRUB's `echo` had already announced the install before anything was attempted — so a box that never received a boot entry was indistinguishable from one that did. The VM pass (POL-39) could not have caught it: it offloaded onto a *blank* ESP on a disk with no OS, no competing boot entries, and cooperative EDK2 NVRAM. The operator also expected an "are you sure?" step; the honest answer — Polyptic never erases anything — is exactly what is worth saying at the moment of choosing, so it is said in the menu titles, the console's USB modal, and on success. Aborting on an ambiguous ESP is deliberate: the old first-`lsblk`-wins behaviour is a coin toss that fails silently, and a coin toss that writes boot entries is worse than a message naming the flag that resolves it. |
| D61 | 2026-07-10 | **The netboot GRUB menu stays FLAT and keeps its established entry names; `arch`/`net` are exported. Supersedes the confirmation-submenu half of D60.** The three entries are `Polyptic (Live)`, `Polyptic (Offload Bootloader)`, `Polyptic (Debug Console)` — siblings, no submenu, no interstitial "are you sure?" page. The `set arch=` / `set net=` lines that precede them are now `export`ed. | Accepted | The submenu D60 added was **broken**, and broken in a way only a real boot revealed: GRUB opens a fresh environment context for a `submenu` and copies only EXPORTED variables into it (`normal/menu.c`: `if (entry->submenu) grub_env_context_open()`), so inside it `$net` and `$arch` were empty and the offload entry asked the firmware for `/dist/image//vmlinuz` — *file not found*, on a box whose top-level live entry had booted seconds earlier. A plain `menuentry` never opens a context, which is exactly why the flat entries always worked and why the guard entry did not. `export` fixes the mechanism, and is kept so a future nesting cannot silently reintroduce it, but the guard itself is gone: offload is non-destructive by construction (D60's real content — verify, choose, report — stands), so it never needed an interlock, and the reassurance belongs in the console where the operator reads it *before* walking to the box, not in a menu they are trying to get past. Renaming the entries was churn: operators know these three names. |
| D62 | 2026-07-10 | **POL-57: page zoom is a property of the (target, page) PAIR, and it lives on the surface.** `WebSurface`/`DashboardSurface` gain `zoom` (a scale factor, 0.25–4, default 1); media has none. `PUT /api/v1/screens/:id/zoom` and `PUT /api/v1/walls/:id/zoom` patch the EXISTING surface's zoom and re-push — same stable surface id, same url, so the player restyles the live iframe and the page never reloads (D5). The server persists the value in a new `zoom_preferences` table keyed `(target_id, source_key)`, where a target is a screen or a wall and a source_key is `source:<id>` for a library source or `url:<url>` for an ad-hoc link; assigning that page to that target again RESTORES the zoom, across scene switches and server restarts. Keying on the source **id** (not its url) means re-pointing a library source keeps each screen's dialled-in zoom. The player implements zoom the way a browser does — lay the iframe out at `1/zoom` of the box it must fill, then `scale(zoom)` it back up — so the embedded page sees a smaller CSS viewport and genuinely re-lays-out (media queries, `vw`, `rem` all respond); it composes with the 3b span as `translate(…) scale(…)`, leaving the span math untouched. A wall member is refused (409) and defers to its combined surface, where every member takes the same zoom. Console: a browser-style −/value/+ control in the Inspector, shown only when the selection frames a page. | Accepted | "Remember per screen/site combination" was the ticket's whole ask, and it rules out the two simpler models: a per-source zoom can't serve a 4K panel and a 1080p one differently, and a per-screen zoom can't survive a screen cycling through pages. A `transform: scale()` on a full-size frame was rejected — it magnifies a page laid out for the wrong viewport (clipping, wrong breakpoints) instead of zooming it. Zoom rides on the surface rather than in a side-channel so the player stays a dumb reconciler of one message, and a scene apply re-resolves it for free. |
| D63 | 2026-07-10 | **POL-50: `surf` is the ONE kiosk browser, and the wall's dev tools are shown ON the wall. Supersedes D27's browser clause (`.deb` Chromium, `cog` fallback).** **(a) The browser seam is gone.** Polyptic shipped three kiosk-browser implementations behind a `Browser` interface, but on the only OS it provisions (Ubuntu 26.04, netboot-only since D58) exactly one is installable — verified today: `apt-cache policy chromium` → **no candidate**; `chromium-browser` → `2:1snap1-0ubuntu4`, the confined snap a kiosk must avoid (D27); `cog` → **not packaged** since 25.10. The remaining path to Chromium was `ppa:xtradeb/apps` (single-maintainer, which `browser.ts` itself flagged), sitting oddly beside a boot chain of deliberately-pinned Canonical debs under Secure Boot (D47), and costing ~275 MiB installed vs surf+WebKitGTK's ~96 MiB on an image that is streamed into RAM every boot (492 MiB, ~2 GB floor — D55). **Nobody ever decided surf**: the image went `cog` → `auto` (`cog` if packaged, else `surf`) → hard-coded `surf` when 25.10 dropped cog, while the setup CLI still defaulted to `chromium`. Two provisioning paths, two browsers, no decision. So: `backends/{chromium,cog,browser}.ts` deleted, supervision generalised into `backends/supervise.ts`, `--browser`/`--chromium-deb`/`--chromium-ppa`/`POLYPTIC_BROWSER`/`agent.toml browser` removed, and no Chromium reference survives outside this log. **(b) Remote dev tools are IMPOSSIBLE on this browser, so the inspector is on-screen.** POL-50 asked for a central "Inspect" button tunnelling the debug port. Chromium would allow it (`--remote-debugging-port` → loopback-bound CDP + a bundled DevTools frontend at `/devtools/inspector.html`; note it 500s any request whose `Host` isn't an IP/localhost, and its frontend CSP is `connect-src 'self' ws://127.0.0.1:*`). **WebKitGTK does not.** Measured against `libwebkit2gtk-4.1 2.52.3` on Ubuntu 26.04: `WEBKIT_INSPECTOR_SERVER=host:port` opens a socket that answers **neither HTTP nor a WebSocket upgrade** (it accepts and holds); `WEBKIT_INSPECTOR_HTTP_SERVER` binds a port, displaces the former onto a random one, and **still answers no HTTP**; the frontend strings (`Main.html?ws=…/socket/…`, "Inspectable targets") exist inside libwebkit but nothing serves them. The intended client is another WebKitGTK app opening `inspector://host:port` — and **surf cannot even load that URI** (it prepends `http://` to any unknown scheme and tries to resolve the host "inspector"). There is therefore nothing to tunnel to a Chromium/macOS operator. **(c) What shipped instead.** `POST /api/v1/screens/:id/inspect {on}` (session-gated, 202) → `server/inspect` → the agent relaunches THAT output's surf with **`-N`** (surf takes its inspector only at launch), focuses the window, and synthesises **Ctrl+Shift+O** followed by a reload. Two traps, both silent: `xdotool key --window <id>` uses `XSendEvent`, which **GTK ignores** — it must be **XTEST** (`xdotool key`, no `--window`) against the focused window; and WebKit's inspector does **not backfill**, so without the reload the Console/Network tabs of an already-finished load are empty — which is precisely the failure POL-49 needed to see. Turning it off relaunches without `-N`, re-sealing the kiosk: developer extras are never one keypress from a public panel by default. The agent answers `agent/inspect-ack`, and that ack — never the operator's click — is the sole writer of `ScreenView.inspecting`, so the console can never badge an inspector that isn't on the glass; the flag is ephemeral and cleared when the machine drops. A REFUSAL needed its own field (`ScreenView.inspectError`), found by screenshotting the real UI: a refusal leaves `inspecting` false — i.e. UNCHANGED — so the console could not distinguish "the wall said no" from "the wall has not answered yet", and its button spun on "Opening…" until a 20s timeout for precisely the case the operator most needs to hear about. The reason now rides on the screen, is cleared when a fresh request is delivered, and is toasted verbatim. Opening the inspector belongs to the LAUNCH rather than to the operator's click, so a browser that crashes and respawns while being inspected comes back inspected instead of leaving the console badging a panel that no longer shows one. `xdotool` joins `surf` + `xwayland` as an image dependency (surf is an X11 client under XWayland, so the keystroke goes through X on both backends). | Accepted | A wall you can only debug by photographing it is a wall you can't debug (POL-49: a dashboard that boots everywhere except on the panel). The ticket assumed both tiers were reachable; the browser matrix says only the cheap one is, and finding that out cost an afternoon of `docker run ubuntu:26.04` rather than a week of building a tunnel to a port that speaks no protocol we can proxy. Collapsing to one browser is Alex's call and the honest end of the same finding: a seam with one installable implementation is not vendor-neutrality, it is three ways to describe the same box, two of which are lies. Non-negotiable #5 is about the **display backend, identity and content** staying swappable; the browser is substrate (#6, "buy the substrate"). The on-screen inspector also matches the precedent set days earlier by the Debug Console GRUB entry (POL-35): on a sealed kiosk with no SSH and no passwords, diagnosis is a deliberate, physical, unmistakable act. NB: the *remote* tier is not refuted in general, only for WebKitGTK — should the wall browser ever change, `server/inspect` is the seam a tunnel would slot behind. |
| D64 | 2026-07-10 | **POL-53: the boot splash renders at the panel's native resolution, because the initramfs now carries the real KMS drivers (`add_dracutmodules+=" drm "`).** On a 1440p wall the splash was a soft, blocky ~1280×800 image stretched to fit. Cause: Ubuntu's `plymouthd.defaults` sets `UseSimpledrm=1`; dracut's `plymouth` module reads that key and `depends()` on `simpledrm` **instead of** `drm`, and the `drm` module's own `check()` returns 255 so nothing else ever pulls it in. `simpledrm` is built into the kernel (`CONFIG_DRM_SIMPLEDRM=y`) and is a fixed-mode shim over whatever framebuffer the firmware's GOP left behind — it cannot mode-set. So plymouth composed the whole splash at the firmware's resolution, and i915/amdgpu/virtio-gpu only loaded after switch-root, by which point plymouth was quitting. Naming `drm` in the splash drop-in (`packages/agent/src/setup/plymouth.ts`) puts the real drivers in the initramfs; udev autoloads one by PCI modalias, plymouth drops the simpledrm renderer for a real DRM one and mode-sets the connector's **preferred** mode. Costs ~47 MiB of initramfs (73 → 120 MiB on 26.04/amd64), which the ~2 GB netboot RAM floor absorbs. **Two adjacent defects fell out of the same investigation and are fixed with it.** **(a) `plymouthd.conf` now names `ThemeDir=` as well as `Theme=`.** `plymouth-populate-initrd`'s `set_theme_dir()` only believes `plymouthd.conf` when it names both, and with `Theme=` alone it falls back to `update-alternatives --query default.plymouth`. That resolves today only because `install.ts` step 2b registers that alternative — with `allowFail`. Where it does not take, the theme resolves to the literal name `none` and the script exits 1 with "The default plymouth plugin () doesn't exist", an error dracut discards (`2> /dev/null`), *before* the block that installs plymouth's systemd units — so `plymouth-start.service` never reaches the initramfs, plymouthd cannot start there, and POL-35's `plymouth display-message` narration hooks address a daemon that does not exist. Verified in a 26.04 container: with no alternative registered, `Theme=` alone exits 1 and installs zero units; `Theme=` + `ThemeDir=` exits 0 and installs all ten. The splash's presence in the initramfs no longer depends on a best-effort `update-alternatives` call. **(b) The theme now re-lays-out when the window changes size**, which the `drm` fix made mandatory: `plymouth-start.service` runs from `sysinit.target` while udev is still probing, so plymouthd can paint before the KMS driver takes over from simpledrm — and the mode then changes underneath it. Top-level script code runs ONCE, so the sprites kept their old sizes and positions and the splash ended up correctly proportioned but small and off-centre in the corner of the panel (seen directly in a QEMU capture: without this, the `drm` fix alone looks worse than the bug it fixes). The layout moved into a `layout()` function that the refresh callback re-runs whenever `Window.GetWidth()/GetHeight()` change, and the names it shares with the draw functions are declared at the top level — Plymouth's DSL makes `x = 1` inside a `fun` a function-local unless the global already exists, and that silently blanked the status line. An empty status update (systemd sends one when a job settles) no longer wipes the line, which also keeps an image-less sprite off the screen (the POL-7 segfault). **(c) The splash shows ONE status line, not two.** systemd's unit-name status and `plymouth display-message` narration each had their own sprite, so a netbooting wall read `dracut-initqueue.service` on one line and "Downloading the OS image ..." on the next. They now share a line, our narration wins while it is up (it is written for a human; systemd's is a unit name), and a new `pre-pivot` hook (`deploy/live/.../polyptic-progress-done.sh`) calls `plymouth hide-message` before switch-root so the retained final frame cannot claim the box is still downloading. **Deliberately NOT fixed here:** the status line is still `Image.Text` at Pango's default `Sans 12` scaled up `sh / 620` (~2.3× at 1440p), so it stays the softest thing on the splash. Passing a point size is the fix; a first attempt stopped the line rendering at all, and this is a boot path where a mistake is a black wall. | Accepted | The fix belongs in the drop-in, not in `deploy/build-live-image.sh`'s `--add` list: the same file is what an installed dracut box gets from `polyptic-agent setup`, so both paths are fixed in one place. Forcing a resolution from GRUB was rejected — `gfxmode=auto` deliberately keeps the firmware's current GOP mode (see `grub_video_gop_setup`), so it would mean hard-coding a panel size, which no generic image can know. A driver whose firmware sits outside the curated set simply fails to probe and the splash falls back to simpledrm exactly as before; `FULL_FIRMWARE=1` is the escape hatch. |
| D65 | 2026-07-10 | **POL-47: GRUB paints the Polyptic splash. The "streaming the amd64 live image into RAM ..." line is gone, and the boot is one continuous branded screen from power-on.** The ticket asked whether the message could move *into* the splashscreen. It cannot, and the reason is the design: Plymouth is a userspace daemon inside the initramfs — the very thing GRUB is fetching when GRUB prints — so nothing exists to move the text into. The fix runs the other way. GRUB paints its own screen, made to look like the splash, and hands its video mode to the kernel so Plymouth takes over from the same dark at the same resolution. **It costs nothing, because of what Canonical already ships.** The SIGNED network GRUB we pin (D47, `grubnet{x64,aa64}.efi.signed`, never rebuilt) carries a squashfs **memdisk** holding `fonts/unicode.pf2` — a full 2.4 MB Unicode PF2 — plus the `gfxterm`, `gfxmenu`, `gfxterm_background` and `png` modules, on **both** arches (read out of the pinned binaries, not out of documentation). So `loadfont (memdisk)/fonts/unicode.pf2` needs no network round trip and no new file on the boot medium; the control plane serves only `GET /boot/theme.txt` + `GET /boot/logo.png` (ungated + secret-free like `/boot/grub.cfg`; GRUB resolves a theme's `logo.png` against the theme's own directory, so no base URL is baked into it). **Secure Boot is untouched:** a `grub.cfg` is not signature-verified and no loader changes. **Everything graphical hangs off `if loadfont …; then`** — a GRUB without the font, the modules, or a video mode keeps its text console and boots identically; a theme that fails to parse, or a 404 logo, degrades to GRUB's plain menu. Nothing added here can stop a box booting. **`gfxmode=auto` + `gfxpayload=keep`, and no resolution is ever pinned — the same call D64 made from the other end.** Per `grub_video_gop_setup`, `auto` (width=0/height=0) makes GOP **keep the mode the firmware is already in**, consulting EDID only when it must change modes and falling back to a hard-coded 800x600 if that fails too; `keep` hands that mode to the kernel so the hand-off does not make the panel re-sync. What the splash then renders AT is D64's business, not GRUB's: plymouth mode-sets the connector's preferred mode itself once the real KMS driver loads, and re-lays-out when it does. So GRUB's mode only has to be *something sane*, which is exactly why `auto` is right and why hard-coding a panel size — rejected in D64 — would be wrong here too. (Under OVMF the whole chain lands on 800x600: its GOP starts there and exposes no EDID. A VM artifact, not a panel's.) **Copy, everywhere the wall can read it — this SUPERSEDES the entry-naming half of D61, which kept `Polyptic (Live)` / `Polyptic (Offload Bootloader)` / `Polyptic (Debug Console)` on the grounds that renaming was churn.** D61 was weighing churn against a reverted submenu experiment; it never weighed the fact that this menu is a public sign. The flat-menu and `export arch`/`export net` halves of D61 stand untouched, and the `--id live|offload|debug` handles — what `polyptic.offload=1`, the docs and the tests actually address — never move. The entries become `Polyptic` / `Set up this screen to start without the USB stick` / `Debug console`; the echoes become `Starting Polyptic ...` / `Setting up this screen ...`; the DEBUG entry keeps its `root shell on tty9 (Ctrl+Alt+F9)` line, because it is the one line nobody sees unless they deliberately chose it, and by then tty9 is the fact they walked to the box for; the boot medium's stage 1 drops "bringing the network up (DHCP on all NICs)" and "chaining …" for one `Starting Polyptic ...`, and the initramfs splash says "Waiting for the network" rather than naming DHCP. **The technical detail is not deleted, it is moved to where it is wanted:** the failure path. A box that cannot chain now says `Could not reach the Polyptic control plane at <host>` above a `Try again / Restart this screen / Firmware setup` menu, and the dracut hooks keep host + protocol on `/dev/console`. Two traps found in the VM while wiring that up, both now commented in place: GRUB paints the fallback menu over the console **the instant the script ends**, so the message needs a `sleep -i 8` or it may as well not have been printed; and it cannot be made conditional, because `configfile` reports no testable status — an `if ! configfile …` never takes its branch. **Three copies of the same intent in three languages** — a TS theme generator, the boot medium's `dongle-grub.cfg.tmpl`, and a shell heredoc inside `offload.sh` that a box has no repo to read from — plus a committed `packages/server/assets/boot-logo.png` (GRUB has no SVG renderer and there is nowhere to rasterise on a box with no OS; `bun deploy/render-boot-logo.ts` regenerates it from the SAME `logoSvg()` the Plymouth theme uses). `packages/e2e/boot-splash.test.ts` is the join: it diffs the two shell copies against each other byte-for-byte, the theme's `desktop-color` against Plymouth's `SPLASH_COLORS.bg`, and the PNG's IHDR against the size the theme asks GRUB to draw. **Verified**, not asserted: the real pinned shim + grubnet, under OVMF/QEMU, chaining a real generated `grub.cfg` → `theme.txt` → `logo.png` over HTTP, screenshotted before and after; the 5 s timeout still auto-boots with a theme active. 366/366 tests green. | Accepted | POL-47, verbatim: "This shows on every screen when it boots up. It's a bit too techy as it stands right now." A wall screen is public signage — everything in the room reads its first frame, and that frame was a kernel-fetch narration written for whoever built the netboot chain. The cheap fix was to reword the `echo`; the ticket explicitly allowed it ("If not, make it a more user-facing text"). But the message is only *half* the problem: the 5-second GRUB menu above it read `Polyptic (Live Bootloader)` on a bare text console, and there is no wording that makes a text console look like a product. Once the memdisk font turned out to be there for free, the branded screen cost one theme string, one 7.5 KB PNG, and two ungated routes — and it closes the last seam in "zero-click cold boot to content" (non-negotiable #4): power-on is now Polyptic dark all the way to the player. The residual risk is the one POL-35 already carries: the real-hardware pass (amd64, Secure Boot ON), where `gfxmode=auto` meets an actual EDID for the first time. |
| D66 | 2026-07-10 | **POL-62: wall-critical branding must never depend on the browser compositor — the idle/pending splash is fully static.** The wall browser is surf/WebKitGTK (D63), and on a box without working GL, WebKit's DMABUF compositing path silently drops elements promoted to their own layer. The splash's infinite `opacity` animations (`idle-breathe` on the mark, `idle-pulse` on the dot) forced exactly that promotion: the logo vanished on the real wall while the plain-painted text survived (photo on POL-62). Both animations are removed, and the mark's SVG fill moved from a `fill="var(…)"` presentation attribute (Chromium-only) into CSS. Any future motion on wall-rendered chrome must not animate opacity/transform/filter. | Accepted | The brand mark is the whole point of the board (POL-27); a nicety animation may not cost the branding on precisely the hardware the product exists for, and infinite animations also keep software-rendered boxes permanently repainting (POL-59/POL-65 heat). |
| D67 | 2026-07-10 | **POL-63: Wi-Fi at every point in the boot stack — a universal medium with a LOCAL boot payload, credentials as an editable file on the medium, and self-refreshing boot media.** GRUB/UEFI can never join a WPA network (no supplicant exists at that stage, industry-wide), so a Wi-Fi-only box must reach Linux on local files first: `polyptic-boot.img` v2 keeps the signed loaders + a network-FIRST stage 1 (a wired box DHCPs and chains the server menu exactly as D47) and adds a per-arch local payload — the Canonical-signed kernel plus a new fat **`initrd-wifi`** (wpa_supplicant + every major vendor's wlan drivers *and firmware*, a SECOND dracut artifact because the lean wired `initrd` must stay small for GRUB's few-MB/s HTTP fetch) — booted only when the wired chain fails. Credentials are an **editable `polyptic/wifi.conf`** on the medium's FAT (KEY=value, CRLF-tolerant, never eval'd; WPA2/WPA3-PSK and WPA-Enterprise peap/ttls/tls with cert files under `polyptic/certs/`; pure tested sh helpers render escape-safe wpa_supplicant configs, hex SSIDs), used twice per boot: the **initrd** associates to stream `rootfs.squashfs` and stages the file into `/run`, then the **rootfs** `polyptic-wifi.service` runs its own supplicant — the Linux-world handoff, because an initrd association dies with its supplicant at the first WPA rekey, and the agent/browser/update-poll need the radio all day. The local `live` entry PINS `root=live:…/builds/<imageId>/…` so the medium's kernel always meets its /lib/modules; **`update-poll` refreshes the medium itself** (fetch the new build's vmlinuz+initrd-wifi into the inactive A/B slot, verify against SHA256SUMS, rewrite the menu LAST) before rebooting, so media never go stale; a recovery entry boots the newest image on a mismatched kernel (box offline past retention) and the poll detects the missing `/lib/modules/$(uname -r)` and heals it. The **offload** also copies payload + credentials to the ESP when the boot came up over a wireless default route (space-checked for BOTH slots first; new report codes `esp-too-small`, `no-local-payload`; `polyptic.offload_wifi=0/1` overrides). The live ISO can bake wifi.conf (+certs) for zero-infrastructure Wi-Fi boxes. A payload medium carries the enrolment token (the local path can never fetch the server's menu), so the `.img` FILE is a credential exactly like the ISO; `LEAN=1` still builds the old tiny tokenless dongle. | Accepted | "A Wi-Fi connection at every point in the stack", one medium for the whole fleet, and all credential types (incl. Enterprise username/password) were the ticket's asks. Local-boot-stage + streamed-rootfs preserves everything netboot buys — diskless generic boxes, the OS from the control plane, automatic updates — while conceding only the stage where Wi-Fi is physically impossible. A file on FAT beats cmdline keys (no escaping limits, editable on any laptop post-flash, can carry cert FILES) and beats server-side stamping (no FAT surgery in the control plane). Two initrds because hundreds of MB of wlan firmware on the wired GRUB-HTTP fetch would add minutes to every wired power-on. |
| D68 | 2026-07-10 | **POL-69: the cluster's boot-medium Job bakes the CURRENT enrolment token by lifting it from the server's own ungated `/boot/grub.cfg` (in-cluster service), and reads the Wi-Fi payload straight off the depot PVC (`IMAGE_DIR_BASE=/depot/image`).** No token in chart values or a Secret: the token is Console-rotatable (DB-backed), so any static copy goes stale the moment an operator rotates it — the menu the wired boxes chain is already the canonical, always-current source, served ungated by the existing trust model. The Job waits briefly for the server, degrades to a tokenless build if it never answers (open-enrolment fleets need none), and falls back to `LEAN=1` (the old tiny wired-only dongle, loudly) when the depot has no `initrd-wifi` yet — keeping "Download bootloader works from minute one" true on a fresh install, where the v2 build would otherwise hard-fail. The weekly full-rebuild Job's medium re-bake gets the same treatment (grub.cfg lift, the chart Secret as fallback, depot-local payload) — it would otherwise have hard-failed after a successful image build. Re-keying the fleet on Kubernetes = rotate in Console + `helm upgrade` (new Job revision re-bakes) + re-flash Wi-Fi sticks; wired sticks still carry nothing. | Accepted | POL-63 made the downloadable `.img` need the token for its local Wi-Fi path, but the Job only passed `POLYPTIC_BASE`, so a gated fleet's console-downloaded stick silently couldn't enrol Wi-Fi boxes. Fetching over the bake address was rejected (the external bootHost may not resolve in-cluster; the PVC is already mounted), and a values/Secret token was rejected as a second source of truth for a rotatable secret. |
| D69 | 2026-07-11 | **POL-74: the offline (Wi-Fi) boot menu carries the POL-47 splash on the medium, because the wired path's theme is server-fetched and the offline path has no server.** `build-boot-medium.sh` fetches `theme.txt` + `logo.png` from `GET /boot/theme.txt` at build time and bakes them at `/polyptic/boot/theme/` on the medium; `render-local-grub.sh` emits the guarded gfx preamble (mirroring `bootGfxPreamble`) plus `set theme=($root)/polyptic/boot/theme/theme.txt` — `$root`-relative so it resolves on a USB stick or an offloaded ESP alike, and file-guarded so a `LEAN`/theme-less medium degrades to a plain menu on the correct dark background rather than erroring. `offload.sh` copies the theme onto the ESP with the rest of the payload. The theme is legitimately copyable because it bakes no URL (it references `logo.png` relatively, per the existing drift test), so a local copy renders identically to the served one; a `boot-splash.test.ts` case runs the shell renderer and pins the theme line + gfx block so it can't drift. | Accepted | Caught on real hardware: a wired box whose stick pointed at an unreachable server fell through to the local menu and showed a plain white GRUB menu instead of the branded splash. Not a regression (the server/wired path was intact) — the offline path POL-63 added never inherited the theme, because the theme was designed to be served, not carried. Baking it on the medium is the only way the offline path can paint it. |
| D70 | 2026-07-11 | **POL-75: one cluster builds every arch for a mixed fleet, via emulation confined to the build Job.** `imageUpdates.arches` is a list (legacy `arch` still works); the chart renders a refresh + full Job spec PER ARCH into the rebuild ConfigMap (`{kind}-{arch}.json`), and `k8s-run-job.ts` with no arch arg fans out over every one SEQUENTIALLY (they share one RWO depot PVC). `imageUpdates.emulation.enabled` (a) ships an optional privileged binfmt/qemu DaemonSet (`tonistiigi/binfmt`, F-flag, node-kernel) and (b) DROPS the `kubernetes.io/arch` nodeSelector so a foreign-arch chroot runs on the native PVC node under emulation — the whole point being that a single-arch cluster (an arm64 K3s VM on Apple Silicon) can build the amd64 image its amd64 boxes need. The hook-killer + per-arch wait are env-driven (`POLYPTIC_HOOK_TIMEOUT_MS` = per-arch × arch-count, `POLYPTIC_JOB_WAIT_TIMEOUT_MS`) so the emulated multi-arch run isn't killed mid-build; `baseIsoUrl` is now arch-parameterised so each arch's live ISO uses its own base. Validated by a real emulated amd64 build on an arm64 host (`file` confirmed an x86 kernel + a 707 MB squashfs) — only the chroot's apt/dracut emulate; mksquashfs runs native. | Accepted | The build must run on amd64 silicon to be native, which an Apple-Silicon lab hasn't got — so cross-arch is either "build at work" or "emulate locally". Confining the emulation to the one build Job keeps the cluster, serving, console, and native-arch build at full speed, and lets a home lab serve a mixed fleet from a single node. Emulated builds are slow (stated in values), but that's physics, not a design cost. |
| D71 | 2026-07-11 | **POL-76: a carrier-less NIC must not hold up the INITRAMFS `network-online.target` — fix it at the per-link policy, not the wait invocation.** A diskless dual-NIC box (Realtek `r8169` + Intel I225 `igc`, one cable) stalled ~179 s in the initramfs before fetching the root image (depot: initrd fetched → `rootfs.squashfs` requested 179 s later) — a black "Starting Polyptic…" on every cold boot. The initramfs network is `systemd-networkd` (dracut's `network-legacy` needs `dhclient`, dropped on 26.04; networkd binaries ship in `systemd`, so dracut's `40network` selector picks networkd), and its `ip=dhcp` catch-all `.network` leaves **every** physical NIC at the default `RequiredForOnline=routable`. `systemd-networkd-wait-online --any` (D55/POL-35) still blocks on a *required* link stuck below carrier (systemd#5154), so the unplugged NIC held the target. POL-35's `--any` drop-in changed only the wait *invocation*; the fix is the per-link *policy* — a new `polyptic-netboot.network` (installed by the `polyptic-live` dracut module as `/etc/systemd/network/10-polyptic-netboot.network`, sorting ahead of dracut's generated `70-*`/`zzzz-*.network`) that matches `Type=ether` wired links, `DHCP=yes`, `RequiredForOnline=no`. This is the exact initramfs twin of the ROOTFS's netplan `optional: true` (`01-polyptic-dhcp.yaml`), which had solved the identical stall since POL-38 but was never mirrored into the initrd. An unplugged NIC now never blocks; the first carrier-up NIC drives livenet's own initqueue `online` hook (the udev event, not the target) in seconds. Scope is `Type=ether` deliberately: WIRELESS links keep the default (required), so a Wi-Fi-only box still waits for wpa_supplicant to associate before declaring online. `polyptic-wait-online.conf` keeps `--any` and tightens the backstop bound (`--timeout=30`, `TimeoutStartSec=45`) for the case where a link legitimately IS required (Wi-Fi). | Accepted | The rootfs already proved the pattern (`optional: true` → `RequiredForOnline=no` + `--any`); the initramfs simply never inherited it, so `--any` alone was defeated exactly as the wider systemd community reports. Fixing the link policy (not just the wait) makes an unplugged NIC structurally unable to block, and keeping the fix `Type=ether`-scoped preserves the Wi-Fi path's need to wait for association. Verified offline (config invariants test + shell suites + rootfs parity); a live multi-NIC boot is the pending real-hardware check. |
| D72 | 2026-07-11 | **POL-77: when the initramfs rejects a Wi-Fi config, it writes a full diagnostics report BACK to the boot medium — a wall screen has no keyboard and its splash shows only a generic message.** On a real amd64 box the Wi-Fi boot showed "Wi-Fi config rejected" although the `wifi.conf` was valid and the baked `wifi-conf.sh`/`wifi-supplicant-conf.sh` + their tools (`wc`/`od`/`tr`/`grep`) were byte-identical to source and validated cleanly under dash + GNU coreutils in-cluster — i.e. the failure lives in the box's live initramfs runtime (PATH, tool behaviour, or the emulated cross-arch build), which nothing off-box reproduces. Because the node has no keyboard and can't reach the console, `polyptic-wifi-up.sh`'s reject branch now remounts the medium read-write and writes `polyptic/wifi-debug.txt` via a new `wifi-diagnostics.sh`: the staged reject reason, a `sh -x wifi-conf.sh` trace that pinpoints the failing step, the runtime `PATH` + per-tool resolution, and the full net state (`ip link/addr/route`, `iw dev`, `rfkill`, wireless `/sys/class/net/*`, loaded wlan modules, `dmesg` firmware tail). The Wi-Fi initrd gains the collector plus `cat`/`cut`/`sync` (required) and `ip`/`iw`/`rfkill`/`dmesg`/`lsmod` (`inst_multiple -o`, optional so a lean image still builds); `rfkill` joins the image's `WIFI_PACKAGES` (`iw` was already present). Secrets are redacted from the rendered supplicant conf in the report; it is best-effort and never turns a Wi-Fi hiccup into a boot failure (every step guarded, wired boxes never reach it). | Accepted | The report can't be reproduced off the box, so the box must self-report — and a keyboard-less signage node can only hand back a file on the one writable surface it has, the boot medium the operator physically holds. Making the reject path also dump the ENTIRE interface state (not just the reason) answers the operator's "is the radio even found?" in the same pull of the stick. Guarded, redacted, best-effort: diagnostics must never themselves brick a boot. Verified by the wifi shell suite (13 new assertions) + `sh -n`; the live capture is the next real-hardware step. |
| D73 | 2026-07-11 | **POL-78: the initramfs shipped no `dirname`, so Wi-Fi association NEVER started on real hardware — resolve a script's own directory in pure shell, never via an external tool.** POL-77's `wifi-debug.txt`, on its first real capture, named it exactly: `wifi-supplicant-conf.sh: line 23: dirname: command not found` → `sh: /wifi-conf.sh: No such file or directory`. The script located its sibling `wifi-conf.sh` via `LIB="$(CDPATH= cd "$(dirname "$0")" && pwd)"`, but `dirname` was never in the `51polyptic-wifi` module's `inst_multiple` (it had `basename`, not `dirname`), so in the initramfs `$(dirname …)` was empty → `LIB=""` → `sh "/wifi-conf.sh"` (exit 127) → supplicant-conf exited 1 → **every** Wi-Fi config "rejected". Invisible off-box because every dev machine + the cluster pod HAS `dirname`; it is the canonical "works everywhere except the minimal initramfs" bug. Fix: resolve own-dir with pure parameter expansion `_lib="${0%/*}"; [ "$_lib" = "$0" ] && _lib="."` (POLYPTIC_LIB_DIR still overrides) in both `wifi-supplicant-conf.sh` and `wifi-diagnostics.sh` — zero external-tool dependency for path resolution — and, belt-and-suspenders, add `dirname head tail` to the module's `inst_multiple` (`head` was silently missing from the hook's success path, `tail` from the diagnostics). The config, the radio (`wlp45s0` iwlwifi, rfkill clear), and everything else in the report were perfect — purely a missing coreutil. | Accepted | The diagnostics (POL-77) paid for themselves on the first boot, turning an unreproducible "rejected" into a one-line root cause. A minimal initramfs is a hostile environment for `$(external-tool …)` in path resolution — parameter expansion `${0%/*}` is always available and can't go missing, so path-finding should never shell out. Regression-guarded by a test that puts a `dirname` stub (exit 127) on PATH and asserts the supplicant still renders. This finally closes the real-radio Wi-Fi association gap that VMs could never cover. |
| D74 | 2026-07-13 | **POL-79: the depot must ALWAYS mirror the ACTIVE build into `builds/<imageId>/`, healed on FETCH — not only on the server's own reconcile.** A netbooted box 404'd forever "cannot fetch the OS image": its LOCAL boot medium pins `root=live:…/dist/image/<arch>/builds/<active-id>/rootfs.squashfs` (POL-63/D67, so the streamed kernel always meets its modules), but that build-specific mirror existed ONLY where the server's reconcile put it — `ImageUpdates.start()`→`retain()` at boot, or `execute()`→`retain()` after a rebuild the SERVER triggered AND that returned success (`image-updates.ts`). The build scripts write ONLY the arch root: `build-live-image.sh:306-324` and `refresh-live-image.sh:103-107` populate `$OUT_DIR=/depot/image/<arch>` + `image-id.txt`, never `builds/`. So a build that reached the arch root by any other path — an externally-run rebuild Job (`bun deploy/k8s-run-job.ts full amd64` invoked directly), or a partially-failed multi-arch hook run whose non-zero exit made `execute()` skip retain — left no mirror, and since the running server never re-reconciled (start's retain runs once at boot; the pod wasn't restarted) the pinned path stayed a 404. Exactly the homelab state: `builds/` held the two OLDER builds but not the ACTIVE `20260711T191928Z-981386cb`, though the arch root + `image-id.txt` WERE it; the interim fix was a manual hardlink, which no rebuild survives. Fix, server-side (the depot owner), three layers: (1) the serve route `GET /dist/image/:arch/builds/:imageId/:file` now, on a miss, calls `ImageUpdates.ensureActiveBuild(arch)` (= `adopt`, hardlink the arch root into `builds/<active-id>/`, EXDEV→copy) and re-stats before 404-ing — so the box's own pinned fetch heals the mirror with no restart; a non-active/pruned id heals nothing and stays a clean 404; (2) `execute()` runs `retain()` unconditionally (not only on `status==="success"`), so a multi-arch hook run where one arch failed still mirrors the arches that DID publish; (3) `start()`'s boot reconcile is unchanged (already heals a mirror-less depot on restart). `adopt` is idempotent (an already-mirrored active build is one stat + return) and `prune` still never drops the active build, so the arch-root ↔ `builds/<active-id>/` relationship stays consistent. Preserves the arch-root serving, the streaming-by-fetcher contract (POL-73: rootfs/initrd-wifi stream, vmlinuz/initrd buffer for Content-Length), SHA256SUMS/manifest handling. Tested: `image-updates.builds.test.ts` reproduces the mirror-less-active gap and proves `ensureActiveBuild`/`retain` heal it (hardlinked, idempotent, active never pruned even at retain=1); a real-server e2e (`netboot-builds-heal.e2e.test.ts`) publishes the active build at the arch root AFTER boot (so startup-reconcile can't pre-heal) and asserts the pinned path serves 200 with a bogus id still 404. Full `bun test` 395/395. NOT verified: a live netboot on hardware (no cluster/hardware access). | Accepted | The mirror is what the boot chain actually fetches, so it must exist for EVERY build that is active — independent of how the build got there. Healing on the fetch (plus reconciling after every rebuild and on startup) makes a missing mirror structurally unable to strand a box, and removes the "did the server trigger it / did the pod restart" preconditions the old reconcile-only path silently depended on. Hardlink-at-heal is instant and dedupes storage on the shared PVC (copy fallback across mounts); idempotent + active-protected keeps it safe to run on every fetch. |
| D75 | 2026-07-13 | **POL-80: the OFFLINE/Wi-Fi GRUB splash is baked DETERMINISTICALLY (not by a build-time curl) and self-heals onto existing media via `update-poll`.** POL-74/D69 bakes the POL-47 theme onto the boot medium so the offline path (which has no server — GRUB/UEFI can't join WPA) can still paint the branded splash. But that bake was a best-effort NETWORK curl at build time (`build-boot-medium.sh`: `curl -fsS "$POLYPTIC_BASE/boot/theme.txt"` + `/boot/logo.png`), and on failure it **silently shipped a plain, theme-less medium** — exactly what happened on the homelab, where early media were flashed before the cluster could resolve `polyptic-boot.homelab`, so the curl failed and the offline splash was plain. Two fixes. **(1) Deterministic bake.** `build-boot-medium.sh` now GENERATES `theme.txt` with `bun` from the SAME `buildBootThemeTxt()` the server serves at `/boot/theme.txt` (new `deploy/render-boot-theme.ts`, mirroring `render-boot-logo.ts`) and copies the committed `packages/server/assets/boot-logo.png` — the very file the server also serves at `/boot/logo.png`. The baked `theme.txt` is **byte-identical** to the served one because the theme carries no base URL (it references `logo.png` relatively — the existing drift test's invariant), so there is exactly ONE theme body for the served, offline, and offloaded cases; a `boot-splash.test.ts` case runs the generator and pins its stdout `=== buildBootThemeTxt()`. `boot-theme.ts` imports nothing, so the generator needs no `@polyptic/protocol` build and NO network, and runs identically in-cluster (rebuild Job = server image + `/repo` + `bun`) and on macOS (local dongle). Guard discipline is kept: a genuinely missing asset (no `bun`, missing logo, render fails) degrades to a plain-but-booting menu with a LOUD stderr line and a hard build failure is never introduced (`render-local-grub`'s `[ -f theme.txt ]` guard then falls back to a plain menu on the correct dark). `offload.sh` copies the theme dir off the medium, so it inherits the fix unchanged (verified — no offload change needed). **(2) Self-heal on the box.** A stick flashed from an OLD, theme-less medium would otherwise stay plain forever without a re-flash. `update-poll.sh` runs on the box AFTER boot, which DOES have network to the depot (wired or Wi-Fi, unlike GRUB), so it now refreshes `/polyptic/boot/theme/{theme.txt,logo.png}` on the medium from `$POLYPTIC_BASE/boot/*` on EVERY poll — deliberately BEFORE the up-to-date early-exit, because a box already on the current image never reaches the A/B slot refresh yet its stick still needs the splash. It is idempotent (content compare; a current medium is never rewritten — no FAT churn, no log), atomic-ish (stage on tmpfs, `mv` into place, both-or-nothing), best-effort and fully guarded (mount rw, heal, unmount; nothing blocks or bricks the boot), scoped to media that carry the local offline menu (`grub/local-$arch.cfg`), and kept independent of the A/B kernel/initrd slot logic (the theme is SHARED, not slotted). Tested: `wifi.test.sh` gains heal / idempotent-second-run / drift-re-heal / server-serves-no-theme cases (119 sh assertions), `boot-splash.test.ts` gains the generator-equals-served + no-network cases; full `bun test` 397/397. NOT verified: a live GRUB render on hardware (no cluster/hardware access). | Accepted | The offline splash is public wall signage that must look like the product on the first frame, and a splash whose presence depends on whether a build host happened to reach the server at build time is not a splash you can rely on. Generating it from the repo/server-image assets makes the bake a pure function of the source — always present, always byte-identical to the wired path — and healing it on the box (where the network GRUB lacks is available) retroactively fixes every stick already in the field without a re-flash. Both paths keep the "nothing added here can stop a box booting" discipline the whole POL-47 splash was built under. |
| D76 | 2026-07-13 | **POL-82: the offline splash theme is a COMMITTED asset the medium bake COPIES — POL-80's `bun`-at-build-time generation failed in the cluster's bun-less Jobs.** POL-80/D75 made `build-boot-medium.sh` GENERATE `theme.txt` with `bun deploy/render-boot-theme.ts` at bake time. That is deterministic on a dev laptop, but the cluster's medium-baking Jobs (the boot-medium helm hook and the full-rebuild Job) are `ubuntu:24.04` containers that get the repo files under `/repo` but **no `bun` on PATH** — so the guard fired (`'bun' not found … offline menu will be plain`) and the cluster shipped a PLAIN medium again, the very regression POL-80 set out to kill (caught live in the boot-medium hook log). Fix: the theme becomes a committed file, `packages/server/assets/boot-theme.txt`, exactly like `boot-logo.png` — `deploy/render-boot-theme.ts` now WRITES it (mirroring `render-boot-logo.ts`) and `build-boot-medium.sh` just `cp`s both committed assets onto the medium: no `bun`, no network, no `/repo`, no runtime, so it bakes identically in the cluster and on macOS. `packages/e2e/boot-splash.test.ts` pins the committed `boot-theme.txt` byte-identical to `buildBootThemeTxt()` (the drift guard — a theme change fails the test until the asset is regenerated) and asserts the bake is a plain copy (no `bun` invocation). POL-80's `update-poll` self-heal is unchanged and still curls the theme from the server at runtime, so it already covered the outcome while the bake was broken. | Accepted | The offline splash must be a pure function of the source with no build-environment dependency — POL-80 correctly removed the *network* dependency but replaced it with a `bun`-runtime dependency that the real build environment doesn't satisfy, which is the same class of "passes offline tests, fails in the actual Job" bug. A checked-in asset (the pattern `boot-logo.png` already uses, since GRUB needs a pre-rendered file anyway) has NO runtime dependency at all; the drift test keeps it honest. |
| D77 | 2026-07-10 | **POL-67: the kiosk browser is Google Chrome stable, running NATIVE Wayland — surf stays as the fallback. Reverses D63's surf-only clause (and D48's Xwayland plumbing becomes fallback-only), on real-hardware evidence.** **(a) The evidence:** the first real amd64 wall (fpd-ago, amdgpu) ran hot doing "nothing" — `WebKitWebProcess` pegged at 300%+ CPU. Diagnosed over the POL-59 remote shell: sway holds `/dev/dri/card1` fds (the GPU works), WebKit holds none; surf's own stderr says why — `libEGL warning: DRI3 error: Could not get DRI3 device`. surf is X11-only → Xwayland → DRI3, and with DRI3 broken every surf software-renders (not permissions: kiosk is in `video`+`render`). Chrome with `--ozone-platform=wayland` goes EGL/GBM straight to the GPU like sway, sidestepping Xwayland and DRI3 entirely. **(b) The package:** `google-chrome-stable` from GOOGLE'S OWN apt repo (`dl.google.com/linux/chrome/deb`), key at `/etc/apt/keyrings/google-chrome.asc` (apt ≥2.4 takes armored keys — no gnupg dep), list written at Chrome's own postinst path so it never duplicates. Canonical ships Chromium ONLY as a snap (unacceptable in a diskless RAM image: ~700 MB + snapd seeding); the xtradeb/ungoogled `.deb`s stay rejected on supply-chain trust (D63). Because the repo persists into the squashfs, the nightly refresh's plain `apt-get upgrade` tracks the latest stable Chrome with zero new plumbing. apt+amd64 only — Google's apt repo publishes no arm64 index as of 2026-07-13 (`binary-arm64/Packages` 404s while `binary-amd64` is 200), though arm64 Linux Chrome is announced; `setup` HEADs the arch's index rather than hardcoding `amd64`, so arm64 boxes pick Chrome up automatically on their first full rebuild after Google publishes — and, just as importantly, a non-published arch never gets a `deb [arch=…]` line whose 404 index would fail the very next `apt-get update` and take the whole build down — so **surf (+xwayland/xdotool) still installs everywhere** and the agent picks at runtime (`selectKioskBrowser`: `POLYPTIC_BROWSER` override, else chrome-if-present): one image serves both arches, and the UTM arm64 lab VMs simply keep surf. One Chrome per output via per-connector `--user-data-dir` (shared dir = Chrome dedupes into one process; also required for the debug port since Chrome 136). **(c) Remote DevTools through the console** — the tier D63 measured as impossible on WebKitGTK, now real, REPLACING the POL-50 on-wall inspector on Chrome boxes (the surf path remains for fallback walls). Chrome always launches with a LOOPBACK-ONLY `--remote-debugging-port` (9222+n); the existing per-screen `inspect` arm now means "arm the tunnel" there (no relaunch, nothing on the glass — the ack still drives `ScreenView.inspecting`), and a `DevtoolsRelay` + `/api/v1/screens/:id/devtools[/**]` proxy tunnels Chrome's own bundled DevTools frontend + its CDP WebSocket over the agent WS, on the POL-59 shell-relay posture: session-gated HTTP, cookie-gated WS upgrade, armed-per-screen re-checked per frame, audited, agent-side defense in depth (`devtoolsEndpoint` = null unless chrome+running+armed). Two measured traps carried over from D63: the agent fetches 127.0.0.1 so Chrome's Host-header 500 never fires, and Chrome's frontend CSP (`connect-src 'self' ws://127.0.0.1:*`) is simply not forwarded by the proxy. **(d) The player was a secondary amplifier** (~2× the raw page's CPU on the same box): the corner badge's `backdrop-filter: blur(8px)` re-blurred on every underlying iframe repaint, and the ident/badge animations eased `opacity` per-frame — the badge is now an opaque pill, and both blinks are discrete `visibility` steps() (D66-compliant: no opacity/transform/filter, two repaints a cycle). **(e) The costs, called out:** Chrome adds ~300–400 MB to the squashfs (the POL-35 492 MiB image lands back around ~800 MiB — still under the old 1.4 GiB casper ISO; RAM floor to be re-measured), and the D47 signed chain is untouched (browser is userspace). The POL-59 transport caveat applies unchanged: on a plain-HTTP deployment the tunnel is as trustworthy as the network; HTTPS is the prerequisite for hostile networks. | Accepted | POL-35 parked the browser question and D63 answered it for the browsers then installable; a real wall CPU-pegging at idle re-opened it with data instead of taste. The structural fix is GPU access, which no amount of surf tuning reaches (the break is in Xwayland/DRI3 below it). D63's own NB anticipated this: "should the wall browser ever change, `server/inspect` is the seam a tunnel would slot behind" — and it is, verbatim: same endpoint, same ack, same console button, now meaning DevTools where the browser can. Buying Google's signed, security-updated deb rather than maintaining a Chromium build is non-negotiable #6 verbatim; keeping surf beneath it keeps arm64 and Chrome-hostile hardware lit. |
| D78 | 2026-07-13 | **POL-86: the wall paints only PROVEN-reachable content, and a broken boot EXPLAINS ITSELF — replacing the signal-driven frame watchdog that went blind on its first real boot.** Two rounds, recorded honestly. **Round 1 (PR #72)** built a `FrameWatchdog` on the browser's network hints (`online`, the player WS reconnecting, a resource error on the player's own page) because a cross-origin iframe's failure is INVISIBLE under SOP — Chrome commits its own error page and then **fires `load`**, so "did it load?" answers yes for a frame showing `ERR_NETWORK_CHANGED`, and a plain load-timeout can never see the failure. On the very next real boot (TEST 8, Wi-Fi) the wall came up broken and the watchdog did nothing: the Wi-Fi settled with no offline period (no `online`), the WS connected once and stayed up (no reconnect), and PR #72's own font self-hosting had removed the last own-page subresource that could fail (fix #2 deleted the signal fix #1 depended on). The lesson is structural: **hints are optional, so a healer built only on hints is optional too.** **Round 2 (this entry) makes the reachability question direct**: `fetch(url, {mode:"no-cors", cache:"no-store"})` REJECTS on network failure and resolves when response headers arrive, for ANY origin, no CORS cooperation — a per-URL oracle for exactly what the element is about to fetch. The player's new `SurfaceProber` gates EVERY surface kind (web/dashboard iframes AND image/video media — the watchdog covered only iframes, and the TEST 8 corpse was very plausibly an `<img>`, which never had an error handler at all): PROVE (probe w/ backoff 0.5s→15s cap, forever, before the element gets a `src`; the region shows a calm placeholder — never a sad face or broken-image icon) → VERIFY (re-probe +10s/+30s after paint, catching a network move in the probe→element-fetch window; on failure the painted element is NOT blanked — re-prove then reload IN PLACE) → STEADY (no periodic polling; a fleet must not trickle-poll its dashboards). Browser hints are demoted to `recheck()` triggers: probe passes → reload in place anyway (in-flight loads may have been aborted); probe fails → re-prove first. Media `error` events are wired (`elementError`: clear the corpse, re-prove with an OWN backoff, because a reachable-but-undecodable asset would otherwise spin — and the first live verification caught exactly that spin: the content element's error re-entered via the window resource-error listener every 1.5s, so content elements are excluded from the own-page signal). Rate-limited heals are QUEUED, never dropped (dropping a warranted heal is precisely how a wall stays broken with no human watching). A changed URL keeps the OLD content up until the NEW one proves. **Priority A, diagnosability:** every step writes a `diag()` line to console + a localStorage ring (REPLAYED on the next page-life — the refresh that fixes a wall used to also destroy the evidence) + a new `player/diag` protocol frame the server logs (`event: player.diag`), so a failed boot is read with `kubectl logs` alone: no SSH, no DevTools, no racing the console. Player self-caps at 120 lines/min; URLs are redacted to origin+path (POL-24 stamps tokens into the query). Verified live: a staged TEST 8 (content port dead at player boot, up 6s later) shows placeholder → 5 probes with real error text → self-paint → `image fired load`, all in the server log; the undecodable-asset case backs off 1s→15s with no hammer loop. Residual gap, stated: a cross-origin IFRAME whose load is aborted with NO hint after the last verify passes is undetectable from our side of SOP; media has no such gap. | Accepted | TEST 8 failed on hardware and the operator's instinct was right: "hold off until the network is up before attempting the player" — the probe gate is that instinct made per-URL and deterministic. Round 1 shipped without a DECISIONS entry; this one carries both so the SOP/`load`-event reasoning is not re-derived (or re-trusted) next time. Priority A came first by request: a wall that breaks silently is worse than a wall that breaks. |
| D79 | 2026-07-13 | **POL-87: the offline splash theme is only ever half-present by ACCIDENT — so every reader guards on the pair, every writer commits the pair logo-first, and the poll repairs an orphan in place.** The observed wall-facing failure: every Wi-Fi/offline boot painted `error: null src bitmap in grub_video_bitmap_create_scaled` + **"Press any key to continue"** — an error prompt on a screen that by definition has no keyboard (exactly the class D65 removed from the boot path). It still booted after a timeout, but two compounding bugs produced a PERMANENTLY broken stick: **(1) the guard checked the wrong file** — `render-local-grub.sh` enabled `set theme=` when `theme.txt` existed, never checking `logo.png`, which the theme itself references (`file = "logo.png"`); **(2) the self-heal committed the pointer before the data** — `update-poll.sh heal_boot_theme()` mv'd `theme.txt` (the file the guard keys on, i.e. the COMMIT record) *before* `logo.png`, then its cleanup deleted the staged logo — so a heal torn between the two mvs (FAT, yanked stick, reboot) left theme-without-logo forever. The homelab stick got there via the POL-80/82 window: flashed plain (theme-less), later half-healed. Fixes, all three writers + the reader + the poll: **(a)** the renderer guards on BOTH files — nested `if [ -f ]`s, not `-a`, because plain `[ -f ]` is the only test form every GRUB build is guaranteed to have; a half-themed medium now degrades to the plain menu on the correct dark background, which boots silently; **(b)** logo FIRST, `theme.txt` LAST in `heal_boot_theme` (and `build-boot-medium.sh` + `offload.sh`, which also now copies only a COMPLETE pair so an orphan never propagates to an ESP) — a write torn anywhere leaves old-state or logo-without-theme, both silent; **(c)** orphan repair on every poll, BEFORE and independent of the server fetch: `theme.txt` present with a missing **or zero-byte** `logo.png` (a torn FAT write — GRUB scales a 0-byte png into the same null bitmap) is deleted, so an already-broken stick repairs itself with no re-flash even against a server that serves no theme. Note (c) is what fixes fielded media: their baked `local-*.cfg` still carries the OLD single-file guard until an image update regenerates it, so removing the orphan (or completing the pair) is the only fix that doesn't need a re-flash. Tests: wifi.test.sh 38e–38g (orphan repair incl. zero-byte logo + idempotence; torn-write simulated by an mv stub that kills the FINAL mv — asserts no theme.txt commits, no success log, no temp litter, and the next poll completes the pair); boot-splash e2e pins the two-file nested guard string. | Accepted | The theme is decorative; the failure mode was not. Every degradation path must land on "plain menu, boots silently" — never on an error + keypress prompt a wall cannot answer. Writer discipline (dependency before commit record) is cheaper than any recovery logic, but the orphan repair still exists because media already in the field carry yesterday's ordering. |
| D80 | 2026-07-10 | **POL-68 Machines-view declutter + console lifecycle UI, WITHOUT a rebuild gate.** Approved machine cards show exactly three action clusters — [Ident all] · [stateful console button] · [⋯ Reboot / Revoke access / Remove machine] — with status chips (Online pill + amber "Shell armed") between name and actions; the terminal is a full-screen console view (was a modal). The v2 mock had "Enable console" trigger a full image rebuild ("the debug shell is baked into the image"); we deliberately DIVERGED: enabling the console is the existing per-machine shell arm (POL-59, instant). The feed gains an `accent` severity (operator-initiated lifecycle: "Rebooting X", "Console session opened"); console enable logs red (security-relevant), disable green; an in-flight operator reboot is tracked server-side (Presence, 3-min TTL) so the card reads "rebooting…" and the intentional drop doesn't log "went unreachable". Settings→Onboard Screens: the status chip is the row's only status element (spinner + "Updating"/"Rebuilding" while a build runs, detail in its tooltip); the retention footer moved into the ISO-download tooltips ("…treat the file as a credential"). The mock's §1 per-machine CPU/mem/disk stats strip is deferred to the POL-59 monitoring work. | Accepted | The image rebuild is FLEET-wide while console arming is deliberately PER-machine — baking a shell into the shared image would arm every box to arm one, and add a ~45-minute wait for a capability the agent already has (the unprivileged PTY is always in the image; arming is the gate). The mock's rebuild gating papered over that mismatch, so the UI state machine was kept and the semantics corrected. |
| D81 | 2026-07-10 | **POL-60: a deployed wall ignores physical input devices — `input * events disabled` in the sway config — except in a debug boot.** A walk-up keyboard could Tab focus around the dashboard or reach a VT console; a kiosk is glass, not a terminal. The disable is wholesale (hot-plugged devices are caught the moment sway sees them) and cheap to escape: the GRUB "Debug console" entry (`systemd.debug-shell=1`) re-enables keyboard/pointer/touch at sway startup via quote-/comma-/glob-free `exec` lines (sway hands exec to `sh -c`, so anything fancier risks word-splitting), or an operator runs `swaymsg input \* events enabled` over the POL-59 remote shell. Unaffected: agent IPC, the inspector's XTEST keystrokes (inside Xwayland, not a libinput device), and the remote shell. Caveat: virtual seat devices match `input *` too, so a hand-started wayvnc is view-only until re-enabled. The x11-i3 fallback gets a best-effort equivalent (`xinput disable` on physical slave devices at i3 start, sparing XTEST so the POL-50 inspector keeps typing; X11 has no wholesale switch, so hot-plug there stays live until the next i3 reload) + `xinput` in the x11 package sets. Verified against real sway 1.9 (config validates; the three re-enable payloads are accepted at runtime). (D68/D69 are claimed by in-flight branches, hence the gap.) | Accepted | Kiosk hardening with a deliberate, boot-time escape hatch — operators debug over the remote shell or a debug boot, never by walking up with a keyboard. |
| D82 | 2026-07-10 | **POL-25: mTLS agent identity — enrolment issues a durable per-machine CLIENT CERTIFICATE and a dedicated TLS listener rejects a wrong/absent cert in the handshake, before any app code. The 2b credential stays the per-machine identity; the cert is the fleet transport gate.** Setting `AGENT_MTLS_PORT` starts a second listener serving ONLY /agent, terminating TLS with `requestCert` + `rejectUnauthorized` against a **per-deployment private CA** (ECDSA P-256 via WebCrypto + `@peculiar/x509`, no openssl; persisted once in the store's `mtls_ca` row, so restarts never re-key the fleet; the server leaf is re-minted each boot with SANs from localhost/hostname/`AGENT_MTLS_SANS`/the public-URL envs, because the agent pins the CA *and* verifies hostname). **Issuance rides the 2b seam with zero UX change:** the agent generates its keypair ON THE BOX and adds a PEM CSR to `agent/hello` (`csrPem`); any hello that authenticates (bootstrap token or durable credential — and OPEN mode, which already auto-approves) gets it signed with **CN forced to the machine id** (the CSR's claimed subject is ignored) and receives `server/enrolled.mtls {caPem, certPem, port, url?}` (`credential` became optional on that frame); the agent persists the bundle 0600 at `mtls-<machineId>.json`, closes the plain socket and redials `wss://` presenting the cert (same host it already knows, advertised port — `AGENT_MTLS_PUBLIC_URL` overrides for split deployments). Certs live 10 years; the agent CSRs a renewal inside the last 30 days; 3 consecutive failed mTLS dials trigger ONE plain-channel re-enrol (heals a rotated CA), and a diskless netboot box (tmpfs state dir) simply re-enrols per boot via its baked token — nothing new to configure. `AGENT_MTLS_REQUIRE=1` is the enforced end-state: the plain /agent channel then only authenticates + issues bundles and CLOSES (never admits, never marks presence), so every live session rides the mTLS listener; leave it unset while a fleet picks up certs. **Two measured runtime facts shaped the design.** (1) Bun ≤ 1.2 implements `requestCert` as PRESENCE-only — a cert from ANY CA, even expired, passed (measured on 1.2.2) — so the server **self-tests its own live listener at boot** (a genuine cert must pass; a no-cert and a rogue-CA dial must fail — judged by an application-data round-trip, because under TLS 1.3 the client's handshake 'completes' before the server evaluates the cert) and EXITS rather than serve a fake gate; the same probe capability-gates the test suite (full matrix on ≥ 1.3, refuse-to-serve asserted on 1.2). CI moves to Bun 1.3.14 (whole suite green there) so released agent binaries can actually present client certs. (2) Bun cannot expose the peer certificate on an https upgrade socket (`getPeerCertificate` returns undefined there; only raw `node:tls` sockets have it, and Bun's http parser can't adopt injected sockets), so **per-connection CN↔machineId binding is not enforced yet**: the cert (CN is in it) proves fleet membership at the transport, the credential proves which machine at the app layer — strictly stronger than 2b alone; tighten to CN-binding when Bun exposes peer certs. Helm gains `agentMtls.*` (extra Service port; must reach the pod as RAW TCP — an HTTP Ingress would terminate TLS and strip the client cert). | Accepted | The deploy/transport hardening explicitly deferred at Phase 2b (D12) and DESIGN open-question #7. The credential model was built as the seam this drops into; doing it in-server (not a sidecar/proxy) keeps the product self-hostable with zero extra moving parts, and the boot self-test turns "the runtime verifies certs" from an assumption into an enforced invariant. |
| D83 | 2026-07-10 | **POL-32: cached media — the wall's images and videos survive a network outage; the player restores its last-good slice on boot.** The exposure was concrete: a WS drop never blanks the wall (the player keeps its in-memory surfaces and the keyed DOM diff leaves elements alone) and a painted `<img>` survives offline, but a `<video>` STREAMS over HTTP Range and freezes mid-frame the moment the server is unreachable — on a looping wall video that is a frozen wall — and a media fetch that failed during a blip was never retried. **What shipped (player-side, zero protocol change):** (a) `media-cache.ts` + `media-cache-idb.ts` — every image/video src is downloaded once into an **IndexedDB blob cache** and the element is repointed at a `blob:` object URL, so playback is served entirely from the box and an outage of any length cannot touch it; the cache survives reloads. A service worker + Cache API is the textbook tool but both require a SECURE CONTEXT and the wall loads the player over plain HTTP (netboot boxes speak plain HTTP to the control plane by design, D47/D52) — IndexedDB works on insecure origins, so it is the load-bearing choice, not a preference. Control-plane uploads (`/media/<id>`) are IMMUTABLE by construction (a re-upload mints a new id) → cached forever, revalidated never; external URLs get stale-while-revalidate (conditional GET on ETag/Last-Modified, throttled, a 200 swaps the element — videos carry their playback position across the swap). Failed fetches retry on a 5s→60s backoff while the URL is still wanted, so a wall that boots into an outage heals itself the moment the network returns. Startup pruning: entries unused 30 days go, then LRU eviction over a 4 GiB cap (never evicting what is on the glass); `navigator.storage.persist()` best-effort. If IndexedDB is unavailable the player runs uncached — exactly the old behaviour, never an error on the glass. (b) `last-slice.ts` — every `server/render` is snapshotted to localStorage (validated against the SHARED protocol schemas on load; a stale/corrupt snapshot is discarded and cleared) and restored BEFORE the WS dials, so a normal boot paints last-known content instantly and a boot/reload during an outage shows the wall's last-good content (media from the blob cache) instead of an idle splash. Display-only: a restored revision is never ACKed, and the first live render — including an authoritative EMPTY one — overwrites it, so an operator's deliberate clear round-trips correctly. (c) `GET /media/:id` gains `Access-Control-Allow-Origin: *` — the blob cache uses fetch(), which unlike a tag is CORS-gated when the dev player (:5173) fetches the server (:8080); the route was already public by design (unguessable ids, no session), so the header changes nothing about its posture. **Sites (iframes) are already as safe as a client can make them** — the player never reloads or blanks a frame on disconnect; what a live page does when ITS OWN network calls fail is the page's business, and a cross-origin frame can be neither snapshotted nor cached. Verified live in a real Chromium against the real server + built player: image and video render from `blob:` URLs, the server is SIGKILLed mid-playback and the video keeps advancing, then the page is reloaded against a dead control plane (a static stand-in serving only the player shell — no WS, no /media, no API) and the wall comes back with content from localStorage + IndexedDB. 400/400 tests green (21 new). | Accepted | The ticket's whole ask: "if the network goes down the images/videos and sites should be safe." The wall is the product's face — a frozen video or a broken-image glyph during a routine network blip reads as a broken product to every passer-by, precisely the clunky-wall impression Polyptic exists to erase. Roadmap already carried the idea ("caches last-good slice, rides out control-plane outages"); this lands it where the media actually renders. |
| D84 | 2026-07-10 | **POL-34: playlists — carousels of content, first-class in the library, advanced BY THE PLAYER.** **(a) A playlist is a `ContentSource` (`kind: "playlist"`), not a new entity.** The whole assignment fabric — `SetContentBody.sourceId`, scene snapshots, the wall-span math, the console's pickers — already speaks "library source by id", so making playlists a source kind means they assign to screens and video walls, ride scenes, and survive source-edit re-resolution with zero new plumbing. It has no `url`; its content is `items`, an ordered list of `{sourceId, durationSeconds?}` steps referencing OTHER library sources. Nesting is rejected at authoring (a rotation stack on the player and a cycle detector in the console buy no operator value). **(b) Timing is per step, from the ticket:** anything that never ends by itself (web/dashboard/image) MUST carry a duration; a video may omit it, meaning "advance when it ends" (a timed video loops until its slot closes). **(c) The server resolves, the player rotates.** Assignment resolves the steps into ONE `PlaylistSurface` carrying the full rotation (`items` + a `startedAt` anchor); the player advances it locally — a control-plane outage can stop edits, never the carousel (same posture as the agent's cached last-good slice). A fully-TIMED rotation derives its on-air entry from `(now − startedAt) mod cycle`, so video-wall members stay in phase without coordinating and a rebooting box rejoins mid-cycle; any untimed video makes the rotation sequential (only the playing element knows `ended`). Rotation identity is structural (source ids + durations + anchor), so a POL-24 token re-stamp keeps the position while any authored change restarts it. **(d) Library edits ripple through playlists.** Editing a referenced source re-resolves every screen/wall showing a playlist that contains it; deleting one STRIPS it from every playlist and re-pushes the shortened rotation (a playlist emptied this way keeps its library slot and its targets show the POL-27 idle splash — content comes back the moment a step is added). **(e) Auth stamps per ENTRY:** each step carries its resolving `sourceId`, so `decorateSliceForSend` stamps each web/dashboard entry with ITS OWN source's credential-profile token, and the token-usable re-push edge (POL-24) counts screens reached via playlist steps. Zoom (POL-57) is refused on playlists — a rotation is not one page. Console: playlists get their OWN sidebar view (Playlists) holding the list + the step-builder modal (source picker + per-step seconds, reorder, remove; videos read "until it ends") — the Content view stays a flat catalogue of addresses/files, while playlists still assign from every picker like any source. Verified live end-to-end (image→image→video rotation on a real player, per-step timing observed) + 27 new tests (authoring validation, resolution, wall spans, ripple, per-entry stamping, restart persistence, clock-derived phase). |
| D85 | 2026-07-11 | **POL-42: authored PAGES — a built-in Studio composes framing elements into a `page` content source, and the definition travels INSIDE the slice as ONE surface.** New content kind `page`: a `PageDefinition` (element tree with %-based regions over a solid background, aspect 16:9/9:16) instead of a url, authored in **Console ▸ Content ▸ New page → the Studio** (drag-and-drop canvas, resize handles, 0.5% grid snap, smart alignment guides, layers/z-order, bounded session undo/redo, per-element inspector — exactly the pitch's v1 list, nothing more) and assigned to screens/walls like any other source. **Element library (v1):** embed (a library source by id, or a raw URL — this is what composites a dashboard onto a page), RSS/Atom feed, scrolling ticker, image (uploaded media), text, clock, background shape, weather, QR, countdown. **Architecture:** a page is still ONE `PageSurface` in the `ScreenSlice`, so the 3b span math (video walls) and the keyed DOM-diff path (D5) work unchanged — saving in the Studio re-pushes the slice and the wall updates in <150 ms, no reload. The STORED definition never carries a resolved URL, a credential, or a headline: embeds/images reference sources **by id** and the server resolves + POL-24-credential-stamps them into `PageSurface.data` at SEND time (`decorateSliceForSend`, per-embed profiles — same clean-at-rest pattern as token stamping); editing/deleting an embedded source re-pushes the pages that embed it. **Server-side poller** (`page-data.ts`): RSS 2.0 + Atom via a small tolerant parser (~5 min) and weather via **Open-Meteo** (keyless; free-text location geocoded once, cached ~15 min), polled ONLY for pages assigned to ≥1 screen, last-good on failure, change-driven re-push. **Anti-drift:** one shared `@polyptic/elements` package of Vue components renders elements in BOTH the player and the Studio canvas — the preview IS the renderer; sizing is container-query units (page declares `container-type:size`), so a definition reads identically at any resolution. **D66 carve-out, documented:** the ticker is a plain CSS `transform` loop — fine on the GPU-composited Chrome boxes (D67) that are the primary target; on surf/arm64 software-rendered fallback boxes a ticker is an operator choice. Clock/countdown/feed-ages update TEXT NODES on one shared 1 Hz timer; QR encodes to SVG client-side; no `fill="var()"` SVG attrs anywhere. **No-Gos (the scope fence):** no screen-level layering (`setScreenContent` stays single-surface — compositing happens inside pages only), no zoom on pages (refused; % regions scale by design), no pages-in-pages, no `placement:"window"` embeds, no multi-select/copy-paste/align toolbar, no rotation/templates/theming. **On DESIGN.md's "not a generic signage CMS":** unchanged in spirit — pages exist to FRAME wall content (the 2050-style ticker-under-dashboard), not to compete with PowerPoint; the fence above is what keeps that true. Storage: `content_sources.url` DROP NOT NULL + `definition jsonb` (idempotent). 419/419 tests (25 new unit + 6 new e2e). | Accepted | The wall's whole problem was compositing: a dashboard PLUS framing on one panel, with nothing an operator can touch from the console — the old workaround (hand-built HTML hosted elsewhere) defeated central orchestration, live editing and credential stamping. Putting the definition inside the slice as one surface was the load-bearing call: everything downstream (span, zoom, instant updates, stable ids) already works on surfaces, so pages inherit the machinery instead of forking it. Server-side feed/weather polling is forced by CORS and by sealed kiosks; Open-Meteo keeps vendor-neutrality (keyless, isolated behind the poller). One shared renderer package is what makes "the canvas is the preview" true rather than aspirational. |
| D86 | 2026-07-13 | **POL-88: the cluster Jobs copy `packages/server/assets` into `/repo`, so the medium bake can actually find the committed splash assets.** POL-82/D76 made the offline GRUB theme a committed asset precisely so the bake needs no bun and no network — but the helm init containers assembled `/repo` from `/app/deploy` ONLY, so `build-boot-medium.sh`'s `$REPO_ROOT/packages/server/assets/boot-theme.txt` lookup failed inside every cluster Job and the guard shipped a plain offline menu again (caught in the v0.2.27 full-rebuild log — the THIRD incarnation of this regression, after POL-80's network bake and POL-82's bun bake). Fix: both init containers (`rebuild-jobs.yaml` payload dict + `boot-medium-job.yaml`) copy `/app/packages/server/assets` alongside `deploy/`; a `boot-splash.test.ts` pin asserts any Job that runs the bake carries the assets copy. Wall-facing impact was already contained (update-poll heals the theme onto sticks at runtime, and the POL-87 guard degrades to a plain menu, never the `null src bitmap` error) — but the downloadable medium itself was plain until a box healed it. | Accepted | The bake's asset resolution and the Job's `/repo` assembly are two halves of one contract that lived in different files with no test across the seam — exactly how D76's fix silently regressed. The pin makes the seam explicit; each incarnation of this bug was only ever caught by READING the Job log, which is not a regression net. |
| D87 | 2026-07-13 | **POL-85: dev-tools quick-launch from the Wall page — a ⋯ overflow in the Inspector's single-screen header, driven by the SAME Inspect/DevTools state machine as the Machines view, extracted from ScreenRow into one shared composable (`useScreenInspect`).** The menu lives in the Inspector, NOT on the canvas tile: POL-72 deliberately keeps screen nodes minimal (label chip + live preview; every per-screen affordance lives in the Inspector), and a dropdown inside a Vue Flow node renders at canvas zoom and fights the drag/pan gestures the node exists for. Semantics are the Machines view's verbatim (D77/D63), no new endpoint behaviour: chrome = open the DevTools tab INSIDE the user gesture (popup blockers) while the `POST /screens/:id/inspect` arm races it, settling only on the agent's ack; surf/unknown-browser = the on-panel Web Inspector behind a confirm (it shows on the glass and reloads the page); offline machine = disabled with the explanatory tooltip. `inspecting` stays ack-driven, never optimistic. The extraction moves ScreenRow's subtle settle rules — a NEW refusal notifies (a refusal leaves `inspecting` unchanged, so the flag alone would spin forever); a cleared STALE error must not settle the fresh request that cleared it; unrelated broadcasts settle nothing; plus one new rule, a selection switch resets silently (the Inspector follows the canvas selection; ScreenRow never moves) — into `console/src/components/useInspect.ts` with injected transport, pinned by 10 unit tests those rules never had. Refusals/timeouts surface as a transient inline notice in the panel (the Wall view has no toast rail). | Accepted | An operator debugging a wall was bouncing Wall → Machines → find the box → find the screen → DevTools; the Wall page is where the symptom is visible, so the launcher belongs there. Extracting rather than copying keeps the ack-driven pending semantics in exactly one place — two hand-maintained copies of those edge cases is how they would drift. |
| D88 | 2026-07-13 | **POL-54: the /player WS is no longer an open broadcast — every screen gets a bearer token, minted into its playerUrl and demanded on `player/hello` whenever auth is enabled.** The audit of the live surfaces found ONE real hole and two adjacent nicks. The hole: /player was ungated by design ("players carry no user session"), so anyone who could reach the port could hello for any screenId and receive that screen's slice live, forever — including the POL-24 `?auth_token=` credential-stamped dashboard URLs (live IdP access tokens), every content URL and page definition — plus mark screens ONLINE in the console. The identity a player legitimately has is the URL the AGENT launched it with, and that URL is SERVER-minted (`ScreenAssignment.playerUrl` in `server/apply`, over the authenticated agent channel) — so the fix rides the trust chain that already exists: a per-screen token = HMAC-SHA256(secret, screenId) is stamped into every playerUrl (`?token=`), echoed in `player/hello` (new optional field), and verified constant-time before the socket is registered; a miss is closed (4401), never rendered, never marked online. The secret is per-deployment, generated on first boot and PERSISTED (new single-row store table, like the mTLS CA) — deliberately durable, not short-lived: the wall must repaint on every reconnect/reload forever with the URL it was launched with (D5's live path is a reconnect away at all times), token expiry adds wall-killing failure modes and zero attacker cost, and rotation is still available by replacing the secret row (the next `server/apply` carries fresh URLs; agents diff playerUrl and relaunch, so the fleet self-heals hands-free — the same mechanism that upgrades a pre-POL-54 fleet: old agents pass the new URL through untouched, so compat is free). Enforcement rides `AUTH_ENABLED` — the exact switch that gates REST and the /admin WS — so dev stacks keep the paste-a-URL workflow; minting is unconditional, so URLs handed out with auth off stay valid when auth is flipped on. The nicks, both fixed: a socket is now BOUND to its admitted screen — `player/ack`/`player/diag` claiming another screenId are dropped (an authorized screen-A player could previously write ANY screen's observed revision and speak in any screen's name in the diag log), acks/diag before a hello are dropped, and a re-hello for a different screen closes (4400). Audited and left alone, with reasons: `/media/:id` stays ungated (128-bit random ids = capability URLs; players and walls have no session — same posture as any external content URL, per D33); `/boot/grub.cfg` baking the enrolment token is the netboot design (D47/D68 — machines still land pending); `/boot/report` was already token-gated + leaky-bucket-limited (D60); /admin and devtools WS gates (origin + cookie) verified sound. Deferred: per-screen token revocation UI (rotate-the-secret is the lever), an admin-WS session RE-check mid-connection (a logout doesn't drop an already-open console socket today), and player-channel encryption-in-transit, which is deployment TLS termination (wss://), not an app-layer concern. | Accepted | The player channel is the one live surface whose subscribers are neither humans with cookies nor agents with credentials — but its launch URL is already server-authored and agent-delivered, so a bearer token in that URL gets real access control with ZERO new moving parts on the box, no boot click (D4), no reload on the live path (D5), and graceful degradation everywhere (dev open, old agents compatible, restart-proof, rotation self-healing). |
| D89 | 2026-07-13 | **POL-70 (provisional): HTTPS is the default posture for the operator surface; plain HTTP is a supported degrade with a loud banner, never a refusal.** ONE public origin drives everything: `PUBLIC_BASE_URL` becomes the third leg of the SECURE_COOKIES precedence (explicit `SECURE_COOKIES` → `PUBLIC_BASE_URL` scheme, https→Secure on / declared-http→Secure off → `NODE_ENV=production`), so a TLS deployment gets Secure cookies with zero extra knobs and a declared plain-HTTP homelab never re-hits POL-43's silently-dropped-cookie login failure. The chart mirrors it: new `config.publicBaseUrl` (or, unset, the enabled ingress host — IngressRoute is https by design, the generic Ingress follows `ingress.tls.enabled`, which now **defaults true**) derives `PUBLIC_BASE_URL`, `CORS_ORIGIN`, `PLAYER_BASE_URL` and `MEDIA_PUBLIC_BASE`, so naming a host yields an HTTPS deployment end to end; `config.secureCookies` defaults to `""` (auto). The primary TLS path is a terminating ingress in front of plain :8080 (WS upgrades, cookies and the players' protocol-following `wss:` all already survive that); **native TLS** (`TLS_CERT_FILE`+`TLS_KEY_FILE` → Fastify's `https` option, half-configured pair refuses to boot) is the cheap alternative for bare/docker hosts, with `computeBaseUrl` now falling back to the socket's own protocol. Round 2 (same PR) adds the two certificate stories: **Let's Encrypt** — a condition-gated, VENDORED cert-manager subchart (`letsEncrypt.enabled/email/staging/additionalDnsNames/solverIngressClass`, the ACS-proven Issuer+Certificate pattern; the Certificate writes the exact secret the Ingress references, so one hostname + one email is a complete auto-renewing HTTPS deployment; `.helmignore` dropped `*.tgz` or the loader hides the vendored chart) — and **self-signed** — `TLS_MODE=self-signed` (chart `tls.mode`) mints a CA + leaf with the POL-25 x509 machinery into a new single-row `server_tls` store table and REUSES both across boots (a re-mint would re-warn every trusted browser; the leaf alone re-mints from the same CA when SANs grow or expiry nears, ~2y validity), SANs from PUBLIC_BASE_URL + TLS_SANS (+ Service DNS names in the chart), probes flip to HTTPS, exposure is direct Service (NodePort/LoadBalancer — no ingress re-encryption support, deliberately), and `Console ▸ Settings ▸ HTTPS` (`GET /api/v1/settings/https` + `…/ca.crt`, gated like every settings surface: the CA cert is public material but its only consumer is a session-holding operator) offers the CA download with fingerprint + per-OS trust instructions. self-signed × letsEncrypt and self-signed × cert-files are refused conflicts. Deliberately NOT done: the netboot depot stays plain HTTP by contract (GRUB/shim have no TLS stack, D47 — native TLS therefore warns that netboot needs the depot on an http host, e.g. `ingressRoute.bootHost`); the mTLS agent listener (D82) is untouched; no HSTS/redirect middleware (that is the ingress's job); no ingress→pod re-encryption. | Accepted | "Ensure HTTPS works and make it the default" decomposes into scheme-awareness (the server must KNOW it is https to mark cookies) plus derivation (operators should say the hostname once, not five times); both POL-59 and POL-67 audits carry "HTTPS is the prerequisite for hostile networks", but non-negotiable #4 (zero-click boot on a plain-HTTP homelab) forbids refusing plain HTTP — hence default-on ingress TLS, auto cookies, and a banner instead of a gate. |
| D126 | 2026-07-14 | **POL-101 (provisional): panels get a POWER state — manual wake/sleep and a daily panel-hours window — WITHOUT re-opening blanking during content hours.** The distinction is the whole decision: turning a panel off OUTSIDE its hours is a feature (power, panel life, and the "why is that screen on at 2am" complaint every buyer raises); blanking one DURING its hours is the bug the on-device stack forbids structurally (D41: `output * dpms on`, no `swayidle`, no lock), and nothing here weakens it — a panel goes dark **if and only if** an operator asked, or a window an operator set says the day is over. Idleness, load and connectivity remain meaningless to a wall. **Two rungs, reported honestly (ScreenCloud-matrix style), because they do different things:** `dpms` — always available on a real compositor (`swaymsg output <connector> dpms off`; the x11-i3 fallback pins every DPMS timeout to **zero** before enabling the extension, so the only thing that can ever darken it is an explicit `force`, and X11 has no per-output DPMS, so on a multi-output i3 box sleep applies to every panel it drives) — and `cec` — best-effort, **capability-probed at agent startup** (`cec-ctl` on `/dev/cec*`, else libcec's `cec-client`), because DPMS stops the pixels but not the PANEL: plenty of displays answer a dead signal by staying lit with a black backlight, which burns exactly the power this feature exists to save. A box with no adapter degrades to DPMS-only — loudly in the log, silently on the glass — and the console SAYS which rung it got rather than implying the TV went off. `cec-utils`/`v4l-utils` install in their own `allowFail` transaction (an unresolvable optional package must never break an image build) and a `70-polyptic-cec.rules` udev rule hands `/dev/cec*` to the `video` group the kiosk user is already in — the agent stays unprivileged. **The scheduler is EDGE-triggered, and that is not an implementation detail:** a level-triggered one ("out of hours → sleep") puts the wall an operator just woke by hand straight back to sleep thirty seconds later, which is how a scheduling feature gets switched off for good. So it acts on BOUNDARY CROSSINGS and remembers only the SCHEDULE's last opinion — a manual override therefore holds until the next boundary, then the schedule resumes (the semantics every heating timer has already taught people). A box that reboots at 3am comes back LIT (the compositor asserts `dpms on` by design, so a wall that boots dark is indistinguishable from a broken one) and is re-slept on its `agent/hello`. **A sleeping screen must never read as a dead one:** `asleep` is a distinct, EPHEMERAL state written solely by `agent/power-ack` (the click is a request; only the box knows whether the compositor took the command, and a refusal leaves the screen AWAKE — never claim a wall is dark when it might be lit), it sits alongside `online: true` because the player is still connected and still holding its content, and the console renders it as a calm indigo moon, never a fault. Waking is a DPMS/CEC command and not a re-render: the browser was never torn down, so the content is on the glass the moment the panel lights (D5). **Scope, deliberately:** ONE daily window per screen plus a per-deployment IANA timezone (explicit — the server's TZ is an accident of hosting and the operator's browser an accident of where they are standing). Full recurrence — weekdays, exceptions, holidays — belongs to the scene scheduler; two half-built calendars is the worst of both worlds, so **the two schedulers should converge on one recurrence engine when that work lands**, at which point panel hours becomes a target of it rather than a private calendar. | Provisional | The most-requested ops feature the capability map lacked and the facilities/energy story every buyer asks about — and the one place a careless implementation would violate a non-negotiable, so the fence (schedule/operator only, never idleness) is written into the contract, the compositor config, the x11 command order, and the tests rather than left to memory. Provisional until a real panel confirms the CEC half on hardware: DPMS is exercised by the unit + e2e suites, but whether a given display actually honours a CEC standby is a property of the display, not of this code. |
| D125 | 2026-07-14 | **POL-96 + POL-100 (provisional): the wall canvas grows bulk operations, an explicit CLEAR, and a wall that drags as one unit — with adjacency as an enforced invariant.** (1) **Clear is a real intent.** Content could only ever be REPLACED — "show nothing" had no API path at all — so `DELETE /screens/:id/content` + `DELETE /walls/:id/content` empty the slice, drop the library-source assignment, and let the panel fall back to the D39 idle splash; a Clear button now sits on every content read-out (Inspector single + wall, selection toolbar, multi-select). (2) **Bulk is one call.** `POST /content/assign {screenIds, wallIds, content|null}` runs the existing per-target primitives under one suppressed emit: N targets, one REST round trip, one broadcast, one activity line, `content: null` = the bulk clear; a target that cannot take the change (unknown id, wall member) is SKIPPED and named back rather than failing the lot, and an unresolvable source fails fast before anything is written. `POST /screens/unplace {screenIds}` is the same shape for the tray. Ident stays per-screen (a fire-and-forget WS pulse — no broadcast, no storm to fix). (3) **THE ADJACENCY RULE (the wall-drag call).** A combined surface must be ONE CONTIGUOUS REGION: two members are in contact when they share an edge with positive overlap, or intersect (overlap is odd but never a hole); a gap and a corner-only touch are not. A wall is valid iff its contact graph is CONNECTED. The predicate lives in `@polyptic/protocol/geometry` (pure, shared) so the server enforces it and the console warns with the same maths. Enforced at three seams: **combine** refuses a gappy selection (400 `not-adjacent`) or, with `pack: true`, packs the members into a bezel-tight grid first (`packRects` — cluster into columns/rows, butt them up, keep the order) and refuses if even that leaves a corner join; **the atomic move** `POST /murals/:id/move {screenIds, wallIds, dx, dy}` (the wall drag, the keyboard nudge, a group drag) projects the translation, re-checks every wall it touches, and writes nothing at all if one would break; **the single-screen placement route** 409s a drag that would pull a member out of its wall. So the answer to "a wall whose members stop being adjacent" is REFUSE, never split: a rigid translation cannot break a wall by construction, and every other path is guarded — an invalid wall is unreachable, and the operator is told to split first. A wall move pushes NO render (span offsets are union-relative, so a translation changes nothing on the glass) — placements broadcast, pixels stand. (4) **Canvas ergonomics = the Studio's, ported.** Snap to siblings' edges/centres + the wall's own bounds with dashed accent guides (`snap.ts`, canvas px instead of page %), a coarse grid when nothing is in reach, arrow-key nudge (Shift = a big step) that snaps as it lands, Delete = unplace, Escape = clear, shift-drag = rubber band. | Provisional | Every daily flow at fleet size hit one of these walls: you could not blank a screen, you could not put one dashboard on five screens without five interactions, and moving a video wall meant split → drag each panel at 0.094 scale → recombine → re-assign. Adjacency was the sharp edge: Combine happily unioned screens with a gap, and the span math then stretched content across canvas nobody was showing. Refusing (rather than auto-splitting) keeps the operator's grouping intact and their intent explicit — the console offers the pack, and the pack is bezel-tight. Provisional until the ergonomics are driven on a real wall with a real operator. |
| D123 | 2026-07-14 | **POL-103 (provisional): machines carry FLAT, OPAQUE tags, and a one-keyword selector (`tag=atrium`, comma = AND) is what a bulk operation targets — reboot, arm/disarm the console, ident, approve — each answering a per-machine RESULT list rather than one verdict.** At fifty boxes every fleet action is fifty clicks, so the grouping had to exist; the question was how much syntax to buy. **Tags are flat strings, not key=value.** `floor:2` is a CONVENTION an operator may use; the engine matches a tag by set membership and never parses one. The moment a tag carries a key, the selector has to grow `!=`, `in (…)`, and precedence — and a "small documented syntax" becomes a query language nobody can read at a glance next to a Reboot button. Set membership already says "the atrium", "the ground floor", "the canary ring". **The empty selector matches NOTHING, and a bulk verb that names no target is a 400** — a mis-typed filter must never fan a reboot out across the fleet, and "everything" is only reachable by an explicit machineIds list, i.e. boxes the operator has SEEN. **Partial success is the normal case, so it is the response shape:** every bulk route returns `{matched, applied, results[]}` where an offline box is an `offline` OUTCOME, a pending box a `skipped` one, and a machine whose action throws a `failed` one — three dark boxes do not fail the other nine. The console names the blast radius before acting ("Reboot 12 machines?"), reports what landed ("reboot: 9 of 12 machines · 3 offline"), and leaves ONE summarized activity line per fan-out. Tags are registry state (a re-hello never wipes them) and do NOT bump the revision — like a rename, they change nothing a player or an agent renders. Storage is a `tags` jsonb column, not a join table: a tag set is small, always read whole, and only ever replaced whole (PUT replaces the set; add and remove are the same call). **This is the substrate for canary rollouts (POL-105)** — a staged image roll is "the same fan-out over `tag=canary` first" — and no rollout logic is implemented here. A selector-targeted IMAGE UPDATE was deliberately left out for the same reason: the image flow is a fleet-wide manifest each box polls (D51), so there is nothing per-machine to fan out over until POL-105 gives the manifest a scope. Player untouched; agent untouched. | Provisional | Grouping is what makes a 50-box fleet operable, and the smallest grammar that survives being typed next to a destructive verb is one keyword and an AND. |
| D93 | 2026-07-14 | **POL-89 (provisional): scenes get a REAL scheduler — named dayparts, weekday/date recurrence, integer priority, a default-scene floor — and D24's "scheduling is illustrative" clause is SUPERSEDED.** Every scene row carried an `at HH:MM` box that stored a time and never fired: a decoy control on the product's most-used view. It is gone, and so is `Scene.scheduleAt` (protocol, `scenes.schedule_at`, the console input, the footnote). In its place: a **daypart** is a named window of the day (`end <= start` wraps midnight; `start == end` is all-day) and a **schedule** binds a scene to a daypart on a recurrence (weekdays + an optional inclusive date range) at a priority; the **default scene** is the always-on floor that fills every gap. **The resolver lives in `@polyptic/protocol`, not the server** — the server's ticker fires from it AND the console's "what plays when" week strip is painted from it, so the strip cannot promise the operator something the wall will not do. **Resolution is a total order** (documented, no coin-flips): higher priority wins → then the window that STARTED most recently (lunch inside opening hours is the newer thing on air) → then the SHORTER window (the more specific one) → then the OLDER schedule (`createdAt`, then id). A window belongs to the day it STARTS on, so a Friday 22:00–02:00 daypart runs into Saturday morning. **The ticker (`scheduler.ts`, default 10s) calls the EXISTING `applyScene`** — the same code the operator's Apply button runs — so a scheduled switch fans out over the ordinary `server/render` WS push, in the ordinary <150ms, with no reload; the agent and the player learn nothing and neither package changed. It fires only on a **CHANGE OF VERDICT**, never on a timer edge: an operator who applies a scene by hand mid-window KEEPS it until the next boundary (the schedule is a floor, not a leash), and a boot ASSERTS the schedule once (a control plane that restarts at 09:05 puts the morning wall back by itself). **Timezone is explicit and configurable** (`SchedulerSettings.timezone`, one per deployment, seeded from the server's own zone, refused if the runtime doesn't know it) and every evaluation is pure WALL-CLOCK CONTAINMENT in that zone — never absolute-instant arithmetic, never a pre-computed fire time. That is what makes **DST a non-event**: a window spanning the spring-forward gap stays covered (not skipped); a window that STARTS inside the gap has no wall-clock moment to fire on and correctly does not run that day; and the repeated fall-back hour re-resolves to the same scene, which — because the ticker only fires on a change of verdict — means no double-apply. **Existing `scheduleAt` values are DROPPED, not migrated** (`ALTER TABLE scenes DROP COLUMN IF EXISTS schedule_at`): a stored-but-never-fired time is not a schedule (no recurrence, no window, no priority), and inventing one from it would make a live wall start flipping scenes nobody asked it to. Deleting a scene deletes the schedules bound to it; deleting a daypart still in use is a 409 (tidying the library must not silently unschedule a wall); a bad timezone is a 400 (a typo must not quietly move every window an hour). | Provisional | The decoy was the single most obvious "finish me" in the product, and scheduling is the strongest asset of every competitor (Xibo dayparts, Yodeck calendars): it turns "a wall you drive" into "a wall that runs itself". Provisional because it has not yet run a real operator's week on real hardware — the resolution rules (especially the recency tie-break) and the daypart-vs-per-schedule-window split are the parts most likely to want revising once someone builds a real day out of them. |
| D130 (POL-94; renumbered from D92 at v0.2.36 merge) | 2026-07-14 | **The content library is an inventory: search + kind filter + sort, a usage fold, and a live health badge sourced from the PLAYERS (POL-94).** *(Provisional — proposed with the PR; supersede or accept on review.)* Three parts. (1) **Usage** — `ControlPlane.getContentSourceUsage()` folds desired state into, per source, the screens / video walls / playlists / pages that reference it (ids, not just counts; a wall counts as ONE wall, not as its member screens — the operator assigned content to the wall). It rides in `admin/state.sourceStatus` (optional = back-compat), drives the library's "Used on 2 screens · 1 playlist" line, and lets the delete confirmation say exactly what it will break ("this will clear 2 screens (Lobby 1, Lobby 2), and remove a step from 1 playlist (Lobby rotation)") instead of the true-but-useless "any screen showing it will be cleared". (2) **Health from the glass, not from the server** — POL-86 already gave the player a `SurfaceProber` that PROVES a URL fetchable before painting it; the honest signal about a wall's content is the one the wall itself has. So the prober now emits a verdict on state CHANGE only (never per probe, never per retry) and the player sends `player/surface-health {screenId, surfaceId, sourceId?, url, state, at, detail?}`; the server folds it per source (`SourceHealthTracker`, live-only like Presence: pessimistic — one broken screen makes the source broken; a screen that drops takes its verdict with it; a source nobody shows is `unknown`, never a comforting green). Attribution works because `decorateSliceForSend` now STAMPS `Surface.sourceId` at send time (same clean-at-rest pattern as the POL-24 token and the POL-29 friendly name). REJECTED: a server-side reachability probe (it reports the SERVER's network, not the wall's — the failures that motivated POL-86 were all box-side; and a library of 200 URLs would become 200 outbound requests on a schedule, from the control plane, forever). (3) **Search/filter/sort are client-side** — a library is tens of rows, and a round trip to filter a list you are already holding is worse UX than typing into it. **What the badge cannot know, stated in its own tooltip:** it proves REACHABILITY, not correctness — the same-origin policy hides a cross-origin frame's contents from the box itself, so an HTTP 500, an expired session's login page and an empty dashboard all read as "loading". A wall nobody is showing reads "not checked" — we never probe a source no screen displays. | Provisional | The library grew into a flat pile: no search, no "used by", and no signal when a URL died — the operator found out from a passer-by while the prober's knowledge sat in the pod log (POL-86/D78). Surfacing it per SOURCE is how an operator thinks ("is the lobby dashboard broken?"), not per surface. Change-only reporting keeps non-negotiable #3 intact (no player work per frame, no polling of a fleet's dashboards), and the send-time stamp adds nothing to the DB. |
| D105 | 2026-07-14 | **POL-116 (provisional): a box that cannot fetch the build its medium PINNED heals itself in the initramfs — and retention stays a simple count.** The offline/Wi-Fi menu pins `root=live:…/builds/<imageId>/rootfs.squashfs` (D67) and is only re-pinned by `update-poll` AFTER a successful boot; retention (D54) prunes a build once `retainBuilds` (default 3) newer ones exist. A box powered off across three rebuilds therefore came back to a 404 it could never boot past — livenet retrying it forever, 5s apart (real hardware, 2026-07-14). The fix is a dracut `netroot` hook (`polyptic-pinned-fallback.sh`, sourced by `/sbin/netroot` in the very shell that holds `$netroot`, so livenet is re-pointed without patching it): probe the pin; if the depot answers but no longer has it, take the ACTIVE build id from the ungated `manifest.json` (POL-79's `ensureActiveBuild` heals the mirror on the fetch), else the unpinned arch root; if NOTHING answers, change nothing — a dead network keeps the old retry behaviour and is never dressed up as a prune. The swap is LOUD: a plain-English splash line and a `pinned-build-missing` boot report (`warn`, not `bad` — the wall is up). **Pin-aware retention was considered and REJECTED**: protecting every build a fielded medium might still pin means effectively never pruning, so the depot would grow without bound to defend a box that may never return. | Accepted (provisional) | The initramfs is the one place that has both the failure and a working network — the box has just joined the Wi-Fi, and the only thing wrong is which build id it is asking for. Across daily rebuilds of the same kernel ABI the module mismatch the pin guards against is usually a non-event, and a box that boots with a loud warning beats a box that never boots at all; a genuine mismatch still lands in the existing D67 recovery path (the missing-`/lib/modules` tell → refresh the medium → reboot into a matched pair). Self-limiting: after this boot `update-poll` re-pins the medium, so the fallback fires once. |
| D106 | 2026-07-14 | **POL-118: stage-1 GRUB DHCPs one card at a time and takes the first lease, and the wired wait finally talks. The GRUB-stage twin of POL-76/D71.** A wired boot on the dual-NIC amd64 box (Realtek `r8169` + Intel I225 `igc`, one cable) sat on `Starting Polyptic ...` for minutes. `net_dhcp` with no argument runs DHCP on **every** card GRUB enumerates and shares ONE retransmit loop across them, breaking out only when no card still needs a poll (GRUB 2.12 `grub_cmd_bootp`) — so the carrier-less port kept the boot waiting long **after** the live port already had its address. Exactly the shape D71 fixed in the initramfs; GRUB never learned it. Wi-Fi boots dodged it because the offline path never uses GRUB networking. **What GRUB script can and cannot do, read out of the pinned source (grub 2.12-1ubuntu7.3, the signed `grubnet*.efi` we ship) and then checked in a VM, not assumed:** it CANNOT enumerate cards (`net_ls_cards` only *prints* them, and the language has no command substitution) and it CANNOT see carrier or link state; it CAN target one card (`net_dhcp [CARD]`, `[CARD]` matched against `card->name`), it CAN branch on the result (`$?` is the raw `grub_err_t`: 0 = leased, 36 = `GRUB_ERR_NET_NO_CARD`), and after a lease it CAN read the address back out of the variable GRUB registers for the interface it configured (`net_<card>_dhcp_ip` — the interface is `<card>:dhcp` and the colon becomes an underscore; note `net_default_ip` is NOT set on this path, `is_def` is 0 in `grub_net_process_dhcp`). EFI names cards `efinet0..N` in enumeration order, so stage 1 now **walks the names itself and stops at the first port that answers** (or at the first `36`, i.e. the end of the box's real ports). The bare all-cards sweep survives on the failure menu's **Try again**, which is the escape hatch for a box with more than four ports or a GRUB that names cards some other way. **The honest limit: the ideal fix is not reachable from a config file.** What GRUB *should* do is poll every card in parallel and return the moment ONE leases — that is a change to `grub_cmd_bootp`'s loop, i.e. to a **signed binary we do not build and will not touch (D47)**. From script you get to choose one of two shapes, and we chose the one that never punishes success: an unplugged port can no longer outlive a working one, but if the dead port is enumerated FIRST we still pay its own bounded DHCP schedule before reaching the live one, and a box that is offline on **every** port now pays each port's schedule in series rather than sharing one. **Measured under OVMF** (QEMU/HVF, the real pinned signed shim + grubnet, two NICs, one wired to nowhere): live port first **18.3 s → 6.3 s** (the bug, and the fix); dead port first 18.3 s → 18.7 s (no better, no worse); offline single-NIC 18.4 s → 18.5 s (unchanged — the `36` stop earns its keep); offline dual-NIC 18.4 s → 31.7 s (**the price, +13 s, paid only on a path that was already failing on its way to the Wi-Fi/local payload**). On real hardware a carrier-less port costs more than a VM's (the firmware's SNP transmit path, `send_card_buffer`'s 4 s per-packet wait), which is why the observed stall was minutes rather than the ~25 s GRUB budgets for itself — and why the win is bigger there than the VM shows. **The wall now talks while it waits:** `Looking for a wired network ...` → `Got an address (10.0.2.15) - fetching the boot menu ...` → `Starting Polyptic - downloading the operating system ...`. This is NOT a D65 regression: D65 threw the *diagnostics* off the happy path (RAM, arch, "image"), not *progress*. A wall that is slow but talking is a wall you can read; a wall that is silent for three minutes is indistinguishable from a dead one. **And an operator can now WATCH a boot:** a fourth menu entry, `Watch this screen boot (verbose)` (server menu) / `Try again, and show the network conversation` (stage-1 fallback), sets `debug=net,efinet,http` + `pager=1` and, on the server menu, boots the same kernel **without** `quiet splash` so the transcript runs on into the initramfs instead of hitting the Plymouth curtain — verified under OVMF: 78 lines of GRUB's own DHCP/HTTP narration on one boot. Chosen deliberately, never the default, so the happy path is untouched. Secure Boot is untouched: this is `grub.cfg` text, and not one loader byte moves. | Accepted (provisional) | The operator's box stalls for minutes on every wired boot and says nothing while it does — the two halves of POL-118, and the second is the one that makes the first unreadable from the room. The per-card walk is the only fix a *config* can express, and it is the right trade: the case it fixes (a live port waiting on a dead one) is every healthy wired boot, and the case it costs (every port dead) already ends in a fallback. **VM-verified end to end** (stage 1 → real server menu → four entries painted) on the aa64 signed loader under OVMF; the pending real-hardware check is the dual-NIC amd64 box itself, where the firmware's behaviour on a carrier-less port — not GRUB's — sets the worst case. |
| D107 | 2026-07-14 | **POL-120: the player stamps the version it was actually built from, not `package.json`'s permanent `0.0.0`.** Every wall's badge, idle splash and `player.diag` line reported `v0.0.0` — worthless exactly when an operator is staring at a misbehaving screen asking which build it is (caught live on the amd64 box). Two halves: `packages/player/package.json` is never bumped (the real version is the git TAG, which `release.yml` already passes into the image build as `POLYPTIC_VERSION`), and the Dockerfile declared that `ARG` **after** the two `vite build` RUNs — an ARG is only in scope for the RUNs that follow it, so even a config that read the env would have seen nothing. Now `vite.config.ts` stamps `POLYPTIC_VERSION` (leading `v` stripped) with `pkg.version` as the dev fallback, and the ARG is hoisted above the SPA builds. The agent already did this correctly (`POLYPTIC_BUILD_VERSION` via `bun build --define`, D-lineage POL-7) — the player simply never got wired to the same seam. Pinned by `boot-splash.test.ts`, including an assertion that the ARG precedes the player build step: the whole bug in one line. | Accepted | A wall that cannot tell you which build it is running makes every other diagnostic a guess — and this is the same diagnosability discipline POL-86/D78 was built on. The fix costs nothing (the value was already being passed into the image; it just never reached the last hop). |
| D108 | 2026-07-14 | **POL-123 (provisional): the chart brings its OWN Postgres — a first-party StatefulSet, on by DEFAULT — because the `postgresql.*` values were a LIE and a fresh install crash-looped.** `values.yaml` advertised a bundled database (`postgresql.enabled`, `auth.*`, `primary.persistence.*`) that the chart had never implemented: no dependency in `Chart.yaml`, no template anywhere. `postgresql.enabled=true` deployed **nothing** — and the shipped default (`config.store: postgres` + `postgresql.enabled: false` + an empty `externalDatabase.url`) installed cleanly and then crash-looped the server on `DNSException: getaddrinfo ENOTFOUND`, reaching for a database nobody had created. Live on the operator's cluster after a k3s reinstall, **an hour of downtime**; it had only ever worked because of an undocumented hand-applied companion manifest (a PVC + StatefulSet + Service named `polyptic-db`, `postgres:16-alpine`) that had to land BEFORE the helm install. A self-hostable product whose chart cannot bring up its own database in one command is not self-hostable. **Bundled StatefulSet, NOT a subchart:** ~60 lines of first-party template beats another OCI dependency to vendor — POL-70 already taught us the cost (the cert-manager chart had to be vendored as a tgz plus a `.helmignore` fix, because helm silently *hides* an unvendored subchart), and a Bitnami dependency adds a large surface, a registry/licence to track, and an upgrade path we don't control, to run one `postgres:16-alpine` pod. **Default ON *and* fail-fast**, both: `postgresql.enabled: true` means a bare `helm install` just works, and `templates/postgres.yaml` runs an ungated pre-flight (`polyptic.validateDatabase`) that REFUSES to render an impossible config — a bundled DB *and* an `externalDatabase.url` together, `store=postgres` with **neither** (the exact crash-loop), an unknown `config.store` — each with a plain-English message naming the value to set. Never render a config that cannot possibly work. **Password:** generated (32-char random) and preserved across `helm upgrade` by the same `lookup` pattern as `secrets.cookieSecret`, with `auth.existingSecret` support; the shipped literal `password: polyptic` is dead. It is never baked into a rendered connection string either — it rides as `POSTGRES_PASSWORD` and the kubelet expands `postgres://polyptic:$(POSTGRES_PASSWORD)@polyptic-db:5432/polyptic` in the pod. **Data:** a `volumeClaimTemplate` (`data-<release>-db-0`), which neither Kubernetes nor `helm uninstall` will delete — a wall's registry should take an explicit `kubectl delete pvc` to destroy — with `PGDATA` in a subdirectory (initdb refuses a mount root holding a `lost+found`) and `pg_isready` probes (the port listens long before the database answers). **The hand-rolled manifest has an adoption path:** the bundled Service is named `<fullname>-db` — `polyptic-db` for the conventional release — so an existing `DATABASE_URL` still resolves, and `postgresql.persistence.existingClaim` mounts the PVC that is already there (`kubectl delete statefulset polyptic-db --cascade=orphan` first, then `helm upgrade` with the claim + the old password). Pinned by `packages/e2e/bundled-postgres.test.ts`: 14 tests, file pins on the seams plus real `helm template` renders of every posture, including each refusal. | Accepted (provisional) | The one-command install is the whole promise of a self-hostable chart, and this one couldn't keep it — it shipped values describing a feature that did not exist, which is worse than not having the feature, because it reads as configuration and behaves as a trap. Provisional: the bundled Postgres is deliberately a single unreplicated pod (the control plane is a single writer anyway), and a deployment that outgrows that should point `externalDatabase.url` at a managed database — the posture the chart still supports first-class. |
| D109 | 2026-07-14 | **POL-121 (provisional): a fresh install builds its OWN first OS image. An empty depot is a bug, not a waiting room.** After a clean `helm install` the depot was empty and *nothing* filled it: the nightly cycle (D51) only REFRESHES an existing image, and the weekly full rebuild (D52, Sun 02:00) is the only thing that creates one — so a new deployment could sit **up to seven days** unable to netboot a single screen, with nothing in the console telling the operator to go and press ⋯ ▸ Full rebuild. Worse, it failed *quietly*: the boot-medium install hook ran, found no image, and fell back to `LEAN=1` (D68), so the operator downloaded a **subtly lesser** medium — wired-only, no Wi-Fi payload — whose only trace was one line in a Job log. Fix, at the seam that already reconciles the depot at boot (`ImageUpdates.start()` → `retain()`, D74): `bootstrapFirstImage()` fires the FULL hook ONCE, asynchronously (it is a k8s Job — the server never stops serving), when it finds **no published image for any arch**. The full Job's own last step re-bakes the boot medium, so the download becomes the full, Wi-Fi-capable stick instead of the LEAN one baked against an empty depot — the auto-build deliberately takes the same path as the operator's Full rebuild rather than inventing a second one. **The guardrails are the feature.** (1) EMPTY ONLY — one published image anywhere and it is a no-op; it never overwrites and never re-triggers against an existing image. (2) ONE SHOT, EVER — a new `firstBuildAt` latch is claimed in the STORE (`image_rollout.first_build_at`, COALESCE'd so no write can un-latch it) **before** the hook is spawned, so a crash-looping or rescheduled pod reads the claim and stands down; a `running` row from a previous pod is respected too (the Job outlives the pod that asked for it). A build that FAILS is announced, not retried — a retry keyed on "the depot is still empty" is exactly the build storm we must not have. (3) The operator's choice wins: no `IMAGE_FULL_REBUILD_CMD` (dev stack, laptop server, `imageUpdates.enabled: false`) or the weekly cycle switched off → no build at all, just a log line saying netboot is unavailable until an image exists. (4) It ANNOUNCES itself: a Live Activity line ("Building the first OS image — screens can't netboot until this finishes" → "First OS image built …"), the existing Settings status chip reading "Building first image", and an empty-depot notice on the Onboard Screens card — reusing the `lastBuild`/`images` surface rather than a parallel mechanism. Tested: 8 unit tests (empty-depot trigger, non-empty no-op, idempotency across a simulated restart AND a crash-loop that kills the pod mid-build, disabled, unconfigured, the feed's start/finish lines, a failed build that is not retried) + a 3-test real-server e2e on two depots (`first-image.e2e.test.ts`). Full suite 639/639 under CI's Bun 1.3.14. NOT verified: a real cluster (the Job actually running, and the re-baked medium coming out FULL rather than LEAN). | Accepted | Non-negotiable #4 is "zero-click cold boot to content" — a deployment that cannot boot a screen until an operator finds an undocumented menu item, for a week, is not that. The empty depot was the one state the reconciler had no opinion about; now it has one. The latch, not the depot's emptiness, is what makes the build unrepeatable — state you can crash-loop against must live in the store, not in the filesystem you are about to fill. |
| D110 | 2026-07-14 | **POL-122 (provisional — D105–D109 are provisional in other PRs, so this number may need renumbering at merge): a fresh install publishes NO boot medium rather than a misleading one, and the medium now describes itself.** Two very different sticks were sharing one filename (`polyptic-boot.img`), one URL (`/dist/boot/polyptic-boot.img`) and no metadata: the FULL universal medium (local payload per arch + `wifi.conf` — the whole POL-63 feature: it boots a screen with no cable) and the LEAN one (`LEAN=1`, the pre-POL-63 dongle: wired netboot only). `boot-medium-job.yaml` deliberately fell back to `LEAN=1` when the depot held no `initrd-wifi` — which on a **fresh install is always** — "keeping 'Download bootloader works from minute one' true". So the first medium every new deployment ever published was the crippled one, 64 MB of wired-only stick that the console offered as "Download bootloader" with no caveat, and nothing downstream could tell the difference: `resolveBootMedium()` stat'd the file, `provisionBootSummary` reported a `boolean`, the console button enabled on "a file exists". Flash it for a Wi-Fi screen and the screen simply never boots, with no tell anywhere. Three parts. **(1) The Job never bakes lean automatically:** an empty depot now SKIPS the bake, exits 0, and logs what is missing and what unblocks it (build the first live image; the full rebuild bakes the medium itself, and `helm upgrade` re-runs the Job). Publishing nothing beats publishing a lie — the download button is allowed to be dark for the minutes before the first image exists. `LEAN=1` survives as an **explicit, documented escape hatch on the script** for someone who genuinely wants a wired-only dongle; nothing selects it for them. **(2) The medium describes itself:** `build-boot-medium.sh` writes a sidecar manifest beside the image (`polyptic-boot.json` — `lean`, `arches`, `imageIds`, `mediumId`, `builtAt`, `tokenBaked`, `bytes`) via a new POSIX-`sh` `write-boot-manifest.sh` (its own script so the contract is *executed* by a test, not grepped — and no exotic tools, POL-78), and it rides to the depot with the image (the Job copies `deploy/dist/boot/*` wholesale; the server image now COPYs the new script). The manifest is a cross-process contract, so it is a zod schema in `protocol` (`BootMediumManifest`), parsed at the edge; a corrupt one degrades to inference, it never takes the download down. **(3) The server and console tell the truth:** `NetbootInfo` gains `bootMedium: {url, lean, arches, imageIds, mediumId, builtAt, tokenBaked, selfDescribed} \| null` (the bare `bootMediumUrl` stays as the download URL); `provisionBootSummary.bootMedium` becomes `"none" \| "lean" \| "full"` and a lean medium gets a loud boot-time `warn`; the console gates the button on the medium's real shape with four honest states — **full** (download), **lean** (downloadable, but labelled "Download (wired-only)" with a warning that a cable-less screen cannot boot from it), **building** ("Building the first OS image — screens can't netboot until it finishes"), **none** ("No bootloader yet", with what to run). A pre-POL-122 medium on an upgraded deployment's PVC carries no manifest, so its lean-ness is **inferred from size** (the lean medium is a flat 64 MiB, a payload medium is >= 384 MiB by construction — the inference cannot flip) and nothing finer is claimed (`tokenBaked: null`, `selfDescribed: false`). | Accepted (provisional) | The failure was silent in the worst place: the very first thing an operator does on a new deployment, with the very artifact everything else depends on, and a symptom (Wi-Fi screens never boot) that shows up hours later on a wall, far from the button that caused it. A dark button with a sentence explaining what is building is a better first-run experience than a live button that hands you a stick that cannot boot half your fleet — and the self-describing manifest means no future caller can make the same "a file exists" mistake. |
| D111 | 2026-07-14 | **POL-119: cast to a screen — a native AirPlay receiver window, generic `cast` framing, always-on on-screen PIN.** AirPlay is Apple-proprietary but mirroring is reverse-engineered well enough that **UxPlay** (GPLv3, GStreamer, Ubuntu universe) is a mature receiver; per the no-vendor-names rule the feature is **cast** with `airplay` as its first kind. The player content path is UNTOUCHED — the cast is an OS window the agent supervises and fullscreens on top of the browser on one connector; on disconnect the scene content underneath is revealed, no reload. `Screen.castEnabled` is persistent, TTL-less desired state (unlike `Machine.shellEnabled`'s arm+TTL — a meeting-room panel is meant to STAY castable; the PIN, not the toggle, gates each session): it rides the `server/apply` assignments (with the send-time `friendlyName` the receiver advertises on mDNS) so a reconnecting agent re-reaches it from the apply alone, and a toggle/rename re-pushes a **same-revision** apply + render (the D44 rename trick — neither is render data). The agent runs one supervised UxPlay per cast-enabled connector (`SupervisedProcess<T>` generalizes the browser supervision; distinct fixed base ports and a deterministic locally-administered per-connector MAC so instances don't collide and a respawn keeps its PIN registrations; `-reg` persists them, resetting harmlessly on a RAM-boot reboot). Receiver windows appear at SENDER-CONNECT time — the PIN prompt is a window too — so sway gains a PERSISTENT pid-matched window watch (vs. launch-time `waitForWindow`): every appearance is moved+fullscreened onto the connector, and window presence IS the session signal, level-reported via `agent/status.screens[].casting` (heartbeat + immediate on change) into `Presence`/`ScreenView.castActive` → the console's "Casting now". Non-negotiables baked into argv and pinned by tests: `-pin` ALWAYS (no PIN-less path — proof of physical presence), `-as 0` (video only, no audio server on the image), `-vs waylandsink -fs` natively (POL-67 forbids Xwayland sinks — so x11-i3/dev-open REFUSE `setCast` with a reason, sway-only for now). Image: `uxplay avahi-daemon gstreamer1.0-plugins-bad` in a new `cast` apt set (wayland only) + `systemctl enable avahi-daemon` (shipped avahi rather than UxPlay 1.74's experimental built-in mDNS). Console: Inspector cast toggle + live session state, canvas tile "Casting" badge, Machines-row chip; player badge shows a static cast glyph (stamped on `server/render` like the name). No-gos held: no wall-spanning casts, no other cast protocols yet (the seam is the generic framing), no cross-VLAN discovery, DRM content won't mirror. | Accepted | The wall should serve the person standing in front of it: walk up, pick the screen in the native Screen Mirroring menu, present, disconnect — without an operator hosting the content somewhere first. Reusing the placement/supervision machinery keeps the blast radius small; the pitch's 10-day appetite fits because the seams (window placement, apply reconcile, badge push) already existed. |
| D112 | 2026-07-14 | **POL-92 (provisional): the box reports its OWN health on every heartbeat, and the fleet becomes scrapeable — with the `/dev/dri` fd as the software-render tell.** Three layers, one sample. **(a) The wire.** `agent/status` grows an optional `vitals` object (`@polyptic/protocol`): CPU busy %, cores, loadavg, memory, root-fs usage (on a netbooted box that IS the RAM image), hottest thermal zone, uptime, the RUNNING `imageId` (`/etc/polyptic/image-id` — which is how an operator learns a box never took a roll-out), and per-output browser vitals (tree RSS, respawn count, `gpuAccel`). EVERY field is optional and an unknown is OMITTED, never zeroed: a fleet upgrades one box at a time, so a pre-POL-92 agent's status frame must keep parsing forever (pinned by a test), and a zero on a meter is a claim we may not be able to make. **(b) The tell.** `gpuAccel` is read by walking the supervised browser's PROCESS TREE in /proc and looking for an open fd on `/dev/dri/*` — Chrome opens it in its GPU process, a CHILD, and sway holds its own (which must NOT launder the browser's verdict). No fd anywhere in the tree = the browser is painting the wall on the CPU. This is exactly the D77 failure — surf under Xwayland lost DRI3, software-rendered, and pegged a real amdgpu box at 300% — and finding it cost a remote-shell session with `top` and `/proc/PID/fd`. It is now a red banner in the console and a `polyptic_machine_gpu_accelerated == 0` alert. An agent that could not tell reports NOTHING (an unknown must never page someone at 03:00). **(c) Cheapness is a constraint, not a nicety.** The sampler reads a handful of /proc files and one pass of `/proc/*/stat` per heartbeat (10s) — no `top`, no `ps`, no shelling out, unprivileged throughout; the sample is best-effort and a failure never costs the heartbeat (a machine that stops heartbeating reads as OFFLINE, which would be a far worse lie than a missing meter). **(d) Server.** Vitals ring in `Presence` (live-only, 30 samples ≈ 5 min, never persisted — vitals describe a RUNNING box) and ride the existing coalesced `admin/state`; `admin/state` carries them ONLY while the machine is online, so the console can say "System stats unavailable while offline" instead of drawing an epitaph. **(e) Metrics.** `/metrics` exports per-machine labelled gauges (`polyptic_machine_up`/`_last_seen_seconds`/`_cpu_percent`/`_memory_percent`/`_disk_percent`/`_temperature_celsius`/`_gpu_accelerated`/`_image_info`, `_browser_respawns_total` as a counter) plus depot fetch counters and image build age, hand-formatted through a tiny in-repo registry (`server/src/metrics.ts`) rather than a Prometheus client dep. An approved box that goes dark stays in the exposition at 0 — an absent series cannot fire an alert. Sample rules ship at `deploy/prometheus-alerts.example.yaml`. **(f) Console.** The stats strip POL-68 §1 designed and deferred: 4px bars, tabular-nums, tooltips, thresholds <70 ok / 70–89 warn / >=90 bad, and an overload banner with HYSTERESIS (arms at 90, clears at 85) because the mock's banner strobed on a box hovering at the threshold. | Provisional | The heartbeat carried only `observedRevision` + placement ok/note, so the fleet's #1 known failure class was diagnosable only by hand, per box, over a remote shell — and `/metrics` had six process-level gauges, making "is box 37 up?" unanswerable by the Prometheus stack a self-hoster already runs. Exporting per-machine labels turns mature Grafana/Alertmanager tooling into free fleet monitoring, vendor-neutral by construction (non-negotiable #5). **Provisional** because the GPU tell, the thermal zones and the RAM-image disk numbers have only been exercised against fixture /proc trees and a dev stack — the shapes are pinned by tests, but the readings themselves need a real wall box (amd64 Chrome + an arm64 surf fallback) to confirm. |
| D131 | 2026-07-14 | **POL-95: the ACTIVE scene is server-authoritative, and applying one is previewed before the wall jumps.** The console's "Active" badge was a client-side fiction: `activeSceneId` was set optimistically in `stores/console.ts` when *that tab* clicked Apply, and the server's own `DesiredState.activeSceneId` — which `applyScene` had been setting all along — was never persisted and never left the building. So a reload showed no badge, a second operator showed no badge, and neither noticed when a manual change had already pulled the wall away from the scene. A control plane that is the brain cannot let a client invent its state. Now: the active scene is **persisted** (single-row `meta.active_scene_id`, alongside the revision — it survives a restart), **broadcast** in `admin/state` (optional on the wire = back-compat), and **cleared on divergence** — every manual mutator (place/unplace/remove, combine/split, screen/wall content, mural or machine removal) calls `reconcileActiveScene()`, which re-diffs the live wall against the scene and drops the badge unless *nothing* would change. The console's optimistic write is deleted; the badge is now told, never guessed, so two consoles and a reloaded tab always agree — over the existing WS broadcast, no refetch, no reload (D5). Second half: **the apply-preview** (`GET /api/v1/scenes/:id/diff` → `SceneDiff` in `@polyptic/protocol`) — the server, the only party that knows BOTH the snapshot and the live wall, returns the changeset in exactly the terms `applyScene` reconciles in (content `from → to` with the library source's *name*, move/place/unplace, combine/split, cleared) plus plain-English `warnings` for what apply would silently skip (a captured screen that is gone, a source since deleted → that target lands EMPTY). The console renders it as a hovercard on the scene chips (Wall) and the Apply button (Scenes). It is a **read-out, not a gate**: apply stays one click. Crucially the diff is the SAME engine the divergence guard judges the badge with, so the preview and the badge can never disagree — right after an apply the diff is `identical`. | Accepted | Terraform-plan / kubectl-diff: show the plan, then reconcile. A console that lies about which scene is live corrodes trust in everything else it shows; and a blind 12-screen apply was the one operator action with no read-out. Both halves fall out of the same authoritative answer, so they cannot drift apart. |
| D113 | 2026-07-14 | **POL-127: a StatefulSet's `volumeClaimTemplates` carries STABLE labels only — the bundled Postgres had made every `helm upgrade` fail, permanently.** POL-123/D108 rendered `polyptic.labels` into the PVC template, and that helper embeds `helm.sh/chart: polyptic-<version>` + `app.kubernetes.io/version` — values that change on EVERY release. `volumeClaimTemplates` is one of the IMMUTABLE fields of a StatefulSet spec, so the first upgrade after a version bump was a forbidden patch by construction: `cannot patch "polyptic-db" ... updates to statefulset spec for fields other than 'replicas', 'ordinals', 'template', ... are forbidden`, leaving the release wedged in `failed`. Caught live on v0.2.32 → v0.2.33 — and the reason it survived POL-123's 14 tests and a full from-scratch cluster validation is that **a fresh install never exercises a patch**: the bug is only reachable on the second release, which is exactly the path a self-hosted product's users take and its authors don't. The fix is `polyptic.selectorLabels` (name + instance) + `component: database`; the StatefulSet's own metadata keeps the full provenance labels, because top-level metadata is mutable. (The pod `template` already used the stable set, which is why the pod spec was never what blocked the patch.) Pinned by rendering the chart at two different chart versions and asserting the `volumeClaimTemplates` block is BYTE-IDENTICAL — the whole bug in one assertion — plus a seam pin that `polyptic.labels` never appears below `volumeClaimTemplates:`. Verified the test actually catches it by reintroducing the bug (4 fail) and removing it again (0 fail). **Existing clusters need one manual step**: the poisoned StatefulSet must be dropped with `kubectl delete statefulset polyptic-db --cascade=orphan` (the PVC and the running pod survive untouched) before the next `helm upgrade` can recreate it with stable labels. | Accepted | The worst failure mode a self-hostable product can ship is "your upgrades now fail forever, silently, and the fix is undocumented". The general lesson is bigger than the label: **immutable fields must be rendered from immutable inputs**, and the only honest test for that is to render twice at different versions and diff — asserting on the contents of one render would have passed happily while the bug sat there. |
| D133 | 2026-07-14 | **POL-111 (renumbered from D114 at v0.2.36 merge — D114–D132 taken by sibling PRs): per-screen template variables, substituted at SEND time, never persisted.** One source, fifty screens: a screen gains an optional key/value `variables` map (`@polyptic/protocol`, edited in the console Inspector) and content carries `{{placeholder}}` tokens that the server resolves **per receiving screen** in `decorateSliceForSend` — the exact seam POL-24/D63 stamps credential tokens at, under the exact same rule: **the DB and the stored slices always keep the CLEAN, tokenised value.** Substituting on the way IN would fossilise fifty copies of one source, which is the problem the feature exists to kill; substituting on the way OUT means a rename, a variable edit or a source edit re-resolves for free, a reconnecting screen gets the current substitution rather than a fossil, and a wall's members each get their own resolved slice with no extra machinery. Scope = the screen's own map + always-available built-ins `{{screen.name}}` / `{{screen.id}}` / `{{machine.hostname}}`; custom keys are dot-FREE, which is what makes shadowing a built-in impossible. Substituted: web/dashboard/media URLs, playlist entry URLs, page embed URLs, and page text/ticker/countdown-label text. NOT substituted, deliberately: feed URLs and weather locations (inputs to a server-side poller whose cache is keyed by that string — per-screen values would fan the cache out per screen for no product win) and QR content. **Security is per context, three deep.** (1) At the edge, a value may not contain control characters or `{{`/`}}`. (2) Substitution is single-pass by construction — `String.replace` with a *function* replacer scans the TEMPLATE only, so a value is never re-scanned and `$&` in a value is never re-interpreted: no recursive expansion, no way for one variable to reach another. (3) In a URL the value is percent-encoded, so it cannot introduce a scheme (`javascript:`), a parameter (`&orgId=666`), a fragment, a traversal or a quote that escapes an attribute; the result must still parse as http(s) or it degrades. **A placeholder in the SCHEME or AUTHORITY is refused outright** (the template goes out unsubstituted): percent-encoding is no defence there, because the WHATWG URL parser percent-DECODES the host, so an encoded `/` or `@` in a value comes back to life and re-cuts where the host ends. Variables are a path/query/fragment feature; a per-region SOURCE costs nothing. In TEXT nothing is escaped and nothing is mangled — the player paints text NODES (there is no `v-html` anywhere in `@polyptic/elements` or the player), so `<script>` is displayed, not executed, and an operator who wants a `<` on the glass gets one. Undefined placeholders resolve to EMPTY (never literal braces on a wall) and the price is paid in the console: `ScreenView.unresolvedVariables` is server-computed from the screen's clean slice and shown as a warning badge on the Variables card, which is the only way an operator ever finds a typo'd `{{lien}}`. Setting variables does not bump the revision (the slice is unchanged; only its decoration is) — the same-revision re-push DOM-diffs in place: instant, no reload. 35 tests, incl. the property that nothing substituted reaches the store (in-memory slice, library source, persisted rows, and a restart) and the injection cases above. | Accepted | "Screens are first-class and named" applied to content. The alternative — fifty near-identical sources differing by one query param — is the duplication the content library exists to prevent, and it rots the moment the dashboard URL changes. The send-time seam is not an implementation detail but the whole design: it is what keeps one source one source, and it already existed. |
| D129 | 2026-07-14 | **POL-109 (D129; renumbered from D114 at v0.2.36 merge — D123 also collided, then POL-133's D114): media upload becomes an INGEST PIPELINE, and an un-probeable file is ACCEPTED WITH A WARNING, never rejected.** Upload was a byte sink: an AVI (MPEG-4 Part 2) was accepted with a smile and discovered as a black rectangle on a wall; a playlist's video steps had no duration to plan with (the rotation leaned entirely on the `ended` event); the library had no pictures. Now `POST /api/v1/media` runs **probe → validate → poster** before the file is allowed to become a `ContentSource`: metadata (duration/dimensions/container/codecs) lands in the media catalogue's sidecar, a **poster frame** (video) or **downscaled thumbnail** (image) is generated, and a container/codec a wall browser provably cannot decode is **refused at upload with a sentence that names the format and states the fix** — never as a black screen on the glass. **The seam:** core (`media.ts`/`rest.ts`/`state.ts`) knows only a `MediaProber` interface (`available()` / `probe()` / `poster()`), exactly like `DisplayBackend` and `ContentSource`; the one implementation that shells out to an external media toolchain lives in `media-probe.ts` alone, its binaries are configurable (`MEDIA_PROBE_CMD`/`MEDIA_FRAME_CMD`, `MEDIA_PROBE=off` kills it), and the playability policy (`assessPlayability`) is a pure, fully-tested function — no vendor name in any core path. **The call:** a file the server could not inspect (no toolchain — a dev laptop, a minimal container) is **accepted**, marked `probed: false`, and the operator is told the file could not be checked. Refusing it would turn "I don't know" into "no", break every existing deployment's uploads the moment the tool is absent, and punish the operator for the server's missing dependency; ingest therefore refuses only on **evidence** (a codec we KNOW is undecodable), never on its absence — an unknown-but-not-blacklisted codec is likewise accepted with a caveat. `ContentSource.media` (POL-109, zod) is **derived, never stored**: decorated on at read time from the catalogue that travels with the media volume, so no DB migration and a re-ingest is instantly visible. Video surfaces + playlist entries carry `poster`, so the player pre-paints a video's own first frame instead of black; the console's playlist editor auto-fills a video step's hold time from the probed duration (making a rotation fully TIMED, which is what lets every wall member derive the same entry from the clock, POL-34); library rows show real thumbnails/posters and the probed facts. Tests: a pure policy suite + a **real probe of committed 6–12 KB fixtures** (H.264 MP4, MPEG-4-Part-2 AVI, PNG) + an e2e that drives the route both WITH and WITHOUT a toolchain — CI stays green on a runner that has neither binary. |
| D128 | 2026-07-14 | **POL-112 (D128): audio end-to-end — the player's `muted` hardcode is deleted, `volume` joins the contract, and a wall stays SILENT until an operator says otherwise.** `PlaylistRotator` shipped `muted` as a literal attribute on its video element, so the `muted` flag the protocol had carried since Phase 1 was decorative: a playlist could not make a sound however the control plane asked, and nothing anywhere could set a level. Now `VideoSurface` and `PlaylistSurface` both carry `muted` (default **true**) + `volume` (0–1, default 1), the player applies exactly what the wire says, and the Inspector grows a Sound card (toggle + slider) beside the zoom control. **Audio belongs to the (target, content) PAIR, not to the screen and not to the library source** — the same call as page zoom (D62/POL-57), and for the same reason: the lobby panel wants the showreel loud and the safety briefing silent, so a per-screen master volume is a knob an operator has to remember to re-set on every content change; and one clip is the sound of the lobby but wallpaper in the boardroom, so it cannot live on the source either. It is persisted (`audio_preferences`, keyed exactly like `zoom_preferences`) and restored when that content returns to that target. **The default is asymmetric with zoom, deliberately: silence.** A pair nobody has touched is MUTED, so NEW content always arrives silent and only the same content returning restores what the operator dialled in — an unexpectedly loud lobby wall is a support call, and a legacy/partial payload must decode to quiet, never to noise. **The one-unmuted-panel guard is server-side** (`wallMemberAudio`): every member of a combined surface decodes its own copy of the spanning video, so unmuting them all would play the room N out-of-phase soundtracks — the control plane gives the operator's intent to the ANCHOR member (index 0, stable across pushes) and forces every other member muted, on every rebuild, whatever a client asks for. The console shows the wall's *intent* (not the guard's per-panel consequence) so the Inspector never claims a sounding wall is silent. **Autoplay:** kiosk Chrome already launches with `--autoplay-policy=no-user-gesture-required` (POL-67), which is what makes unmuted sound-on-cold-boot work with no interaction. Where that flag is not in force — the **surf** fallback under Xwayland, a dev browser, a hardened profile — an unmuted `play()` REJECTS, so the player falls back to **muted playback** and logs it to the diag trail: picture always beats sound, a wall must never freeze on a dead first frame because it wanted audio. Pinned by 36 tests (contract defaults + bounds, the player's wire-is-truth/silent-legacy/muted-fallback rules, and the control plane's muted-default, per-pair memory, restart survival, and the wall guard restated as "exactly one sounding panel"). | Accepted | The protocol was telling a lie the player quietly ignored, which is the worst kind of contract bug: it typechecks. Making sound opt-in per placement (rather than a global or per-source setting) is the only default that cannot embarrass an operator, and putting the echo guard in the control plane means no client — console, script, or future integration — can talk a wall into shouting at itself. |
| D124 | 2026-07-14 | **POL-105 (renumbered from D114 at v0.2.36 merge — sibling PRs claimed D114–D122): every box reports the OS image it BOOTED, and a roll-out can be staged behind a TAG instead of dropped on the whole fleet at once.** Two halves of one gap. **(1) Visibility.** The control plane knew what it SERVED and never what each box had actually booted, so "which machines are still on 20260711T…?" was unanswerable. The box already reads `/etc/polyptic/image-id` for POL-92's vitals; that id now also rides `agent/hello` (first frame of a session — a canary reboot is exactly the moment an operator is watching) and is **PERSISTED** on the machine row (`image_id`, `image_id_at`), unlike the online-only vitals ring: the machine a bad roll-out stranded is precisely the machine that is now DARK, and an epitaph is the whole point. A change ANNOUNCES itself in the Live Activity feed ("wall1 booted image X (was Y)") — that line is the only evidence a roll-out reached a box. A hello carrying no id never erases the one we knew (older agent, dev box; `COALESCE` in the upsert). Console ▸ Settings grows **Fleet versions**: the fleet bucketed by the build each box is running, live off the admin WS, offline boxes included, with a build the depot has since pruned shown as its own (most interesting) bucket. **(2) Targeting.** The `urgent` flag was a fleet-global hammer — a bad image hit every box at once. A **roll-out RING** now pins one build for the machines a **POL-103 selector** matches: `{selector: "tag=canary", arch, imageId, urgent}`, ordered, first match wins. `GET /dist/image/<arch>/manifest.json?machineId=<id>` (the box's own 5-minute poll, ungated as ever) is resolved PER MACHINE: the box's tags decide which ring it matches; a box matching none — including every untagged box, every id the server has never heard of, and every pre-POL-105 box that sends no `machineId` at all — gets the arch's ACTIVE build, i.e. exactly the old behaviour. **A ring can only ever narrow who deviates from the fleet, never widen it** (an unparseable selector targets nothing, per POL-103's empty-selector rule). Each ring carries its OWN urgency, so "canary reboots now, the fleet waits for the nightly window" is finally expressible. **PROMOTE** (`POST /api/v1/settings/image/promote`) is one action: activate the ring's build for that arch (the D54 relink → everyone reboots into it) AND retire the ring, with one activity line — a promotion that left the ring pinned would leave a canary pinned to what is now merely the fleet build. The box-side change is one query parameter in `update-poll.sh`; it still just compares two ids. **RETENTION IS DELIBERATELY UNCHANGED (D105's call stands).** Pin-aware retention — "never prune a build a ring still wants" — is NOT reintroduced: a forgotten ring would stop the depot pruning forever. Instead a ring must name a **retained** build when it is set (400 otherwise), and if retention later prunes it out from under a long-lived ring, the resolver degrades that ring's machines back onto the **active** build and says so loudly (one warn line per stranded ring, not one per box per poll). In practice the interaction barely bites: prune keeps the newest `retainBuilds` and never the active build, and a canary by definition pins the NEWEST build — so only a ring left pinned to an ageing build across N rebuilds is stranded, which is a stale canary anyway. **Not solved here:** the depot still makes each new build ACTIVE the moment the hook publishes it (D51), so the staged flow is: hold the fleet on the known-good build (Recent builds ▸ Roll fleet back), pin the new build to `tag=canary`, watch Fleet versions, then Promote. Making a fresh build land INACTIVE would change D51's contract and is left as a follow-up. Tests: 20 unit (ring resolution incl. prune-stranding, arch scoping, first-match, unparseable-selector-targets-nothing; the distribution incl. offline + pruned + never-reported boxes), 6 server (ring validation, per-machine resolve, promote, restart durability), 6 REST e2e on a real control plane + real agent sockets (two boxes, one tagged; the canary is served a different build from the same depot; promote converges them), and 3 shell (the poll sends `?machineId=`, and sends nothing meaningless when it has no id). | Accepted (provisional) | The image pipeline (D51/D52/D54) could rebuild, publish and roll back, but it could not answer the first question an operator asks after a bad night ("who is still on the old build?") and it could not do the one thing that makes an automatic pipeline safe to trust at fleet scale: try the new image on three screens before the other forty-seven. Both gaps are the same missing fact — WHICH BOX IS ON WHICH BUILD — so they are one ticket. Targeting reuses POL-103's tags rather than inventing a rollout-group concept: an operator who can already say "the atrium" to a reboot should be able to say "the canary ring" to an image, in the same words. |
| D122 | 2026-07-14 | **POL-104 (provisional): the flat fleet bootstrap token becomes a SET of scoped, revocable batch tokens, and boxes can be declared before they boot. The upgrade cannot strand a single stick already flashed.** One shared secret was baked into every boot medium ever made: a stick that walked out of the building compromised enrolment for the whole estate, and rotating it meant a helm upgrade plus re-flashing every medium. A token is now a first-class record — named (the batch/site it was cut for), optionally EXPIRING, optionally CAPPED at N new machines, individually REVOCABLE — and exactly one carries a `bake` flag: the one `GET /boot/grub.cfg` writes into the kernel cmdline, which is the one `deploy/build-boot-medium.sh` lifts straight back out of that menu at bake time (the helm Job greps `polyptic.token=` out of the live server's own menu, so an operator's choice of bake token reaches the stick with NO change to the bake script). **THE MIGRATION IS THE FEATURE.** A token model change has a physical consequence: the media in the field carry the old secret, and there is no way to reach them. So on the first boot after the upgrade the server LIFTS the existing `bootstrap.token` into the new table verbatim — same secret, no expiry, no cap, still the bake token, marked `legacy` — and keeps the old single-row `bootstrap` mirroring whichever token is currently baked (so even a DOWNGRADE finds a live token where it expects one). Every stick that enrolled a box yesterday enrols one today; nothing changes until an operator deliberately rotates or revokes. Pinned by an e2e that boots the server the way an existing gated deployment comes up (`POLYPTIC_BOOTSTRAP_TOKEN` set) and enrols a box on that exact secret. **THREE RULES, and the second is the one that keeps walls lit.** (1) A token that is revoked/expired/used-up enrols NOTHING — and says WHICH, because "invalid token" on an expired stick sends an operator hunting for a typo that isn't there. (2) **Revocation gates the DOOR, not the room:** a machine that already enrolled holds a durable per-machine credential (Phase 2b), and `authenticate` consults NO token on the credential path — so revoking the stick a wall came in on never darkens that wall. Killing a box stays a separate, explicit act (reject it). The console says so at the point of the click, and the machine's card carries an `enrolledVia` chip that reads as provenance, not status. The one thing a revoked token cannot do is RE-KEY a box that lost its credential — that is a new decision, and it goes through the operator. (3) **Rotation has a grace window**: rotating cuts a successor, hands it the bake flag, and puts the OLD secret on a 24 h timer instead of killing it (never EXTENDING it past its own expiry), so boots in flight and media not yet re-flashed still land; `graceHours: 0` is the louder "a stick is gone" path. The pre-POL-104 `regenerate` action — which replaced the one token outright, i.e. stranded the entire estate on a click — is now exactly this rotation. The enrolment CAP counts MACHINES ONBOARDED, not re-keys: a box that already consumed a slot may still re-key against a used-up token, or a wiped disk would lock a wall out of its own slot. **`POST /boot/report` now accepts ANY token the deployment recognises, including a revoked or expired one** — deliberately: a box booting from a stick we just cut is exactly the box whose boot report we most need, the route grants no authority and mutates no registry state, and gating it on the current bake token would have gone silent on every medium in the field the moment an operator rotated. **PRE-REGISTRATION composes with enrolment, it does not replace it.** A `pre_registrations` table (label, tags, auto-approve, keyed on machineId / DMI serial / MAC) is consulted ONLY AFTER a hello has authenticated against a valid token: it is not a credential and admits nothing on its own — all it decides is what a box that has ALREADY proved it belongs is CALLED, which tags it carries, and whether a human clicks Approve. That ordering is the entire security argument for keying on something as soft as a MAC — and it IS soft: the homelab lesson (POL-63/POL-78) is that a Wi-Fi-bridged VM's MAC is REWRITTEN by its host, so a MAC is a convenience, never a proof. Hence: strongest key first (machineId → serial → MAC), vendor placeholder serials (`To be filled by O.E.M.`, `Default string`, …) never match, and **an AMBIGUOUS match matches NOTHING** — auto-naming (and auto-approving) the wrong physical panel is worse than one manual approval. A matched record CLAIMS its machine, so it can never quietly re-target another. Tags ride the record and are stored + shown today; the one-line hookup onto `Machine.tags` is left for POL-103 (#88) to land, rather than shipping a second, competing tags column. **The other half of the ticket is pure operator win**: pending cards now carry the box's MACs, chassis serial, vendor/model, arch, its LIVE IP (live-only — an IP describes a connection, and a stale one on an offline card is a lie) and the token it came in on, and the section gained an "Approve all N". `agent/hello` grows an optional `hardware` (`HostIdentity`) that a pre-POL-104 agent simply does not send — every field optional, an unknown OMITTED rather than zeroed, and a hello with no hardware never blanks what the row already knows. The on-glass side of ident already existed: a pending box's screens show the POL-46 holding board carrying the same machine id the card does, so the card ↔ panel mapping is closed without a new protocol message. **The honest limits:** token secrets are stored RAW, not hashed — `/boot/grub.cfg` has to bake one into a kernel cmdline and a hash cannot be baked into anything (true of the flat token this replaces: POL-104 does not lower the bar, but it does not raise it either); and a medium remains a credential (POL-63's local Wi-Fi payload), which is now a per-batch credential rather than a fleet one. Tests: 19 policy units (every negative — revoked, expired, used-up — plus the do-no-harm reconnect and the grace window), 14 matcher/CSV units, 14 e2e against the real control plane. | Accepted (provisional) | "What happens when a stick walks out of the building" is the first question a security review asks, and the honest answer was "you re-flash the estate". The scoped token makes the blast radius a batch; the grace window makes rotation something an operator will actually do rather than fear; and the credential/token split means the safe act (cut the stick) can never be confused with the destructive one (take the wall down). Provisional on two counts: the store schema is exercised against Postgres only through the migration path in CI, not a live cluster, and the pre-registration CSV shape will meet real delivery notes before it is settled. |
| D114 | 2026-07-15 | **POL-133 (provisional — several open PRs also claim D114/D115, renumber at merge): presentation controls — playlist step zoom is D62 one level down; embed zoom, feed font size and QR colours are AUTHORING-time element props; an unscannable QR is refused at the contract edge.** Four operator asks from live testing, one dividing line: **does the knob change what the composition IS, or how one screen shows it?** (1) **Playlist step zoom is per-(screen, step source)** — the D62 pair taken at its word: a page playing as a playlist step is still "this content on this glass", so each resolved `PlaylistEntry` gains `zoom` (default 1, framed kinds only) and `PUT …/screens/:id/playlist-zoom` / `…/walls/:id/playlist-zoom` `{sourceId, zoom}` re-stamps that step inside the EXISTING surface — same surface id, same `startedAt`, and the player's rotation signature deliberately ignores zoom — so the carousel keeps its position and a live step rescales with no reload (D5). The value is remembered in the SAME `zoom_preferences` table under the step source's `source:<id>` key, which means the pair identifies the content and the target, not the route: the same dashboard assigned directly to that screen shares the dialled-in zoom (pinned by a test). Consistent with POL-112's audio choice of (target, content) — audio is one setting per rotation because an operator unmutes "the lobby playlist", but zoom must be per STEP because each framed page lays out differently. (2) **Studio embed zoom is a property of the ELEMENT, not the (screen, element) pair** — the deciding argument is D74's one-renderer invariant: a composition must render identically in the Studio preview and on every wall, and a per-screen embed zoom would make the same saved page look different per target with no authoring surface to see it on; an operator who wants per-screen variants saves two pages. `PageEmbedElement.props.zoom` (default 1) renders through the shared `embedZoomStyle()` (the D62 1/zoom-layout-then-scale math, inside the element's box) in `@polyptic/elements` — both surfaces consume the same function, so preview==wall by construction. (3) **Feed font size is `props.fontScale`** (percent, 50–200, default 100): a multiplier over the existing height-proportional sizing rather than an absolute px, so 100 renders BYTE-identically to a pre-POL-133 composition (pinned) and the card's masthead/headline/age keep their proportions at any size. (4) **QR colours are `props.fg`/`props.bg`** (defaults = the old hardcoded pair) with scannability enforced AT THE CONTRACT EDGE: `qrContrastIssue()` (in `protocol`, because the zod refine needs it and the console reuses it) REFUSES a hex pair below 3:1 contrast — a QR that doesn't scan is worse than an ugly one — and returns a loud warn for inverted (light-on-dark, which some cameras refuse) or marginal (<4.5:1) pairs, which the Studio renders as a red banner; non-hex colours are unjudgeable and never refused. Every new field is optional-with-default, so pre-POL-133 playlists and compositions parse forever to exactly the old behaviour (pinned across protocol/server/elements). Console: the playlist steps get the SAME −/value/+ ZoomControl the screens Inspector uses (one per framed step, per screen or per wall); the Studio grows a matching zoom control, a font-size slider and free colour pickers. | Accepted (provisional) | The (target, content) pair vs the composition is the load-bearing distinction: zoom on a playlist step serves "this 4K panel needs THIS step bigger" (per-screen state, D62's whole point), while an embed's zoom, a feed's type size and a QR's colours change what the authored page IS — and anything that changes the page must live where the D74 invariant can hold it: in the definition, rendered by the one shared renderer. Refusing unscannable QR pairs in the schema rather than the UI means no future caller — API script, import, migration — can persist a code the camera can't read. |
| D115 | 2026-07-15 | **POL-130: the boot theme carries a real `desktop-image` — GRUB 2.12's gfxmenu scales one on EVERY draw, and a desktop-color-only theme hands it NULL. "File exists" is not "file loads", so PNGs are now gated by a decoder check at bake and at heal.** The operator reproduced POL-87's `error: null src bitmap in grub_video_bitmap_create_scaled` + **"Press any key to continue"** on a FRESHLY-CUT v0.2.3x medium — which D79 could not explain: a fresh stick is never torn, and dissecting the actual failing image (mtools) showed `theme.txt` + `logo.png` present, complete, byte-identical to the committed assets, the logo a perfectly GRUB-decodable 8-bit RGB non-interlaced PNG. The root cause is upstream, read out of the pinned grub 2.12 source and then watched frame-by-frame under OVMF on the operator's own image: `grub_gfxmenu_view_draw()` → `init_background()` passes `view->raw_desktop_image` to `grub_video_bitmap_create_scaled()` UNCONDITIONALLY (default scale method STRETCH; `verify_source_bitmap` rejects NULL either way), and a theme that sets only `desktop-color` leaves that image NULL — so `GRUB_ERR_BUG` sits latent in `grub_errno` while the menu paints pixel-perfect off `desktop-color` ("Starting in 1 s", logo and all), then fires onto the glass the instant the countdown boots the entry, trailing the keypress prompt D65 exists to ban. EVERY themed boot, wired or offline, torn or fresh, had this; D79's pair-guard fixed the real torn-media class but could not touch it. Fix, three parts: **(a)** the theme gains `desktop-image: "bg.png"` — an 8x8 solid-`#0b0b0d` PNG stretched over the panel, visually identical to the `desktop-color` fill it replaces (`desktop-color` stays as fallback). The bytes are built in `boot-theme.ts` dependency-free and byte-deterministically (a STORED deflate block — no zlib version can change the output; grub's own inflate handles stored blocks), committed as `packages/server/assets/boot-bg.png`, served at `GET /boot/bg.png`, regenerated by `render-boot-theme.ts`, drift-pinned committed==generated. **(b)** the theme is now a THREE-file set everywhere D79 made it a pair: `render-local-grub.sh` guards nested on theme.txt+logo+bg (anything less → plain dark menu, silent — VM-verified fail-closed), `build-boot-medium.sh`/`update-poll.sh heal`/`offload.sh` write bitmaps first + theme.txt last, and the poll's orphan repair treats a bg-less theme — every pre-POL-130 fielded stick, i.e. the erroring state itself — as broken, so fielded media self-heal to plain-then-rethemed with no re-flash. **(c)** a new `grub-png-check.sh` (POSIX sh + dd + od only, the POL-78 lesson) validates every theme bitmap against grub 2.12 `png.c`'s actual limits (8/16-bit, truecolour±alpha, non-interlaced, nonzero dims): `build-boot-medium.sh` FAILS THE BUILD on a reject (a medium that errors on the glass must be unbuildable — POL-121's quiet-degradation lesson), and the heal refuses to commit an undecodable fetch. Verified end-to-end under QEMU/OVMF on the operator's actual failing medium: before — themed menu, then error+prompt at boot; after — themed menu, then a clean `Starting Polyptic ...`; guard-tripped (bg deleted) — plain menu, silent. Tests: `boot-theme-png.test.sh` (+e2e wrapper) pins the checker against grub's accept/reject matrix incl. the committed assets; wifi.test.sh 38-series extended (bg in heal/orphan/torn cases; new bad-PNG case proves an interlaced fetch is never committed and the next good poll recovers); boot-splash pins the three-file guard string, the desktop-image line, and the bake's validate+reject. | Accepted | The menu that LOOKS perfect is the one that errors — the failure lives in the frame nobody screenshots, between countdown-zero and the kernel's first pixel, which is why D65's and D79's verifications both missed it. A theme property GRUB insists on scaling is part of the theme's contract, so the pair becomes a triple and the writers' commit-ordering discipline extends unchanged; and once "present" stopped implying "loadable", presence-only guards needed a decode gate at the two places bytes enter the pipeline. Rejecting at build/heal is cheap and names the file; rejecting on a wall is the entire bug class. |
| D116 | 2026-07-15 | **POL-134 (provisional — open PRs claim D114/D115, so this number may need renumbering at merge): agent mTLS is ON BY DEFAULT, and the require posture graduates itself — zero-config secure, no operator ceremony.** The POL-25/D82 machinery shipped behind `AGENT_MTLS_PORT` and the boot log said `agentMtls: "off"`; operator verdict (v0.2.29 t25, verbatim): "Why isn't it enabled by default? I want it just on and working and secure." Now `resolveAgentMtlsEnv` defaults the dedicated listener ON at :8443 with NOTHING configured; `AGENT_MTLS=off` (or the old `AGENT_MTLS_PORT=0`) is the escape hatch. The default-on/explicit split matters: an EXPLICITLY configured listener that cannot come up stays FATAL (D82's never-serve-a-fake-gate invariant, self-test included), while the zero-config default degrades LOUDLY to off (`mtls.default.unavailable` + the boot summary names the reason) — a dev laptop with :8443 occupied, or a Bun ≤ 1.2 runtime that cannot verify client certs, must not brick a server nobody configured. **The require posture is a persisted state machine (`MtlsPosture`), not a flag**: it starts MIGRATING (plain channel still admits; certless agents keep working and pick up certs on their next hello — the no-truck-roll upgrade path, narrated per box as "wall1 now on mTLS") and AUTO-PROMOTES to REQUIRED once ≥1 machine exists and EVERY known machine has been SEEN on the mTLS listener (`mtlsSeenAt`, persisted per machine alongside `mtlsCertIssuedAt`). Automatic-with-notice over one-explicit-click, argued: a click every deployment must eventually click is a default in denial (and exactly the ceremony the ticket deletes); promotion is safe by construction because even under REQUIRE the plain door still authenticates + issues (zero-click netboot survives — first contact by token → cert → mTLS reconnect), and the only strandable client would be an agent too old to CSR, which the all-machines-SEEN condition proves absent; an EMPTY fleet never promotes (vacuous truth must not lock out dev tooling before the arc ever ran once). Promotion is one-way, persisted (`agent_mtls_posture` row), announced in the activity feed, re-evaluated on boot/each mTLS hello/each settings GET; `AGENT_MTLS_REQUIRE=1/0` pins it either way — and a **pin=1 PERSISTS the promotion** (unpinning later must not regress a required fleet to migrating), while pin=0 suppresses without erasing the row. **Required never silently degrades** (review round 2): the persisted posture is consulted BEFORE the degrade decision, so a REQUIRED deployment (row or pin) whose listener cannot start at boot is FATAL even under zero-config — a crash loop is loud, a plaintext fleet drifted onto by self-healing agents is not; `AGENT_MTLS_REQUIRE=0`/`AGENT_MTLS=off` are the explicit consents that re-allow plain admission. An unparseable `AGENT_MTLS_PORT` likewise refuses to boot (explicit config with a typo must not silently mean off). Surfaced for operators: `GET /api/v1/settings/agent-security` + a Console ▸ Settings ▸ "Agent security" card in plain words (what the agent channel carries — shell, DevTools, previews, placement; players/boot surfaces have their own gates; per-machine cert state), and the boot log line reads `:8443 (required|migrating)` or `off — <reason>`. Chart: `agentMtls.enabled` defaults **true**; `enabled=false` must render an explicit `AGENT_MTLS: "off"` (the server otherwise self-enables); `require: ""` = self-managing, `true`/`false` = pinned. Pinned end to end over real sockets: the zero-config cold-boot arc, the migration narration + graduation, and the negative (a certless agent gets no session once required; a certless TLS dial dies in the handshake) — suites capability-gate on the runtime actually verifying certs and spawn the server on `process.execPath` (a PATH `bun` may be 1.2). | Accepted | "Secure by default" is a product decision, not a feature flag, and the operator's confusion was a UX bug as much as a defaults bug: the posture had no story an operator could read. The design keeps every D82 invariant (self-test, cert=transport-gate credential=identity, first-contact door) and turns the one manual step that remained — deciding when to require — into a provable condition the server checks better than a human would. |
| D117 | 2026-07-15 | **POL-132 (provisional — sibling PRs also carry provisional D-numbers; renumber at merge): the player APP survives a page reload while the control plane is down — a shell service worker, cache-first, /player scope only, with page-owned swap timing.** The v0.2.29 outage drill found the hole D83 left: content survived an outage (last-good slice + IndexedDB media) but the player shell was fetched from the control plane on every load, so a reload mid-outage rendered the browser's "no available server" page — the healing layer never ran because the thing that would run it couldn't load. Reboot survival stays explicitly OUT of scope (operator: "an antipattern to how we work"; the netboot chain owns cold start). **What shipped:** (a) a Vite `generateBundle` plugin emits `dist/sw.js` — a static, reviewable worker template (`sw/shell-sw.js`) prefixed with this build's config: version, shell URL, and the EXACT emitted file list (content-hashed names are only knowable at build time). Cache-first forever — a stale player that paints beats a fresh 502; revalidation happens at the REGISTRATION level (a new build = a byte-different sw.js = a browser-installed update into its OWN versioned cache), never by mutating a live cache, so a cached shell and its assets are internally consistent by construction. (b) Scope discipline: registered under `/player` (spa.ts stamps `Service-Worker-Allowed: /player` + `no-cache` on sw.js), and within scope it answers ONLY player navigations and precached paths — /api, /media (D83's cache owns media), Range requests, cross-origin dashboards and POSTs pass through untouched, pinned by executing the generated worker against a fake SW scope in tests. (c) Version discipline (D107): the worker NEVER skipWaiting()s on its own; the page revalidates on every player-WS open and tells a waiting build to take over only at that safe moment — server reachable, so the one reload repaints instantly from the last-good slice and reconnects immediately — logging "shell from cache (vX) → updating to vY" in the diag trail (D78); activation retires all older shell caches. (d) A reload-from-cache says so: "shell from cache, restored slice rev N" in player.diag. (e) The kiosk reality: service workers need a SECURE CONTEXT and netboot walls speak plain HTTP by design (D47/D52), so the agent's Chrome argv grows `--unsafely-treat-insecure-origin-as-secure=<player origin>` for http origins only — honestly stated: the flag grants that origin the FULL secure-context treatment (WebCrypto, geolocation, getUserMedia, …), not just SW, acceptable only because a kiosk's one origin IS the control plane it already trusts with the wall (https deploys get no carve-out); SW storage lands in the per-connector `--user-data-dir` (D67), surviving a browser respawn (XDG_RUNTIME_DIR tmpfs — gone on reboot, which is in-spec). The surf fallback (arm64) has no such flag: those boxes keep pre-POL-132 behaviour, and the player logs exactly that. (f) Dev never suffers: registration is `import.meta.env.PROD`-only (Vite/HMR can never be cache-poisoned) and `?sw=off` is the kill switch (unregisters, runs uncontrolled). Verified by the automated outage drill in packages/e2e (playwright-core against a real Chromium + the real server serving the BUILT player): content up → SIGKILL the plane → RELOAD → last-good content paints and the trail says so → server returns with a NEWER build → live reconcile with no manual action, then a logged swap into the new build. | Accepted | The wall is the product's face and a reload is an operator's most reflexive recovery move — it must never make an outage LOOK worse than it is. Cache-first + registration-level updates is the one shape where offline correctness and version honesty don't fight: the shell always paints, and the next successful contact always wins. |
| D118 | 2026-07-15 | **POL-136 (provisional — open PRs claim D114–D116): the agent learns the pairing PIN from the receiver's stdout and the PLAYER paints it — the receiver never draws its own PIN.** D111 designed the PIN flow on the premise that "the PIN prompt is a window too", handled by the persistent pid-matched window watch. It is not, and never was: UxPlay's `display_pin` callback renders the PIN as ASCII art on STDOUT (`uxplay.cpp`, via `LOGI`) and its raop library logs `*** CLIENT MUST NOW ENTER PIN = "NNNN" AS AIRPLAY PASSWORD` right after (`raop_handlers.h`) — no GStreamer window exists until MIRRORING starts, which is after pairing. So the wall showed nothing while the phone demanded a code, and the box's journal showed nothing either: the browser-output filter keeps only failure-looking lines, and a PIN doesn't look like a failure. The fix takes the ticket's "agent learns the PIN" path: the agent already supervises the receiver and owns its stdout, so a per-connector `CastPairingTracker` (pure, timer-injected, pinned by tests against the verbatim upstream format strings) parses the announcement line — keeping UxPlay's per-pairing RANDOM pin, the strongest proof-of-physical-presence (a fixed `-pin NNNN` in argv was rejected: one long-lived code, readable in /proc, defeats the point) — and clears it on registration, on the mirror window appearing, on receiver death, or on a 120s TTL. Surfacing rides the existing paths end to end: `agent/status.screens[].castPin` (level-reported like `casting`, self-healing across reconnects) → Presence → a new ephemeral `server/cast-pin` player frame (the `server/ident-pulse` pattern; never render data, never persisted, replayed to a player that connects mid-pairing) → a fullscreen static overlay (D66 motion rules) with the code at 24vw. This is the RIGHT half of D111's trade, not a reversal: window presence remains the SESSION signal (the mirror is a window), but the PIN phase never had a window to signal with — and the player is exactly what is on the glass at pairing time, so no new native window, no new placement machinery, no new image packages. Adversarial review added three hardenings: the receiver is spawned through `stdbuf -oL -eL` (verified present at spawn; loud fallback without it), because UxPlay's logger is unflushed `printf` — into a pipe, libc block-buffers ~4 KiB and the PIN lines could otherwise sit unread past the whole pairing window, silently reproducing the bug one layer down; the PIN itself NEVER enters the activity feed (it rides every admin/state snapshot and lingers in the ring — the feed announces the pairing, the code travels only the gated per-screen player channel and the box journal); and the server's stored PIN is TTL-guarded (120s, heartbeat-refreshed) so a lost clear can never replay a stale code to a late-connecting player. Failure is loud twice over: a pairing with no parseable PIN logs a `***` line in the agent journal (`onPinMissing`), and a pairing whose screen has no connected player pushes a `bad` activity-feed line pointing the operator at the box's journal. | Accepted | The feature's headline flow — walk up, pick the screen, type the code the WALL shows — was unusable because the code only ever existed in a stdout nobody could see. Owning the PIN's whole journey through our own supervised/authenticated channels is strictly more reliable than hoping a third-party process draws a window, and it reuses every existing seam (status level-reporting, player overlay, activity feed) without touching the content path. |
| D119 | 2026-07-15 | **POL-117 (provisional — D114–D117 are claimed by open PRs; renumber at merge if needed): the operator's name is a machine's identity; the hostname never is — and a PENDING box can ident.** Every netbooted box boots the same live image, so every box reports the hostname `localhost.localdomain`: with several boxes queued for approval the Machines view was a wall of identical "names" over unreadable `dmi-…` ids. Three parts. **(1) Manual naming on the existing field.** `POST /api/v1/machines/:id/rename` writes `Machine.label` — the SAME field the hostname used to default into — so a rename simply wins from then on (`labelForHello` treats "label ≠ id AND meaningful" as an operator name and never overwrites it; deliberately additive so POL-104's pre-registration names, which land in the same field, compose with no seam). Any machine, any status: naming a still-pending box is the point. Registry metadata like a screen rename — no revision bump; the coalesced `admin/state` broadcast relabels every open console live (<150ms, no reload). **(2) The hostname stops pretending.** A shared contract helper `meaningfulHostname()` (protocol, so server and console can never disagree) rejects `localhost` / `localhost.localdomain` / `*.localdomain`; the server refuses to ADOPT such a hostname as a label (the label stays = machineId, the unnamed sentinel — and a STALE pre-fix `localhost.localdomain` label relabels to the sentinel on the box's next hello, because a meaningless label is not mistaken for a rename), and the console refuses to DISPLAY one (`machineDisplayName` renders "Unnamed box · <id tail>" — honest, comparable across cards). A meaningful hostname (a real deployment that names its boxes) is still adopted, as before. **(3) Pre-approval ident rides `server/pending`, not a new capability.** A pending box has no screens and hence NO player WS to pulse — and the trust model wants nothing NEW reachable pre-approval. So `POST /machines/:id/ident` on a pending machine re-sends the existing `server/pending` frame with `&ident=1` appended to the holding-board URL (and again without it on the TTL); the agent's existing URL-diff handling re-places the board, which comes up flashing the machine id (the POL-46 pending page reads the param — same overlay approved screens get). Zero new agent code, works on already-deployed agents; the console stretches the TTL to 12s because the re-place relaunches the kiosk browser (seconds, not millis). The same PR closes the seam the test found: everything ELSE now 409s pre-approval (reboot already did; the shell ARM now does too — approval is the trust boundary, and an armable pending box would have been a terminal on hardware nobody admitted; disarm stays always-allowed). Pinned by 16 unit tests (label adoption, rename round-trip + persistence across a ControlPlane restart, rename-beats-hostname, the never-shows-localhost display rule) + 6 e2e tests against the real gated server (pending ident on/off frames on the agent socket, reboot/shell 409 + no frame leaks to the agent, rename of a pending machine broadcast live, name survives re-hello). | Accepted (provisional) | Screens are first-class and named; machines are plumbing (non-negotiable #1) — but plumbing still has to be TELLABLE-APART at the one moment an operator must point at physical hardware: approval. A name an operator chose beats a hostname nobody did, an honest "Unnamed box" beats a confident lie, and the ident path reuses the one channel a pending box already holds rather than widening what unapproved hardware can be told to do. |
| D135 | 2026-07-15 | **POL-144 (provisional): the AirPlay mirror was torn and banded because the box had no HARDWARE H.264 decoder — a decode-path fix, not a sink or vsync one.** POL-136 got the PIN flow working end-to-end on a real iPhone (pairing, code on the panel, mirror starts), leaving one defect: the mirror itself came through "torn and distorted" on the Intel wall box (v0.2.35). The operator's screenshot is not a clean vsync shear (which would be one horizontal displacement line over an otherwise-coherent image) but the whole pillarboxed portrait strip shredded into horizontal bands with smeared colour — the signature of a stride/format mismatch, not tearing proper. The territory the ticket flagged held up: the image shipped the modern GStreamer `va` decode plugin (inside `gstreamer1.0-plugins-bad`, which the `cast` set already pulls) but NO VA DRIVER, so `vah264dec` registered no hardware decoder and UxPlay's `decodebin` fell back to `avdec_h264` (ffmpeg, software). On a busy Intel CPU that both approaches the 300%-peg POL-67 saw for the browser AND routes every frame through waylandsink's software SHM buffer path, where a dropped/again-honoured `GstVideoMeta` stride turns padded I420 into exactly this banding. The fix is a PACKAGE one, not an argv one: add the vendor-neutral `va-driver-all` (Intel-media + i965 + Mesa-Gallium VA backends; the box's own GPU binds its backend at runtime) plus `vainfo` to the apt `cast` set, so `vah264dec` lights up and hands waylandsink dmabuf frames it scans out directly — the same "keep the GPU in the path" principle as POL-67, one layer down. The D111 argv is UNTOUCHED and stays correct: `-vs waylandsink` was never the problem (it is the native-Wayland sink POL-67 mandates), it just needed a hardware decoder feeding it; `-pin`/`-as 0`/`-fs` and the D118 `stdbuf`-wrapped spawn + PIN stdout parsing + pid-matched window watch all keep working (the change adds packages only, no process/argv change). dnf/pacman get the neutral Mesa VA driver + `libva-utils` for parity, though casting stays best-effort there (no packaged uxplay). Pinned by codecs.test.ts (VA driver present for wayland-sway, absent for x11-i3/dev-open). Cannot be verified without an Apple device: the image MUST be rebuilt (the cast packages ride `polyptic-agent setup`, so the running fleet lacks them) and re-tested on the Intel box — mirror clean, CPU not pegged (`top` shows uxplay/gst well under a core), `vainfo` reports a bound driver and an H.264 decode entrypoint. | Accepted (provisional) | The feature's last defect was that the mirror was unwatchable, and the evidence (banding not shear, software-only decode stack, an Intel iGPU sitting idle) points at decode, not display. Shipping the hardware decoder is the smallest change that both fixes the artefact and lifts the CPU, reuses the exact POL-67 lesson, and touches no argv/process contract POL-119/POL-136 depend on. |
| D120 | 2026-07-15 | **POL-140 (provisional — D114–D119 are claimed by open PRs; renumber at merge): the pinned-build fallback stops narrating on the splash — the swap is console + boot-report only.** D105 made the POL-116 self-heal LOUD on three channels: splash, console, and the `pinned-build-missing` boot report. The splash line ("The OS image this screen was set up with is gone; starting the newest one ...") was too much for the glass: it surfaces depot bookkeeping to whoever is in the room, on a wall that is public signage, during what is otherwise a standard startup that ends in a working screen. The line (and its `plymouth display-message`, plus the matching `hide-message` in the pre-pivot hook) is removed; the console line still names both build ids and the boot report still lands a `warn` in the activity feed — the operator-facing loudness D105 argued for is untouched, only the room-facing half goes. Pinned by the shell suite (the pruned-pin cases now assert an EMPTY plymouth log) and the e2e wiring test (the hook never invokes plymouth; the pre-pivot hook no longer hides a line nobody raises). | Accepted | "A box that boots with a loud warning beats a box that never boots" was always about the OPERATOR hearing the warning, not the audience: the people who can act on a pruned pin read the activity feed and the console, and neither channel changed. To the room, a self-heal that works should look like nothing happened. |
| D121 | 2026-07-14 | **POL-107: the single admin credential becomes real ACCOUNTS with ROLES — `admin` · `operator` · `viewer` — enforced by ONE per-route policy table on the server.** Until now there was literally one account: marketing could reboot a box, IT could delete the content library, and the activity feed could not name either of them. `users` gains a `role` column (`ALTER TABLE … ADD COLUMN IF NOT EXISTS role text NOT NULL DEFAULT 'admin'`), which is also **the upgrade path**: an existing deployment's `secrets.adminEmail`/`adminPassword` account is back-filled to `admin` and keeps every capability it had — an upgrade is never a lockout — and it can then invite the rest. Enforcement is a single table, `packages/server/src/roles.ts`: `requiredRoleFor(method, path)` names the MINIMUM role for each `/api/v1` route, roles are RANKED (`viewer < operator < admin`, each containing the one below), and **anything absent from the table requires `admin` — deny by default**, so the endpoint someone adds next month is closed until it is deliberately opened. The gate (`AuthService.requireAuth`) runs it on the same slash-collapsed path it already used for the 401 check, and answers **403** (authenticated, but may not) rather than 404-by-obscurity. The two WS doors are gated to match: a non-admin's `/admin` socket still receives state (that IS the read half) but its shell frames are refused, and the POL-67 DevTools CDP upgrade is admin-only, mirroring its REST routes. `viewer` = read + **apply a saved scene** (the "staff invoke" half of the author/invoke split every pro video-wall product ships); `operator` = content, layout, scenes, ident, casting; `admin` = machines, enrolment, image builds, settings, credential profiles, the shell, and operator management. The role is read from the ACCOUNT on every request, never baked into the session, so a demotion binds on the demoted operator's very next call; an admin password reset revokes that operator's sessions; and the last admin can be neither demoted nor deleted (a deployment nobody can administer is a bricked deployment). Console: Settings ▸ Operators (add / change role / remove) plus role-gated affordances everywhere — which is COSMETIC by construction, and the tests prove it by never touching the console. | Accepted | A shared password is an enterprise adoption blocker and it makes any future audit trail meaningless — "who rebooted the lobby wall?" has exactly one possible answer today. The design is deliberately shaped so the two open PRs compose onto it rather than fight it: **POL-106/OIDC** (PR #90) mints its session through the same `AuthService` seam, so claim→role mapping is one lookup that ends in the same `role` column; **POL-102/scoped API tokens** (PR #86) already keys its allow-list by (method, path-relative-regex) — the SAME shape as `ROUTE_POLICY` — so a token's scope and its minter's role intersect on one table instead of two competing ones. Deny-by-default is the load-bearing choice: it is the only rule under which a fast-moving REST surface stays safe by DEFAULT rather than by vigilance. |
| D134 | 2026-07-14 | **POL-115 (D134; renumbered from D123 at v0.2.36 merge — D123–D133 were taken by sibling PRs): a box watches its OWN UEFI boot order, REPORTS drift by default, and only re-asserts when an operator opts in.** Real firmware does not leave `BootOrder` alone: a firmware update, a "load setup defaults", or a disk OS running `grub-install` re-prepends its own entry (the homelab Lenovo does it every single time), so a box that offloaded cleanly months ago silently boots a stale disk OS on its next power-cycle — a dark screen, and a truck roll, discovered by a human rather than by the control plane. The box that is UP is the only thing that can see this coming, because it is the only thing that can read its own NVRAM. So `deploy/live/usr/local/lib/polyptic/boot-order.sh` runs on the existing 5-minute `polyptic-update-poll` (a ROOT system unit that already exists — the kiosk agent is not involved and gains **no new escalation**: a touch-file that triggers an NVRAM write would be a strictly larger attack surface for zero benefit, since the root poll already visits this every 5 minutes) and compares the firmware's `BootOrder` against the entry `offload.sh` installed. **The default posture is REPORT-ONLY**: the box writes nothing and posts `boot-order-drift` to the existing POL-58 `POST /boot/report`, which the control plane narrates as one plain-English Live Activity line ("… found its UEFI boot order changed and would boot something else next time … Nothing was written"). Re-assertion is an explicit fleet opt-in (Console ▸ Settings ▸ Boot order → `GET /boot/policy`, a new single-row `boot_order_policy` table, `BootOrderPolicy` in `protocol`), and the box asks for permission *every time*, immediately before it would act: an unreachable control plane, a 401, or a garbled answer all mean report-only, so no failure of ours can make a box start editing firmware variables. When it IS allowed, the write is additive and idempotent — our entry to the front, every other entry (the box's old OS included) kept **in its existing relative order** behind it; `efibootmgr -o` (reorder) and `-a` (activate OUR entry) are the ONLY writes, never `-c`, never `-B`, so no entry is ever created or deleted; a box already leading writes nothing at all (no NVRAM churn every 5 minutes, forever); a box with no entry of ours (never offloaded) is left completely alone. The claim is then **proved**, POL-58-style: NVRAM is re-read from scratch, and the correction is only reported as `boot-order-reasserted` if the firmware really kept it AND the set of boot entries is byte-identical to what it was before. A firmware that wins anyway reports `boot-order-reassert-failed` once — the failure mode is "boot order unchanged, operator told", never "boot order mangled" — and the drift state is reported once per boot, not every 5 minutes, so a weeks-long firmware fight does not turn the activity feed into a metronome. | Accepted (provisional) | Boot order is the one piece of a box's state that Polyptic depends on absolutely and did not watch at all: everything else self-heals over the WS, but a box that boots the wrong OS never reaches us to be healed. Reporting-by-default is the whole design — a mangled `BootOrder` can leave a box unable to boot **anything**, which is a worse outcome than the drift it would be fixing, so the destructive capability ships **off**, is opt-in per fleet, is re-authorized on every single use, and never touches an entry we did not create. **Provisional**: the decision tree is exercised end-to-end against a real `efibootmgr` capture with stubbed firmware, but the write path has not yet been run against real NVRAM on the Lenovo. |
| D132 | 2026-07-14 | **POL-108 (D132; renumbered from D123 at v0.2.36 merge — D123 was taken by POL-103, D124–D131 by sibling PRs): a `stream` surface kind — LIVE video (HLS today) is its own kind, not a `video` with a moving src, and its health is MEASURED, not trusted.** The contract gains `StreamSurface {url, protocol, muted, fit}` and `ContentKind.stream`; `protocol` (`hls` now, `whep` declared) IS the vendor-neutral seam — **RTSP is out of scope of the core**, an operator puts any RTSP-to-HLS restreamer in front and no vendor's name ever reaches core code. The player bundles **hls.js** because Chrome (D67, the wall browser) has NO native HLS: a `.m3u8` in a `<video src>` plays exactly as well as a text file, so MSE is the only path. It is BUNDLED, never a CDN script — a wall box may have no internet beyond the control plane, and Safari's native HLS is used directly where present. A library is not a vendor: the vendor-neutrality rule is about hard-wiring someone's *dashboard/IdP/camera* into core paths; hls.js is a rendering detail of the player behind the `protocol` enum, swappable without a contract change, exactly like Vue. **Failure is the substance of the ticket**: a live source dies in ways a file cannot, and the worst of them is SILENT — the playlist stops advancing, the buffer runs dry, and the element reports nothing at all, so the wall sits on a black rectangle while the browser insists everything is fine. So health = PROGRESS (does `currentTime` advance?), polled by a watchdog, and recovery is layered: (1) hls.js retries a segment itself; (2) `StreamEngine` re-attaches the pipeline on a fatal error OR a stall, with exponential backoff — a restreamer bouncing for 20s heals with the feed never leaving the wall; (3) after 4 failed re-attaches the engine **hands the surface back to the POL-86 `SurfaceProber`** (D78), which clears the element and re-proves the playlist URL with its forever-backoff, repainting a fresh engine when the source returns an hour later. A stream is INCLUDED in `probeTargets` (unlike `playlist`/`page`, which are excluded for having no single URL): it has exactly one — the playlist the pipeline fetches first — and it is the surface that most needs proving, because a cold-booted box whose network has not settled kills an MSE pipeline permanently in a way `<video>` never does. Every state is narrated in plain English on the glass (a `StreamBoard`: "No signal · restream.test · no video for 12s (the source stopped sending)") and into `player.diag`, because a dead camera that reads as a dead camera is a five-minute fix and a black tile is a site visit. A stream is refused where it cannot work: as a playlist step (rejected at the API — `live-stream-step` — not silently dropped) and as a page embed (an iframe cannot mount MSE). Verified end-to-end against a real ffmpeg HLS origin in a real Chromium: live → source killed → "Live stream interrupted" → "No signal" → source restarted → **healed with no human touching it**. | Accepted | Live video is the biggest content-kind hole for pro walls (a NOC wall runs 4–8 camera feeds), and it is the content kind where "the wall must never sit on a black rectangle" is hardest and matters most: a camera feed is watched precisely because something might be wrong. Two caveats worth carrying: hls.js fetches the playlist/segments by XHR, so unlike a plain `<video src>` the origin **must** send CORS headers (real restreamers do; the console's field hint says so) — and a CORS-blocked feed probes fine (`no-cors`) and then fails in the pipeline, landing on the "No signal" board with hls's own error text, which is exactly the diagnosability the trail exists for. Stream URLs are deliberately NOT credential-stamped (POL-24): a token on the playlist URL does not propagate to the segment URLs the playlist body names, so stamping would promise an auth that silently fails — an authenticated feed belongs behind a restreamer that handles it. | 
| D127 | 2026-07-14 | **POL-18: `web-window` surfaces — content that refuses to be framed becomes a TOP-LEVEL browser window placed by the agent over the player, instead of a dead iframe tile.** The contract: `WebSurface`/`DashboardSurface` gain `placement: "iframe" \| "window"` (default iframe — every stored slice and old server stays valid); a `placement: "window"` surface travels in the ScreenSlice like any other so span math, stable ids and the keyed DOM-diff path are untouched, but the PLAYER renders it as a plain dark hole and the SurfaceProber never probes it (one shared predicate, `isAgentPlacedWindow`, gates both — probing on behalf of a load the page never makes would be a lie). The AGENT is told over ITS channel: `server/apply` screen entries gain optional `windows[]` ({surfaceId, credential-stamped url, region in canvas px, canvas}) — the existing apply path, not a new channel — recomputed and re-pushed by the same code path that pushes renders (gated so machines that neither have nor had windows cost nothing), and the agent diffs by signature (id+url+geometry) so an unchanged window is never relaunched, a changed URL relaunches in place and a geometry-only change just repositions via swaymsg (no flash). Placement rides the EXISTING SupervisedBrowser machinery in the sway backend: a second Chrome (`--app=`, per-window `--user-data-dir`, NO `--kiosk`) or surf, floated + sized + moved with `floating enable / resize set / move absolute position` after the same pre-spawn window-event subscription the player launch uses. **Auto-fallback:** the server GETs each web/dashboard source and reads `X-Frame-Options`/CSP `frame-ancestors` (SAMEORIGIN counts as blocked — a wall is never the dashboard's origin; only a plain `*` allow-list counts as open; a failed probe is `unknown` and treated as OK, because a false "blocked" would windowize content that frames fine); the verdict persists on the ContentSource (`framing`, dropped on URL edits and re-probed, plus a POST re-probe route), and an `auto` source flips web → web-window live when blocked. Operator override `placementMode: auto\|iframe\|window` in the Content library, because header detection can never be perfect. **Capability-gated server-side:** only `wayland-sway` machines get windows — dev-open/x11-i3 (and video walls, where a top-level window can neither be sliced nor span) degrade to the iframe with a console-visible activity note, never a hole nobody fills. **Honest limits:** the player's badge/ident overlays CANNOT render above an OS-level window (page z-order loses to the compositor); zoom does not apply to a placed window; playlist entries and page embeds stay iframed (a blocked source there is still a dead tile — noted follow-up); and the swaymsg float/position path itself is hardware-pending (every server/agent seam is test-pinned, the compositor call is not). | Accepted | The whole point of POL-18 is the class of dashboards that ship `frame-ancestors 'self'` and cannot be reconfigured — on a kiosk the iframe shows NOTHING, which reads as a broken wall. Reusing the surface fabric (slice + stable ids) and the agent's existing browser supervision means the escape hatch inherits instant updates, credential stamping, crash-respawn and orphan reaping instead of forking any of it; putting the fallback decision (and the capability gate) on the SERVER keeps agents dumb and guarantees a mixed fleet degrades legibly instead of leaving holes. |
| D136 | 2026-07-15 | **POL-143 (D136; renumbered from D120 at v0.2.36 merge, then D135→D136 at merge — D135 was taken by POL-144): the chart makes mTLS actually REACHABLE on stock K3s, the server advertises the port to dial, and an unreachable door surfaces instead of a forever-promise.** POL-134 shipped mTLS on-by-default, but on the homelab K3s every box got its certificate and then sat at "Certificate issued — moves over on next connection" through multiple restarts. Verified against the live cluster (read-only): the `polyptic` Service is `ClusterIP` (its :8443 reachable only in-cluster), and Traefik exposes only `web`(:80) and `websecure`(:443, TLS-terminating) — neither can carry a raw mutual-TLS handshake — so a box dialling `wss://polyptic.homelab:8443/agent` (192.168.1.102:8443) hit a port nobody was listening on. The listener was up in the pod and correct; it was simply unpublished, and the agent's derived dial address pointed at a door that didn't exist. **Fix, three parts. (1) Reachability by default: a dedicated NodePort Service** (`templates/service-agent-mtls.yaml`, `agentMtls.expose: nodePort`, default nodePort 30843) publishes the raw-TCP listener on every node — reachable off-cluster on stock K3s with no new Traefik entrypoint, no IngressRouteTCP SNI collision with the console on :443, no extra DNS. `expose: none` is the escape hatch for operators who bring their own LoadBalancer or Traefik `IngressRouteTCP` passthrough and point agents at it with `publicUrl`. **(2) The server ADVERTISES the port to dial, so routing can't strand the fleet.** The mTLS bind port (pod :8443) and the advertised/dialled port (the NodePort) are now distinct: a new `AGENT_MTLS_ADVERTISE_PORT` (rendered by the chart to the nodePort) flows into the cert bundle's `port`, and the agent reuses the host it already knows (`polyptic.homelab`) on that advertised port — the box learns the reachable endpoint from the server rather than guessing the bind port. A full `AGENT_MTLS_PUBLIC_URL` still overrides both. **(3) An unreachable door is DIAGNOSABLE, once.** After repeated failed mTLS dials the agent, on its plain-channel hello, reports `mtlsDialFailure` (the exact URL it tried + the consecutive-failure count); the server records it live in `Presence` (never persisted — a stale address on a healed box would lie), narrates ONE `bad` feed line per edge (new report or changed URL, not one per fallback cycle), and surfaces it on `GET /settings/agent-security` as `mtlsDialError`. The Settings card then reads "Can't reach the secure port · tried <url>" (beating the "moves over on next connection" promise) and clears the instant the box connects over mTLS or goes offline. Pinned by unit tests (advertise-port env resolution incl. fatal-on-typo; Presence edge/clear semantics; protocol round-trips for both new optional fields; `deriveMtlsUrl` on the verbatim homelab scenario) + chart renders (NodePort Service + advertised port on the default; dropped by `expose: none` and `enabled: false`; overridden by `publicUrl`) + a POL-127-style two-version render pin proving the mTLS Service's port spec renders byte-identically across chart versions. | Accepted (provisional) | Zero-click cold boot to content + instant (non-negotiables #3/#4) mean the security migration cannot depend on an operator hand-wiring a TCP route the chart never mentioned — the shipped default must be reachable on the substrate we target (stock K3s Traefik). And a promise the UI can't keep ("moves over on next connection", forever) is worse than an honest failure: the box that can't reach the door must SAY so, with the address it tried, so the operator fixes routing instead of waiting. Advertising the reachable endpoint from the server (not deriving the bind port on the box) is what makes the topology the control plane's problem, not each box's guess. |
| D137 | 2026-07-15 | **POL-114 (D137; renumbered from D124→D132 at v0.2.36 merge, then D132→D137 re-landing on main — D132 was taken by POL-108, D133–D136 by sibling PRs): PDFs and slide decks are CONVERTED SERVER-SIDE into image decks; conversion is a JOB, not a request; and a server with no converter REFUSES the upload.** DESIGN.md always said a wall must never render a document live (an Office viewer on a kiosk is a scrollbar, a toolbar and a font substitution away from a broken screen), but the pipeline that makes that true was never built — so every all-hands deck became a screenshot or a flaky Office-online iframe. `POST /api/v1/media` now takes PDF/PPTX/PPT/ODP/DOCX/DOC/ODT, converts the file ONCE into page images on the media volume, and mints a **`deck`** content source. **A deck IS a playlist:** it resolves (in `ControlPlane.specFor`) to the POL-34 **playlist surface** with one `image` entry per page, timed by the deck's dwell — so the player, the offline media cache, video-wall spanning and the <150 ms diff path carry it with code that already exists, and **the player never learns a new surface type**. Pages are served at `GET /media/:id/page/:n` (ungated, immutable, CORS-open, exactly like every other media route), which is why decks work offline for free. **The seam:** core knows only a `DocumentConverter` (`available()` / `convert()`) — the sibling of POL-109's `MediaProber`, hung on the same ingest point in `media.ts` — and the one implementation that shells out to an office suite + a PDF rasterizer lives in `document-convert.ts` alone (`DOC_OFFICE_CMD` / `DOC_RASTER_CMD` / `DOC_CONVERT=off`; the image bakes the toolchain behind `--build-arg DOCUMENT_TOOLCHAIN=full|pdf|none`). **Two calls we defend.** (1) **Conversion is a JOB.** A 60-slide deck is tens of seconds of office + rasterizer — held open as an HTTP request it meets a reverse proxy's read timeout, and an operator watching a dead spinner cannot tell "working" from "hung". So the route answers **202 + a `DocumentJob`**, and the job's progress (pages landing, counted off the converter's real output) is pushed on the **`admin/state` broadcast the console already listens to** — no new socket, no polling, no job table. The "instant" non-negotiable is about content PROPAGATION, and it is untouched: the deck's pages reach a wall the same way an image does. (2) **No converter → REFUSE** (the opposite of D129's accept-and-warn, and deliberately so). Probing is an *inspection*: a file the prober cannot read is still a file the browser can probably paint, so silence is not evidence of guilt. Conversion is the **content**: with no converter there are no page images, and a deck with no pages is a library row that provably cannot paint — precisely the black-screen bug D129 exists to kill. So the server refuses at upload with a sentence that says what to do ("export to PDF and upload that"), advertises **`capabilities.documents: false`** in `admin/state`, and the console does not offer the affordance at all. A rasterizer-only host is a real, useful middle: PDFs convert, a PPTX is refused BY NAME. Deck pages + the one authored field (`dwellSeconds`) live in the **media catalogue**, derived onto the source at read time like `media` — no migration, and they travel with the media volume. A deck cannot nest in a playlist (it is already a rotation). Tests: a real committed 2-page PDF fixture, converted through the REAL adapter — against the host's toolchain when it has one, and otherwise a committed test double honouring the same command-line contract — plus an e2e that pins the 202/job/deck/page-serve/assign/re-time/refuse paths on a runner with NO toolchain at all. |
| D138 | 2026-07-16 | **POL-146: a POL-18 web-window must not be occluded by the fullscreen player, and its Chrome must launch with the SAME GPU flags as the player.** On real amdgpu (fpd-ago) a framing-blocked or forced-windowed page rendered BLACK. Two code causes: (1) sway hides floating windows behind a workspace-fullscreen view, and the player is placed `fullscreen enable` while the web-window is `floating` — so the placed window was rendered but occluded, and the player's `#000` stage showed through the transparent window-hole. Fix: the agent drops the underlying player's sway fullscreen while a web-window occupies that output (tracked per-connector via the player's con_id) and restores it when the last window leaves; a non-fullscreen player is still the sole tiled window so it fills the glass, and the floating window now composites above it. (2) The player's and web-window's Chrome arg builders were separate; the GPU/native-Wayland flags (`--ozone-platform=wayland`, per-instance `--user-data-dir`) were aligned but could silently drift. Fix: both build from a shared `chromeBaseArgs` so a window can never software-render while the player runs on the GPU. The server-side multi-box-wall fallback (window→spanning-iframe) was already correct (verified + regression-tested). Operator-facing display-backend jargon ("backend without window placement", "compositor DPMS — a development backend") removed from console/server copy. | Accepted | The occlusion is the concrete, code-derived root cause of the black screen (GPU flags were already aligned, so the POL-67-echo hypothesis did not hold in code); the fullscreen toggle is scoped to exactly the web-window feature and leaves the normal single-window player path untouched. End-to-end visibility on the glass is hardware-pending (needs an amdgpu box to confirm sway's floating-over-tiled compositing and that Chrome `--kiosk` does not re-assert Wayland fullscreen). |
| D139 | 2026-07-16 | **POL-147: on an ingress-only cluster, mTLS rides Traefik as a TLS-PASSTHROUGH `IngressRouteTCP` on :443, SNI-routed — the box dials `wss://mtls.<host>`, not a NodePort.** POL-143 (D136) published the raw-TCP mTLS listener on a NodePort (default 30843) and advertised that port to boxes. On the AMRC fpd-ago cluster that failed for real: the box dialled `wss://<bootHost>:30843` and reported "Can't reach the secure port" (0/2 on the secure channel) because the site firewalls off arbitrary NodePorts — **the box can reach only what Traefik routes, on 80/443**. Traefik cannot *terminate* mutual TLS (it would strip the client cert the listener's handshake needs), so the fix routes mTLS THROUGH Traefik untouched: a new chart mode `agentMtls.expose: ingressRouteTCP` renders a `traefik.io/v1alpha1 IngressRouteTCP` with `tls.passthrough: true` on the `websecure` (:443) entrypoint, matched by `HostSNI(mtls.<ingressRoute.host>)` (default-derived — one label under the same wildcard cert, or an explicit `ingressRouteTCP.host`). On :443 Traefik then SNI-splits: the console host terminates TLS normally via the existing HTTPS IngressRoute, while the `mtls.` SNI passes straight through to the pod's :8443 so the pod still performs the client-cert handshake. **No high port is opened.** The server side is env-driven and back-compatible: a new `AGENT_MTLS_ADVERTISE_HOST` makes the server advertise `wss://<sniHost>` (`buildAdvertiseUrl`, port omitted on the standard :443 so the box's ClientHello SNI is exactly the host Traefik matches) and, critically, ADDS that host to the server leaf's SANs — the box pins our CA but still verifies the hostname, so the passed-through handshake must validate against a leaf that names the SNI host. A full `AGENT_MTLS_PUBLIC_URL` still overrides everything; `nodePort` (default) and `none` are untouched. Pinned by: chart renders for all three `expose` modes incl. a POL-127-style two-version byte-identical render pin on the new passthrough route (SNI + passthrough must never churn on an upgrade); server env-resolution (`AGENT_MTLS_ADVERTISE_HOST` parsed, marks config explicit) + `buildAdvertiseUrl` (:443 omitted, non-standard appended) + a cert-SAN test proving the advertised SNI host lands in the leaf's SANs. | Accepted (provisional) | Non-negotiable #4 (zero-click cold boot) plus the AMRC reality ("everything must go through Traefik") mean the shipped mTLS path must be reachable on an ingress-only cluster with no hand-wired TCP port. Passthrough is the only way to carry mutual TLS through Traefik without stripping the client cert; SNI-splitting on :443 reuses the one open port and the existing wildcard cert. **Provisional**: the render + advertise + SAN paths are proven in code and tests, but a real box completing the mTLS migration through the passthrough route on fpd-ago (posture card flipping to "on the secure channel") has NOT yet been confirmed on hardware — that is the DoD's remaining real-cluster check. |
| D140 | 2026-07-16 | **POL-149: a routine credential-token refresh now RE-PUSHES the screens showing that profile's content, so a framed app (e.g. Grafana) re-auths before its session lapses — reversing POL-24's "routine refreshes push nothing".** In the field (AMRC fpd-ago) a Grafana dashboard on a POL-24 kiosk credential loaded authenticated, then after a while an unattended wall was bounced to Grafana's login screen with a flood of `401 (Unauthorized)` on BOTH boxes (so not clock skew); a manual reload re-authed. Cause: POL-24 stamps the short-lived JWT into the source URL (`?auth_token=<jwt>`) at SEND time only, and the `TokenService`'s background refresh at ~75% of token life deliberately did NOT re-push — the original reasoning was that rewriting a live iframe's URL every few minutes would reload it (violating "instant") and that the framed app's own session would carry the page. The field disproved the second premise: Grafana's session expires with the JWT, the slice is never re-sent, so nothing re-presents a fresh token → login screen + 401s until a human reloads. **Fix (server-only): `TokenService` gains an `onTokenRenewed` callback that fires on the routine-refresh success path (a fresh token replacing one that was still usable), wired in `index.ts` to the SAME re-push helper as the existing `onTokenUsable` edge — recompute exactly the screens whose source (directly or via a POL-34 playlist step) carries that `credentialProfileId` (`screenIdsUsingProfile`) and push each a freshly `decorateSliceForSend`-stamped render.** Because refresh runs at ~75% of lifetime, the new token always lands well before the old session dies. **The player needs NO change:** POL-86's proven-before-painted `SurfaceProber` (which landed AFTER POL-24, invalidating its first premise) swaps a re-stamped URL in place — the OLD authenticated content stays up until the NEW url proves reachable — so the re-stamp is a seamless in-place iframe navigation, not a shell reload and not a login-screen flash. Pinned by unit tests: `onTokenRenewed` fires on a renewal (and only the usable edge on first fetch); the re-push set is exactly the profile's screens (a second profile's screen is untouched); and a rotated token re-stamps the affected screen's URL with the new `auth_token` while the unaffected screen keeps its own. | Accepted (provisional) | Non-negotiable #4 (a wall stays on content unattended, zero human clicks) is exactly what breaks when a credentialed surface silently de-auths; re-pushing on the refresh the server already performs is the smallest change that keeps the wall signed in, and it reuses the existing edge-heal re-push path verbatim. **Provisional**: unit-proven, but staying signed in across a REAL Grafana token lifetime (the new token re-establishing the session with no visible flash on real amdgpu) needs the live fpd-ago stack to confirm. |
| D141 | 2026-07-16 | **POL-150: a placed web-window must be POSITIONED only AFTER the underlying player is dropped out of fullscreen — sway constrains a floating window's resize/move on a workspace that still holds a fullscreen container.** Follow-on to POL-146 (D138), which made the web-window visible and thereby exposed a geometry bug: on Screen 3 (a single screen) the page filled only ~the right 60% of the output, offset right, the player's black stage showing through the rest. Root cause (code-derived): the agent applies all `showScreen` (player) placements FIRST and only reconciles web-windows afterwards, so at player-placement time no window spec exists yet and the player is placed `fullscreen enable`. `positionWindow` then ran its `floating enable` + `resize set` + `move absolute position` while that fullscreen player still owned the workspace — and sway constrains geometry ops against a fullscreen container (the same reason `moveToOutput` must `fullscreen disable` before it can relocate a container), so the floating window landed offset and undersized. POL-146 dropped the player's fullscreen only AFTER positioning, so the window was revealed at the wrong geometry. Fix: `setPlayerFullscreen(false)` is now called BEFORE `positionWindow` in both the launch and the geometry-only reposition paths; the rect→swaymsg mapping was extracted into a pure, unit-pinned `windowPlacementCommand` that also re-parents the container to its target output (`move container to output`) before floating, so the float/resize always applies in the target output's coordinate context. The pixel math (`regionToOutputRect`) was already correct and is scale/origin-invariant for the full-canvas case — the bug was purely the fullscreen-workspace constraint, not the arithmetic. | Accepted | The ordering is the concrete, code-derived cause (the reconcile order guarantees the player is fullscreen when the window is positioned, and sway's fullscreen-container constraint is already documented in `moveToOutput`); the rect→geometry mapping (full output = edge-to-edge cover; sub-region = region bounds; multi-output = the output's own global origin) is now unit-proven end to end. Actual fill on the glass is hardware-pending (needs a real wayland-sway box to confirm the window covers the output edge to edge). |
| D142 | 2026-07-16 | **POL-148: fleet clock sync — a BAKED-IN NTP client in the live image + a BUNDLED chrony server reached over Traefik UDP, no internet.** A netboot box shipped no time client at all (`timedatectl` on a real box: `NTP service: n/a`, no systemd-timesyncd), so it free-ran off its RTC — one fpd-ago box sat an hour ahead and silently broke a Grafana dashboard (a relative `now-1m` range resolved from the box clock queried a FUTURE window → empty). The kiosk user is unprivileged (`set-time` → Access denied), so the fix had to be a system service, not a runbook step. **Three parts. (1) Client, baked into the image.** `build-live-image.sh` now installs `systemd-timesyncd` (a separate package on ubuntu-base minimal) and enables it under `sysinit.target.wants` alongside a new `polyptic-timesync-conf.service` (ordered `Before=systemd-timesyncd`, `DefaultDependencies=no`) whose pure-shell helper reads the NTP host off the boot cmdline and writes timesyncd's `/run` drop-in before the daemon starts. timesyncd runs with `CAP_SYS_TIME` — it sets the clock without the kiosk user, and the kernel's 11-minute mode carries the corrected time to the RTC. It syncs early at boot but NEVER blocks it (no `systemd-time-wait-sync`): a wall must render even if the NTP server is unreachable (non-negotiable #4). **(2) Server bakes the endpoint.** `provision.ts` adds `polyptic.ntp=<bootHost>` to the boot cmdline (the helper falls back to the `polyptic.server_url` host for older baked media); the box disciplines to the same host it netboots from, on UDP/123, with nothing per-box to configure. **(3) A bundled NTP server (chart, ON by default `ntp.enabled: true`).** A small `chronyd` Deployment + Service + ConfigMap configured (by default) as a LOCAL stratum reference — no internet upstream, it serves the node's own (cluster-disciplined) clock; `chronyd -x` means it only serves, never steps its own clock, so it needs no `CAP_SYS_TIME`, only `NET_BIND_SERVICE` for UDP/123. Any image with `chronyd` works (the chart owns the config + command). Exposed by default via a `traefik.io/v1alpha1 IngressRouteUDP` on a values-named UDP entrypoint (`ntp.ingressRouteUDP.entryPoint`, default `ntp`) — NTP is UDP/123 and on an ingress-only cluster the box can reach only what Traefik routes. Adding the `:123/udp` entrypoint to Traefik + the LoadBalancer stays a DOCUMENTED OPERATOR STEP: the chart deliberately does NOT manage the cluster's shared Traefik (a chart-rendered K3s `HelmChartConfig` was prototyped and rejected as too cluster-specific — it edits kube-system, triggers a Traefik redeploy, and is a K3s-only CRD), so the README gives the K3s + vanilla-Traefik snippets and the IngressRouteUDP is inert (never an error) until the entrypoint exists. **Escape hatches so on-by-default is safe on networks that already have NTP:** `ntp.clientHost` decouples the baked-in image CLIENT from the bundled SERVER — set it to a site's own NTP (stamped as `polyptic.ntp` via `POLYPTIC_NTP_HOST`) and set `ntp.enabled: false` for a clean "bring your own NTP" posture; `ntp.upstream` chains the bundled chrony to a real time source (drops `-x`, gains `CAP_SYS_TIME`, serves a real stratum with `local stratum` as orphan fallback) instead of serving a possibly-wrong node clock blind. NOTES.txt states loudly, on install, that nothing syncs until the Traefik entrypoint is added (and confirms the BYO posture when the server is off). **(4) Visibility.** The agent reports `vitals.clockSynced` (from `/run/systemd/timesync/synchronized`: present = synced, dir-but-no-file = running-not-synced, no dir = OMIT — an absent flag is never drawn as "not synced"); the server measures the skew at heartbeat receipt (`clockOffsetMs` = box `at` − our clock, live-only) and the Machines card shows a clock-health badge (`clock +1h02m · not synced`). Pinned by: protocol round-trips for the new optional vitals field; the agent sampler's three clock-sync states against a fixture; the server's `clockOffsetMs` sign/guard + Presence measure/forget + the online-only `admin/state` field; the console `clockBadge`/`formatClockOffset`; the timesync-conf shell helper (explicit `polyptic.ntp`, `server_url` fallback with scheme/port/path stripped, no-host writes nothing) wrapped in a bun test; and chart renders for `ntp` on/off/`none` incl. a POL-127-style two-version byte-identical render pin on the `IngressRouteUDP`. | Accepted (provisional) | A free-running clock silently corrupts any relative-range content and the kiosk user can't fix it, so clock sync must be a baked-in system service — and an air-gapped fleet needs a time source it can actually reach, which is the bundled server over the one open ingress (the POL-147 reachability lesson, one protocol down). Measuring the skew server-side (from the timestamp the box already sends) is the honest, zero-box-cost signal; the box only reports whether its own NTP client thinks it's synced. **Provisional**: the render + client-config + reporting paths are proven in code and tests, but an ACTUAL box syncing through the Traefik UDP route — and the operator having added the `:123/udp` entrypoint — has NOT been confirmed on hardware; that plus a rebuilt image (the client rides `build-live-image.sh`, so the running fleet lacks it) are the DoD's remaining real-cluster/real-box checks. |
| D143 | 2026-07-17 | **POL-151: the bundled chrony NTP server (POL-148/D142) CrashLooped — chronyd cannot use its default `/run/chrony`, so the conf relocates the pidfile + command socket into the mounted state volume and pins `user root`.** Deploying v0.2.39 to fpd-ago, the `polyptic-ntp` pod (`cturra/ntp`, chronyd 4.6.1) never went Ready: `Could not change ownership of /run/chrony : Operation not permitted` / `Could not access /run/chrony : No such file or directory` / `Disabled command socket /run/chrony/chronyd.sock` / **`Fatal error : Could not open /var/run/chrony/chronyd.pid : No such file or directory`**. Root cause (code-derived): the Deployment BYPASSES the image entrypoint (`command: ["chronyd"]`, so any chronyd image works — D142), but that entrypoint is what creates `/run/chrony`; and the pod runs with `capabilities.drop: [ALL]` + only `NET_BIND_SERVICE`, so even as uid 0 chronyd holds no CAP_CHOWN. It mounts `/etc/chrony` (config) and `/var/lib/chrony` (state) but NOT `/run/chrony`, and the default pidfile (`/run/chrony/chronyd.pid`) lives in a dir that does not exist and cannot be created → the fatal that kills the pod. **Fix (chart-only, no new volume or fsGroup):** keep chronyd out of `/run/chrony` entirely — in `ntp-configmap.yaml`, pin `user root` (chronyd does not drop privileges, so it never chowns a runtime dir — killing the two non-fatal chown/access warnings too) and relocate both runtime files onto the already-mounted, root-writable state volume: `pidfile /var/lib/chrony/chronyd.pid` and `bindcmdaddress /var/lib/chrony/chronyd.sock` (the loopback command port is kept; the server needs no external chronyc). The `driftfile`/`dumpdir` already lived there, so the state volume is the natural home and no `fsGroup` is needed (chronyd stays root, which owns the emptyDir). The stale `ntp-deployment.yaml` header ("chronyd starts as root … and drops to its own user itself") is corrected to describe the real capped-root/no-drop posture. Pinned by an added `ntp-chart.test.ts` render assertion: the pidfile + command socket resolve to `/var/lib/chrony`, `user root` is present, and NOTHING points a runtime file back at `/run/chrony` or `/var/run/chrony` (so this can't regress). | Accepted (provisional) | Bypassing the entrypoint to keep "any chronyd image works" (D142) is worth keeping, but it means the chart — not the image — must satisfy chronyd's runtime-file needs; relocating them onto the volume that already holds the drift/dump state is the smallest fix that needs no extra volume, no fsGroup and no CAP_CHOWN, and `user root` removes the last dependency on chowning a runtime dir the capped container can't chown. **Provisional**: the render is test-pinned, but a real `polyptic-ntp` pod reaching Ready is confirmed only on the next fpd-ago redeploy (the orchestrator's check). |
| D144 | 2026-07-17 | **POL-152: a placed web-window is now SELF-VERIFYING — after issuing the sway float/resize/move the agent reads the window's ACTUAL geometry back (`get_tree`) and re-issues until it matches the target rect.** Field (fpd-ago, dual-Dell): a single-screen web-window landed intermittently in only HALF the output (left or right half), "always renders on the right first, then flips to the left or goes full screen" — a race, not a geometry bug (D141/POL-150 fixed the geometry). Root cause (code-derived): a fresh Wayland window appears on a DEFAULT output at a DEFAULT (often half-width, tiled-alongside-the-player) size, and the compositor settles a beat AFTER our single fire-and-hope `windowPlacementCommand`, so the reposition raced and sometimes lost. **Fix (agent-only):** `positionWindow` loops up to `PLACE_VERIFY_ATTEMPTS` (5): issue the command, pause `PLACE_VERIFY_DELAY_MS` (120ms), read the con's rect out of `swaymsg -t get_tree` (`findConRect`), and accept only when `geometryMatches` within 2px — else re-issue. A persistent miss logs loudly but leaves the window where it last landed (content beats none). Pure pieces unit-pinned: `geometryMatches` rejects the half-width and wrong-output cases, `findConRect` walks tiled+floating nodes. | Accepted (provisional) | The read-back makes placement deterministic instead of timing-dependent — the smallest fix that removes the flake without guessing at compositor timing. **Provisional**: the race is timing-dependent, so first-apply-every-time is confirmed only on the real sway box. |
| D145 | 2026-07-17 | **POL-156: a single-BOX multi-output wall gets a REAL spanning web-window (one floating Chrome across the box's outputs); only a multi-BOX wall still degrades to the iframe.** Field: a framing-blocked page assigned across DP-1+HDMI-A-1 on ONE box rendered in a top strip, and assigning a window-wanting source to a >1-screen span fell back to the "unable to embed" iframe — because `computeWallSlices` downgraded EVERY wall's window→iframe (the old "a video wall never windows"). **Fix:** the server downgrades ONLY when the wall's members span >1 machine (`wallIsSingleBox`); a single-box wall keeps `placement:"window"`, so every member carries the SAME `wall:<id>` window. The agent's `diffWindows` collapses same-id windows across ITS box's connectors into ONE `WindowToPlace` covering every member connector (cross-box never aggregates — each agent only sees its own box, so this is correct by construction), and the sway backend floats a single Chrome across the UNION of those outputs (`spanningOutputRect`) — a floating window is not clipped to the output it was parented to. Multi-box stays iframe (each member CSS-slices the span); that path is unit-pinned unchanged. | Accepted (provisional) | Placing one window across a box's outputs is exactly what sway supports and what a single logical page on a wall wants; keeping the iframe fallback strictly for the genuinely-impossible cross-machine case preserves the only thing that renders there. **Provisional**: edge-to-edge fill on the real dual-Dell wall is hardware-observable. |
| D146 | 2026-07-17 | **POL-153: a web-window's zoom is applied by the AGENT via Chrome `--force-device-scale-factor`, not by the player.** The per-source zoom (POL-57/POL-133) scales an IFRAME surface — the player renders it — but a web-window (POL-18) is a separate top-level Chrome the player never touches, so player-side zoom never reached it. **Fix:** `WindowPlacement` gains a `zoom` (mirrored from the surface in `windowsForScreen`); `buildChromeWindowArgs` adds `--force-device-scale-factor=<zoom>` (a no-op 1 adds no flag, so an unzoomed window's argv is byte-identical). A zoom change is a LAUNCH change (the scale factor is a launch flag), so it re-places: it rides the window signature (`diffWindows` re-places on a zoom delta) and `showWindow` forces a relaunch when zoom changed with the same url. Reaches parity with the iframe zoom; live-without-reload (a DevTools `Emulation` call on the running window) is a possible follow-up but the launch-flag path is deterministic and unit-pinnable. | Accepted (provisional) | The device scale factor is the closest analogue to browser zoom a `--app` window honours from launch; relaunch-on-change is the same discipline a url change already uses (a brief flash, like the iframe never has, is the one gap noted). **Provisional**: confirmed live on the real sway box. |
| D147 | 2026-07-17 | **POL-154: ident over a web-window fullscreens the PLAYER over the window for the flash, then restores it (Alex's option 3).** An OS-level web-window floats ABOVE the player, so the player-drawn ident overlay (server→player) is occluded — page z-order loses to the compositor. **Fix:** `sendIdentPulse` (server) ALSO sends `server/ident{on,connector}` to the screen's AGENT whenever `windowsForScreen(screen)` is non-empty; the agent's sway backend fullscreens that connector's player over the window (`setPlayerFullscreen(true)` — sway hides floaters behind a workspace-fullscreen view, the same POL-146 mechanism), lets the player draw its ident, and on `off` restores deterministically to `!connectorHasWindow` (windowed if a window is still there, else fullscreen). Reconciles with POL-145 (the player still draws the friendly NAME; the agent only makes it visible). `ServerToAgentIdent` gains an optional `connector` — absent = the legacy machine-wide no-op, so deployed agents that ignore it stay correct; the ident-off at TTL rides the same path and restores automatically. | Accepted (provisional) | Reusing the fullscreen toggle POL-150 already added (no new always-on-top ident surface, no extra window) is the least-code way to put the flash on the glass while keeping the content restorable. **Provisional**: visible flash + restore is hardware-observable on the real sway box. |
| D148 | 2026-07-17 | **POL-155: sign the credentialed wall in ONCE, then never re-push on a routine token renewal — Grafana holds its own session; supersedes POL-149/D140's routine re-push.** POL-149/D140 kept a credentialed Grafana wall signed in by re-pushing the source on EVERY token renewal. In the field (AMRC fpd-ago, v0.2.40) it stopped the login-screen bounce but Grafana reloaded "every few minutes" with a visible flash — the player log showed each re-stamped URL triggering a full Grafana re-init (`PluginMeta: initializing app plugins cache …`). **Correct root cause: `?auth_token=<jwt>` is Grafana's `url_login` SIGN-IN mechanism, not a per-request bearer.** On first load Grafana validates the JWT once and mints its OWN session cookie (default ~30-day lifetime, silently self-rotating); after that the wall stays authenticated with zero help from us. POL-149's routine re-push re-stamped a NEW auth_token and re-pushed → the iframe URL changed → the player re-navigated the frame → Grafana rebooted → the flash. The "seamless in-place swap" D140 claimed never applied to a full re-navigation, and D140's premise ("the framed session expires with the JWT") was wrong. **Fix (server-only): DROP the routine re-push entirely (not throttle it).** `TokenService` loses `onTokenRenewed` and all the throttle machinery an earlier POL-155 attempt added (`renewRePushIntervalMs`, `DEFAULT_RENEW_REPUSH_INTERVAL_MS`, the `CREDENTIAL_REPUSH_MIN_INTERVAL_MS` env, `lastRePushAt`, clock injection, the `token.renew.throttled` log). Only the not-usable → usable EDGE (first fetch / recovery after an error or fixed secret) still re-pushes via `onTokenUsable` — that is the push that signs the frame in and heals a screen genuinely stuck on a login page. The token CACHE still refreshes every cycle (`getToken` always serves a valid token) so any future edge push carries a good token; only the wall-facing routine re-push is gone. Pinned by unit tests (`state.credential-profiles.test.ts`): a routine renewal (`testProfile` while already usable) does NOT re-fire `onTokenUsable`, the cache still warms to the fresh token, and the first-usable edge still fires exactly once. **Accepted caveat:** if Grafana's session genuinely lapses (30+ days, or Grafana restarts) we cannot yet DETECT it — surface-health proves fetchability but can't tell a login page from a dashboard. POL-149 papered over that with brute-force perpetual re-auth; the proper fix is login-page detection (follow-up ticket), so this theoretical gap is accepted for now. | Accepted (provisional) — superseded by D153 | The visible flash was the routine re-push re-navigating a frame that was already signed in by its own cookie — pure cost, no benefit, because auth_token is a sign-in, not a bearer the frame needs re-fed. Removing the routine re-push (rather than throttling it, which just moves the flash to every 30 min) is the honest fix: sign in once on the usable edge, let Grafana's own long-lived session carry the wall. The rare true-lapse case is better served by detecting a login page than by reloading forever. **Provisional**: unit-proven, but the DoD's real confirmation is the live fpd-ago Grafana wall staying signed in with NO flash across well over a token lifetime. |
| D149 | 2026-07-17 | **POL-157: a content SOURCE carries an opt-in REFRESH CADENCE (off / every-N / at-a-clock-time) and the PLAYER reloads that source's surfaces on it, riding POL-86's prove-then-swap — the honest, operator-visible replacement for POL-149/POL-155's covert re-push.** Some framed content does not self-update or eventually needs a hard reload (a Grafana dashboard whose own session lapses after ~30 days, a page that leaks over days, a dashboard that only re-queries on load). POL-155 (D148) removed the covert per-token-renewal re-push; this gives the operator a deliberate cadence instead. **Where — per source (not per surface/screen):** `ContentSource` gains an optional `RefreshPolicy` (a discriminated union in the new `packages/protocol/src/refresh.ts`), meaningful only on frameable kinds (web/dashboard; a superRefine rejects it on image/video/playlist/page). The DEAD `DashboardSurface.refreshSeconds` (defined, honoured by nothing, exposed nowhere — the exact stored-but-never-fired trap `schedule.ts` warns about) is REMOVED and subsumed, so there is one mechanism, not two. **Time math — reused, not reinvented:** the scheduled mode's firing is decided by `isRefreshDue` in refresh.ts, built on `schedule.ts`'s wall-clock primitives (`localInstant`/`minutesOfDay`/`shiftDate`/`weekdayOf`, one IANA zone), so DST is a non-event by the same discipline as the scene scheduler — a fall-back repeat re-reads the last-refresh as the same (date, minute) tuple and fires nothing; a spring-forward gap fires once a beat late (a point-fire that lands late beats one that vanishes for the year). One resolver answers "reload now?" identically for the player and any console preview. **Mechanism — player-honoured:** the policy rides the decorated slice to the player (`decorateSliceForSend`/`state.ts`); a `refresh-scheduler` in the player fires the due reload through the EXISTING prove-then-swap path (`surface-prober.ts` — the reloaded URL must PROVE before it replaces the visible content), so there is NO black flash and unrelated content never reloads. Player-driven means it survives brief WS blips and needs no server tick. A credentialed (POL-24) source re-stamps a fresh `auth_token` at send/prove time on each DELIBERATE reload — the correct place for that, unlike POL-149's unsolicited one. **Console:** a Refresh control on the source editor (Off / Every N minutes·hours·days / At HH:MM on weekdays) round-trips through `CreateContentSourceBody`/`UpdateContentSourceBody`. Pinned by unit tests across all four packages (the resolver’s interval spacing + scheduled/DST cases; the policy riding the slice; the player firing prove-then-swap not a blanking reload; the console draft round-trip). | Accepted (provisional) | Removing the covert auto-refresh (D148) left one honest gap — a genuinely-lapsed session we cannot detect (surface-health can’t tell a login page from a dashboard, POL-158). An operator-set cadence is the deterministic answer: it reloads by choice, zero cost when off, and puts the "does this need periodic reload" decision where it belongs. Reusing schedule.ts’s wall-clock resolver and POL-86’s prove-then-swap means no new time math and no new flash. **Provisional**: the resolver + wiring are unit-proven, but the no-flash reload on real amdgpu and a scheduled fire landing on the glass at the local minute need a live box + Grafana to confirm. |
| D150 | 2026-07-17 | **POL-161: reconcile a stale `lastBuildStatus: "running"` on `ImageUpdates.start()` — a server restart mid-build must not permanently grey the rebuild buttons (renumber if collision at merge).** `trigger()` persists `lastBuildStatus: "running"` before spawning the rebuild hook and only closes the row out in a completion continuation; a `helm upgrade` restarts the pod mid-build, so that continuation never runs and the row stays `"running"` forever. The console greys "Update image now" / "Full rebuild now" off the persisted status (`Settings.vue` `rebuilding`), and `bootstrapFirstImage` skips the first build when it sees `"running"` — so on fpd-ago after v0.2.41 the operator could not trigger the rebuild needed to ship the new agent, with no rebuild pod anywhere. The real in-process guard (`this.running`) reset to false on restart, so the states were inconsistent (the server WOULD accept a trigger; only the UI refused). **Fix:** a new `reconcileStaleBuild()` runs in `start()`'s async chain — `retain()` → **reconcile** → `bootstrapFirstImage()` — BEFORE bootstrap reads the status. A build cannot survive a process restart, so a `"running"` row while `this.running` is false is by definition orphaned: close it out to `failure` with a `lastBuildFinishedAt` and a loud `image.rebuild.orphaned` log line, via `store.setImageRollout`. A genuinely in-flight build (`this.running` true) owns its own row and is left untouched. Pinned by `image-updates.reconcile.test.ts`: a seeded `"running"` store with nothing in flight reconciles to `failure` on `start()`; a real in-flight run is left `"running"`; and reconcile-before-bootstrap lets a fresh install whose first build was interrupted still bootstrap. | Accepted | The bug wedged the exact recovery path an operator needs after every upgrade that restarts the server mid-build. Reconciling on boot (rather than only fixing the UI) makes the persisted state honest again and re-enables both the buttons and the first-image bootstrap; failing the orphaned row (rather than guessing it succeeded) is the safe reading — a build that didn't finish did not publish. |
| D151 | 2026-07-17 | **POL-160: runtime agent SELF-UPDATE — the server offers a newer agent binary on the hello, the box pulls `/dist/agent/<arch>`, verifies it, atomically swaps its own binary and re-execs, no rebuild and no reboot (renumber if collision at merge).** The agent is baked into the netboot squashfs, so an agent-code fix reached the fleet only on a FULL image rebuild + reboot — and a plain `helm upgrade` rebuilds the boot medium, not the image, so nothing reached the fleet and nothing said so (v0.2.41 shipped four web-window fixes; every box kept running 0.2.40, silently defeating a whole test cycle). **Mechanism:** the server already knows each box's reported `agentVersion` and its OWN bundled version (`BUILD_VERSION` == the binary it serves). A new `AgentUpdateService.offerFor(reportedVersion, hardwareArch)` returns an offer (version + `/dist/agent/<arch>` URL + size + a cached sha256) iff the served version is strictly NEWER (`isNewerAgentVersion`, a shared pure guard in `protocol`) and that arch's binary exists on disk; `ws.ts` sends the new `server/update-available` frame on the ADMIT path (authenticated channel), fire-and-forget. **Agent side (`update.ts`):** on the frame it re-checks the version guard, that it is an updatable single-file binary (baked `POLYPTIC_BUILD_VERSION` — a dev/source run is skipped), and that it hasn't already attempted this target this process; then downloads to `<bin>.new`, verifies size + sha256 + a `--version` SELF-CHECK (the new binary must actually run and report the target version — a wrong-arch/truncated binary never becomes live), backs the old binary up to `<bin>.bak`, atomically renames, writes a crash-loop marker, and exits cleanly for systemd `Restart=always` to relaunch the new binary. **Crash-loop guard:** at startup `reconcileSelfUpdateAtStartup` reads the marker, and `decideStartupAction` (pure) either commits the update after it stays up `STABLE_UPTIME_MS` (dropping `.bak`) or, after `MAX_UNSTABLE_BOOTS` boots that never stabilised, rolls back to `.bak`. The box self-reports the whole sequence over `agent/update-status` (feed lines on `restarting`/`failed`), the POL-77 wifi-debug pattern. Unit-proven across all three packages (version guard; server offer matrix; plan/apply/verify/marker decisions). | Accepted (provisional) | This is the mechanism that makes "the control plane keeps the agent current" literally true, and matches the instant/zero-click non-negotiables far better than rebuild-and-reboot (the ticket's option 2). **Provisional / NEEDS-A-REAL-BOX:** the re-exec-and-reconnect and the crash-loop rollback are unit-proven in their pure/IO-injected parts but not yet exercised on a real box; and the "content undisturbed" property is NOT yet true — the browser supervision model reaps + relaunches each output's browser on the first apply after ANY agent restart, so the wall briefly reloads (repainting from the player's cache). Making a self-update truly seamless needs browser adoption across restarts, a separate follow-up. Kernel/OS changes stay on the rebuild+reboot path; this carries only the agent binary. |
| D152 | 2026-07-17 | *(renumber if collision at merge — main was at D149; PRs #139/D150/D151 may merge ahead)* **POL-162: a FULL-region single-output web-window is now FULLSCREENED like the player (sway's own primitive), not floated with hand-computed geometry — floating kept ONLY for a sub-region and a multi-output span; supersedes POL-152/D144's self-verify for the full-screen case and reconciles POL-154/D147's ident surface toggle into ONE rule.** We place the player's Chrome edge-to-edge on every box because we FULLSCREEN it (sway is told "cover this output" and does it, pixel-perfect); the web-window was instead FLOATED — the agent computed an explicit x/y/w/h rect (`windowTargetRect`/`regionToOutputRect`/`windowPlacementCommand`) and told sway to place it there, so every web-window offset bug (POL-150 offset-right, POL-152 half-width race, and on v0.2.41 fpd-ago the BBC page landing offset-right with black down the left) lived in OUR geometry math, not sway; POL-152's self-verify only made it converge on a wrong self-computed rect. **Fix (agent-only, `packages/agent`):** the placement path is chosen from the spec — `windowFillsSingleOutput(connectors, win)` = one connector AND a full-canvas region (`isFullCanvasRegion`). **Full single output → FULLSCREEN:** `windowFullscreenCommand(conId, output)` issues `[con_id] move container to output <name>, fullscreen enable` and NOTHING else — no `resize set`, no `move absolute position`, no target rect, no self-verify loop; sway guarantees edge-to-edge, and enabling fullscreen on the web-window takes the workspace's fullscreen from the player automatically (only one container is fullscreen per workspace). **Sub-region OR single-box multi-output span (POL-156, fullscreen can't cross outputs) → the FLOATING + geometry + self-verify path is kept verbatim** (`positionWindow`/`readWindowRect`/`geometryMatches`). Both launch and the geometry-only reposition funnel through one `placeWindowSurface(id, conId)`, so a region change that flips full-region ↔ sub-region switches paths cleanly (no relaunch — a fullscreen toggle is a swaymsg op, not a browser respawn; POL-153 zoom via `--force-device-scale-factor` is untouched). **The reconcile (POL-154):** a pure `visibleSurfaceFor(connector, windows)` is the ONE rule for which of {player, web-window} owns a connector's glass — `window-fullscreen` (a full-region web-window IS the surface), `player-windowed` (a floating window composites above), or `player-fullscreen` (nothing floated, player reclaims the glass). `restoreVisibleSurface` maps it to the matching toggle and is used by BOTH ident-off and window-teardown, so ident (temporarily player), teardown (permanently player) and showing a web-window (the web-window) never leave two fullscreen mechanisms fighting — the pre-fix ident-off `setPlayerFullscreen(false)` would have left a full-region web-window un-fullscreened (black). Multi-output fullscreen is per-workspace, so fullscreening a web-window on output A leaves output B's player untouched. Pinned by unit tests (`windows.test.ts`): the path selection (full single output → fullscreen; sub-region → floating; span → floating), the fullscreen command shape (move + fullscreen, asserts NO resize/move-absolute/floating), and the surface state machine (all three kinds, incl. cross-connector isolation and full-over-floating precedence). | Accepted (provisional) | The web-window should stop re-deriving what sway already does flawlessly for the player: fullscreen is the primitive that fills an output edge-to-edge with zero math, so the path that kept breaking now uses it, and the fragile hand-rolled geometry survives only where it is genuinely required (a sub-region, and a span that fullscreen cannot cross). Folding ident/teardown/placement into one `visibleSurfaceFor` rule removes the competing-fullscreen hazard POL-154 introduced. **Provisional / NEEDS-A-REAL-BOX**: edge-to-edge fill on first apply on the fpd-ago Dell wall, and the ident-flash + teardown restore of a fullscreen web-window, are hardware-observable — the path selection, command shape and surface machine are unit-proven. |
| D153 | 2026-07-17 | **POL-155 reversed: Grafana's `url_login` is STATELESS — there is no session cookie to hold — so the routine renewal re-push (POL-149/D140) is RESTORED, and the reload cadence is fixed at the IdP by a long token lifespan, not by suppressing the re-push.** D148 dropped the routine re-push on the belief that `?auth_token=<jwt>` signs Grafana in once and mints a ~30-day session cookie. A HAR capture on the live fpd-ago wall disproved that premise outright: across the whole capture there is ZERO `Set-Cookie` in any response and ZERO `Cookie` header in any request — the Grafana frontend re-presents the URL JWT on EVERY API request. With a 300s token (iat→exp = 300s) every request 200s until `exp`, then everything 401s at once at exp+~60s leeway, and the next document load 302s to /login. No cookie, no session, no grace — the wall dies exactly one token life after its last stamped load, which is why removing the re-push reintroduced the login-screen bounce D140 had fixed. **So D140's mechanism was right all along**; it was only painful because the token lived 300 seconds, making the ~75%-of-lifetime re-push (and Grafana's visible re-init) fire every ~4 minutes. **Fix, two halves. (1) This repo: restore the renewal re-push** — `TokenService` regains `onTokenRenewed` on the routine-refresh success path (a fresh token replacing a still-usable one; the `onTokenUsable` not-usable→usable edge is unchanged), wired to the same `rePushForProfile(profileId, "token-renewed")` helper, re-stamping exactly the screens whose source carries that profile. No throttle machinery — the cadence lever is the lifespan, not a timer. POL-86's prove-then-swap player makes each re-push an in-place navigation (old content stays up until the new URL proves). Test-pinned: a renewal fires the re-push for exactly the renewing profile's screens (another profile's screen keeps its own token), the first-usable edge still fires exactly once, and the re-stamped URL carries the new token. **(2) At the IdP (outside this repo): the token lifespan moves to 30 days** — Keycloak per-client `access.token.lifespan` via an ACS chart passthrough (`accessTokenLifespan`). Net behaviour: one soft in-place reload per ~22 days (75% of 30d), never a login screen. Until the IdP change lands, a 300s token means the old every-~4-min reload — accepted, because it is correct (the wall stays alive) and the cadence fix is a config knob elsewhere. (Renumber if collision at merge.) | Accepted | The HAR is dispositive: no cookie ever crosses the wire in either direction, so "Grafana holds its own session" (D148) was false and every argument built on it falls. A stateless frame MUST be re-fed a token before exp — the renewal re-push is the only mechanism that does — and the only real defect of D140 was cadence, which is governed by the token lifespan, a per-client IdP setting. Fix the lifespan where it lives; keep the mechanism that keeps the wall alive. |
| D154 | 2026-07-18 | **POL-163 (POL-119's deferred real-hardware AirPlay pass): the banded/sheared AirPlay picture on real hardware is SOFTWARE H.264 decode (decodebin RANK), not a missing VA driver — fixed by promoting the GStreamer VA decoders on the UxPlay child via `GST_PLUGIN_FEATURE_RANK`, an ENV fix, not a package or argv one (renumber if collision at merge).** D135 diagnosed the torn/banded mirror as a MISSING VA DRIVER and shipped `va-driver-all`. POL-163's real-hardware pass on the homelab Intel (Lenovo) box disproved that: `vainfo` shows the iHD driver with HARDWARE H.264 decode (`VAProfileH264High : VAEntrypointVLD`), the render node is present and `kiosk` is in `render`+`video`, AND `libgstva.so` is on disk in the GStreamer plugin dir — so BOTH the VA driver and the GStreamer `va` decoder (`vah264dec`) are present and working. The banding is therefore not a missing decoder but a RANK one: the `va` plugin registers its decoders at a rank ≤ the software `avdec_h264`, so UxPlay's `decodebin` auto-plugs software H.264, whose CPU-bound frames reach `waylandsink` via its SHM stride path (the shear). **Fix (agent-only):** spawn the UxPlay child with `GST_PLUGIN_FEATURE_RANK=vah264dec:PRIMARY+1,vah265dec:PRIMARY+1,vavp9dec:PRIMARY+1` (a pure `uxplayChildEnv` helper in `backends/cast.ts`, merged OVER the agent's own env, scoped to that child), so decodebin prefers the hardware VA elements → dmabuf → waylandsink honours the stride → no banding. `-vs waylandsink` and the rest of the argv are unchanged. Two supporting changes: the cast child's stdout/stderr is relayed to the agent journal at a visible level WHEN `GST_DEBUG` is set on the agent (it rides `uxplayChildEnv` through to the child), so a future decoder-pick trace is inspectable without a box shell — the exact gap that cost a POL-163 diagnostic round; and `gstreamer1.0-tools` (gst-inspect/gst-launch) is added to the apt `cast` set for on-box debuggability (whose absence was the false-negative "vah264dec MISSING" reading), apt-only, the lean Fedora/Arch sets untouched. Pinned by `cast.test.ts` (the env promotes all three VA decoders, merges over the base without mutating it, and the argv is unchanged). | Accepted (provisional) | The real box proved the driver was never missing, so D135's package story was the wrong layer; rank is the documented GStreamer `va`-plugin gotcha and the env promotion is the standard remedy, cheaper and more correct than any driver or argv change. **Provisional / UNVERIFIED ON HARDWARE:** the rank bump, log relay, and tool add are unit-proven and typecheck/build clean, but a clean full-frame AirPlay mirror must still be confirmed on the real box. Residual risk: if the VA decoder is already PRIMARY yet fails at runtime (caps/alloc) and falls back, the rank bump won't help — in which case the added `GST_DEBUG` journal relay is what lets us see the pipeline's decoder pick next, without a box shell. |
| D155 | 2026-07-21 | **POL-169: the ticker's scroll is QUANTIZED to a fixed compositor frame rate (`TICKER_FPS = 30`) via a stepped timing function — `animation-timing-function: steps(duration × 30, end)` on the same `pel-tickmove` transform loop (renumber if collision at merge).** On the hardware-test box the ticker stuttered while one Chrome process (the high-SHR/~50 GB-VIRT profile of the GPU/compositor process, not a renderer) sat pinned at 100% CPU from boot — the ticker is the ONE thing on a wall that demands a frame every vsync (D66's sanctioned exception; everything else deliberately repaints at ≤2 Hz via `steps(1)` blinks), so on a box that composites in software every vsync is paid in CPU and the strip still can't hold rate. With `steps()` the animation stays compositor-driven — verified in real Chrome: the strip keeps scrolling with the renderer main thread hard-blocked, and produces exactly ~30 distinct transform values/s vs vsync-rate for linear — but between steps the output is constant, so there is no damage and no produced frame. At speed 60 (30 s cycle) a step advances the strip ~2 px per tick at 1080p: marquee cadence, not visible chop. Player-side, a ONE-SHOT `player.diag` probe (rAF-sampled for 2 s, 5 s after a ticker-bearing page lands) reports the achieved fps + viewport, so the box itself now names its frame budget in the field trail. | Accepted (provisional) | Halving/quartering frame production is the largest lever that needs no knowledge of WHY this box's compositing is expensive (GPU-accel state unverifiable remotely: box and cluster unreachable from the dev machine at fix time). **Provisional / UNVERIFIED ON HARDWARE:** compositor-drivenness and the 30 fps frame production are Chrome-verified on the dev machine; the CPU drop on the pegged box is not yet re-measured — the new diag fps line is what confirms or refutes it there. If the box still pegs with the ticker OFF, the cause is elsewhere and the diag line will show a healthy rate. |
