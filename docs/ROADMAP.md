# Polyptic — Development Roadmap

The remembered path. Fixed direction, flexible detail. Update the **CURRENT** marker as we go. Phases are sequenced by dependency, not by calendar.

> **CURRENT: feature-complete through Phase 8 (skipping 6/OIDC). Pending = the UTM/OrbStack VM passes + the prod `vite build`/Docker (esbuild is disabled in the build sandbox).** Phase 3 ✅ (3a–3f). 3c content library (ContentSource) · 3d scenes (snapshot/apply/scenes-left) · 3e Machines view + cold-start wizard (SolidJS admin retired) · 3f real local auth (D29/D31, argon2id/Bun.password, signed httpOnly cookies, dual-key rate-limit, /api+/admin-WS gated, AUTH_ENABLED default-on) + Settings + enrollment-token. **Phase 5 ✅** live preview thumbnails + /healthz + Prometheus /metrics + Helm chart (D32). **Phase 4 ✅** on-device stack (real sway/x11, zero-touch depot install, D41). **Phase 7 ✅** media upload→disk volume + ungated Range serve (D33); **POL-114 (D137)** the document pipeline — PDF/PPTX convert server-side to image DECKS (a `deck` source resolves to a playlist of page images; conversion is a job with live progress; no converter → refuse + `capabilities.documents: false`); **Phase 8 ✅** single-image SPAs + GitHub Actions CI/release/Pages + distribution docs (D34). **POL-9 fixed (D36):** no phantom `HDMI-1` before a compositor, a real backend that can't discover any outputs (and has no explicit override) advertises **zero** outputs, and the server prunes UNUSED connector-stragglers on re-advertise. **POL-14 (D38):** machine + screen **removal**, DELETE machine/screen endpoints that FORGET (not just reject/unplace), console "Remove" affordances. **POL-27 done (D39):** a player with no surfaces now shows a calm "No Content Assigned" idle splash (dark, branded, screen id + build version) instead of a black screen. **POL-7 (D40):** boot splash, a generated, live Plymouth theme (branded SVG logo + baked version/host + live status), `quiet splash` cmdline, retain-splash hand-off (boot + shutdown). **POL-30 ✅ (D42):** the `--kiosk` installer **auto-reboots** when it finishes (short `Ctrl-C`-able countdown; `--no-reboot`/`POLYPTIC_NO_REBOOT` opt-out; agent-only never reboots) so the one-liner is truly zero-touch, no manual `sudo reboot`. Verified: full typecheck green, **217/217 tests across 21 files** (incl. the POL-33 netboot suites + the netboot auth-gate regression guard). **VM passes underway** (Ubuntu 26.04/arm64 UTM, `wall1`/`wall2`): POL-5, POL-14, POL-27, and **POL-7 confirmed on-device** (`wall1`). **POL-7 (D43+D45): fixed + VM-confirmed**, 26.04 (dracut + plymouth 24.004) needed three fixes: theme selection via `plymouthd.conf` (no `plymouth-set-default-theme` on 26.04), dracut-first initramfs embedding via an `install_items` drop-in, and, the real crash, installing the **`plymouth-label`** text renderer (our `script` theme's `Image.Text` had no label plugin → NULL console viewer → segfault every boot). Diagnosed live over SSH with `plymouthd --debug`. Live status line + progress bar preserved. **POL-33 netboot (D46 + D47):** a bare box network-boots a live Polyptic image into RAM (no OS install) with **Secure Boot ON**, owned **by key** not by a DHCP race, the control plane is also the **boot depot**. First stage = Ubuntu's **signed shim + network GRUB** (D47, superseding D46's unsigned self-compiled loader; we sign nothing, the loaders are pinned noble debs): `provision.ts` gains `GET /boot/grub.cfg` [per-request GRUB menu, base + gated token baked from the live `Enrollment`; aliased at `/grub/grub.cfg` + per-arch paths for UEFI HTTP Boot], `/dist/image/:arch/{vmlinuz,initrd,polyptic.iso}` [Range-streamed; casper netboots via `iso-url=`, D46's `fetch=` never existed in casper], `/dist/boot/:file` [the tokenless **universal** `polyptic-boot.img` dongle + the four signed `.efi` loaders]; `GET /api/v1/settings/netboot` + a **Console ▸ Settings ▸ Netboot** card; the whole diskless fix is a **stable `POLYPTIC_MACHINE_ID`** (DMI/MAC) + cmdline parse in `deploy/live/` reusing the agent's existing override + `enroll.ts` case-4 (zero server/protocol change). Server + console + identity layer done & tested (`bun test` netboot suites + `deploy/live/test/identity.test.sh`); `deploy/build-boot-medium.sh` builds the dongle on **macOS or Linux** (downloads + mtools, no compile); `deploy/build-live-image.sh` stays **Linux-build-host-only** (can't build/verify on macOS), the live-image build + real-hardware netboot (dongle/offload, **Secure Boot ON**) is the pending on-device pass. **POL-38 done (D48): the live image is VM-verified end to end** — the self-contained test ISO (new `deploy/build-vm-test-iso.sh`, macOS-buildable, self-verifying El Torito/ESP) auto-boots in UTM (arm64/HVF/virtio-gpu-gl), sway comes up on hardware GL, the agent enrols GATED as pending, is approved in Console ▸ Machines, and the player renders live content on the VM display. Four live-image bugs found by that watched boot are fixed in-repo (netplan DHCP + wait-online --any in the overlay; real `agent.toml` from tokenless `setup`; xwayland installed with surf; `DISPLAY` imported into the user env) — **the squashfs must be REBUILT on the Linux host (`build-live-image.sh`) to pick them up before POL-39.** **POL-38 closed out fully (D48+D49):** the fresh image (agent + squashfs rebuilt via a privileged Docker container, macOS-hosted) cold-boots the downloadable ISO to the branded Plymouth splash (live status line) and straight into wall content, zero hands; Console ▸ Settings ▸ Netboot now offers BOTH downloads (netboot dongle + per-arch VM test ISO). **POL-39 done (D50): the full netboot chain runs in the UTM VM, all three flows** — PXE-served loaders (~60s power-on→content), USB dongle (~30s), offload-to-disk (~70s, dongle removed) — after fixing the portless-Host `computeBaseUrl` bug and shipping `efibootmgr` in the image (offload was a half-install without it). UTM's EDK2 has no HTTP-Boot driver (PXE is the VM path; documented). **POL-41 (D51): automated image updates** — scheduled server-side refresh (Console ▸ Settings ▸ Image updates, default 01:00, `IMAGE_REBUILD_CMD` hook + `deploy/refresh-live-image.sh`/`rebuild-image-docker.sh`), per-build `/etc/polyptic/image-id`, ungated `/dist/image/<arch>/manifest.json`, and a 5-minute box-side poll that reboots stale netbooted boxes (urgent → minutes, else the 03:00–05:00 window). **POL-43 (D52): Kubernetes** — the Helm chart catches up to the netboot line (depot on a PVC, rebuild hooks = privileged k8s Jobs created by `deploy/k8s-run-job.ts`, RBAC-minimal, scripts version-locked inside the server image) and a **weekly FULL-rebuild cycle** (default Sun 02:00, `IMAGE_FULL_REBUILD_CMD`) closes the kernel-CVE gap the nightly refresh can't touch; verified live on OrbStack k8s. **POL-24 (D53): content auth — credential profiles** (OAuth client-credentials at the operator's IdP; the server keeps tokens fresh and stamps `?auth_token=` into web/dashboard URLs at send time; Grafana `[auth.jwt]`/`url_login` consumes them), `ContentSource.credentialProfileId`, “Access credentials” console section + per-source Authentication picker; verified end-to-end against a stub IdP (REST + agent/player WS + console UI). IdP side: ACS PR #668 (serviceAccountsEnabled) + #670 (Grafana 13); fpd-ago is the live testbed. **POL-45 (D54): Settings rebuilt + image BUILD HISTORY with operator-chosen active image.** The depot now keeps `imageUpdates.retainBuilds` (default 3) builds per arch under `<arch>/builds/<imageId>/`, with the arch root hardlinked at the ACTIVE one — so the boot chain is unchanged and **activating an older build is the fleet rollback** (boxes re-pull on the existing 5-minute poll). `POST /api/v1/settings/image/activate` + ungated `GET /dist/image/:arch/builds/:imageId/:file`; depot PVC default 15Gi→30Gi. Console: Netboot → **Onboard Screens** (bootloader download + "Make a bootable USB" modal + Recent builds with Activate/download + a ⋯ menu for Build update / Full rebuild / Deploy-latest-now), Image updates → **Update schedule**, and HTTP Boot / DHCP option-67 behind a "Boot without a USB stick" disclosure. **POL-35 (D55): the netboot root image shrinks 1.41 GiB → 491.8 MiB (−66%) and the netboot RAM floor halves (~4 GB → ~2 GB).** `deploy/build-live-image.sh` now builds the rootfs UP from `ubuntu-base` (no `BASE_ISO`, no `xorriso`) and ships a BARE `rootfs.squashfs` booted by dracut (`root=live:<url>`) instead of a casper ISO wrapper — the initrd is built by `dracut --no-hostonly` inside the chroot, so kernel/`/lib/modules`/initrd come from one apt transaction and the `apt-mark hold` netboot footgun is structurally gone. The D47 signed chain is byte-for-byte unchanged (apt's `linux-image-<abi>-generic` is the same Canonical-signed PE; the `sbverify` guard passes). Firmware is curated (`linux-firmware-minimal` + GPU/NIC allowlist, `FULL_FIRMWARE=1` escape hatch) because `linux-image-generic` **Depends** on the full ~600 MB set. Depot artifact `polyptic.iso` → `rootfs.squashfs` across provision/image-updates/helm/tests; the POL-46 tmpfs fix becomes a dracut module. Verified: the real arm64 artifacts boot under QEMU from a bare HTTP squashfs (livenet → dmsquash-live overlayfs → systemd → `polyptic-agent-env`); 256/256 tests green. **POL-55 (D59): reboot ONE machine from the console** — `POST /api/v1/machines/:id/reboot` → `server/reboot` on that box's live agent socket → `agent/reboot-ack`; a Reboot button on Console ▸ Machines (online machines only), 409 when offline/not-approved. The unprivileged agent escalates by creating `/run/polyptic/requests/reboot`, which a root-owned `polyptic-reboot.path`/`.service` pair written by `setup` turns into `systemctl --no-block reboot` — no sudo, no polkit, nothing in the image to add. `dev-open` and non-Linux hosts REFUSE (a stray reboot must never hit a developer's laptop) and the reason lands in the activity feed.**POL-58 (D60): the first real-hardware bootloader install — a box with Ubuntu on it "claimed to offload" and then booted straight back into the old Ubuntu.** `offload.sh` now VERIFIES rather than assumes (re-reads the UEFI variables and asserts `Polyptic Netboot` leads `BootOrder` before anything is stamped or claimed; the unit no longer maps exit 1 → success), CHOOSES rather than guesses its target ESP (removable media excluded, multi-ESP boxes scored by `PARTUUID` against the firmware's own boot entries, a tie aborts and names `polyptic.offload_disk=`), and REPORTS rather than journals (boot splash + `POST /boot/report` → one Live Activity line; `BootReportBody`/`BootReportCode` in `protocol`). The GRUB menu gains a confirmation submenu defaulting to "No" that says in its titles that nothing is erased, echoed in the console's USB modal. New `deploy/live/test/offload.test.sh` drives the whole decision tree against stubs on any host — the VM pass could never have caught this, because a blank ESP on a disk with no OS exercises none of it. (The confirmation submenu D60 shipped was reverted the same day by **D61**: GRUB gives a submenu its own env context, so `$net`/`$arch` came out empty inside it. Menu is flat; entries are `Polyptic (Live)` / `(Offload Bootloader)` / `(Debug Console)`.) **POL-57 (D62): page zoom, remembered per (screen-or-wall, page) pair.** Framed surfaces (web/dashboard) carry a `zoom` scale factor; `PUT /screens/:id/zoom` + `PUT /walls/:id/zoom` patch the existing surface and re-push, so the player rescales the live iframe with no reload (D5). The player zooms the way a browser does — the frame is laid out at `1/zoom` of its box then scaled up, so the page re-lays-out rather than being magnified — and it composes with the 3b span, leaving the span math untouched. A new `zoom_preferences` table `(target_id, source_key)` restores the operator's zoom whenever that page returns to that target (across scene switches and restarts), keyed on the library source's **id** so re-pointing its url keeps each screen's setting. Console gets a −/value/+ control in the Inspector, on screens and combined surfaces alike. Verified live against a real page (viewport read-out + media-query reflow prove it reaches the page's layout engine); 319/319 tests green. **POL-50 (D63): one kiosk browser, and a wall you can debug.** `surf` is now the ONLY kiosk browser — `backends/{chromium,cog,browser}.ts`, `--browser`, `--chromium-deb/--chromium-ppa`, `POLYPTIC_BROWSER` and the `agent.toml browser` key are gone (on Ubuntu 26.04 `chromium` has no apt candidate, `chromium-browser` IS the snap, and `cog` was dropped after 25.04, so the seam had exactly one installable implementation). Because WebKitGTK exposes **no** browser-openable remote inspector (measured: `WEBKIT_INSPECTOR_SERVER` answers neither HTTP nor a WS upgrade; `inspector://` needs a WebKitGTK client and surf can't load it), the ticket's remote-DevTools tier is impossible on this browser and the inspector is popped **on the panel**: `POST /api/v1/screens/:id/inspect {on}` → `server/inspect` → the agent relaunches that output's surf with `-N`, focuses it, sends Ctrl+Shift+O and reloads (WebKit's inspector does not backfill). `agent/inspect-ack` is the sole writer of `ScreenView.inspecting`, so the console never badges an inspector that isn't on the glass. Console ▸ Machines gains a per-screen **Inspect** toggle (its state comes from the box's ack, never the click). 341/341 tests green. **POL-47 (D65): GRUB paints the Polyptic splash.** `Polyptic: streaming the amd64 live image into RAM ...` is gone: the message could never move *into* the splashscreen (Plymouth lives in the initramfs GRUB is still fetching), so GRUB paints its own — the signed grubnet's memdisk already carries `fonts/unicode.pf2` + the `gfxterm`/`gfxmenu`/`png` modules on both arches, so a themed, branded menu costs one theme string (`GET /boot/theme.txt`), one 7.5 KB `GET /boot/logo.png`, and no change to the signed chain. All of it is guarded behind `if loadfont …` (no font → plain console → boots identically), `gfxpayload=keep` hands the firmware's mode to the kernel (the splash's own resolution is D64's job), and every wall-facing string is now plain English with the diagnostics moved to the failure path. VM-verified end to end (real shim+grubnet under OVMF, before/after screenshots); 366/366 tests. **POL-63 (D67): Wi-Fi at every point in the boot stack.** GRUB/UEFI can never join WPA (industry-wide), so the universal `polyptic-boot.img` gains a per-arch LOCAL payload — kernel + new fat `initrd-wifi` (wpa_supplicant + all-major-vendor wlan drivers/firmware; a second dracut artifact so the lean wired `initrd` stays small for GRUB's slow HTTP fetch) — booted only when the wired chain fails; credentials are an editable `polyptic/wifi.conf` on the FAT (PSK **and** WPA-Enterprise peap/ttls/tls + cert files; validated pure-sh parsers, escape-safe supplicant rendering, CRLF-tolerant), read by the initrd (associate → stream rootfs) and re-used by the rootfs `polyptic-wifi.service` (the supplicant must survive rekeying — agent/browser/poll ride it all day). The local menu PINS `root=live:…/builds/<id>/` so kernel meets modules; `update-poll` refreshes the medium's inactive A/B slot (SHA256SUMS-verified, menu rewritten last) so sticks/ESPs never go stale, and heals a recovery-entry boot via the missing-`/lib/modules` tell. Offload copies payload+credentials to the ESP on Wi-Fi boots (`esp-too-small`/`no-local-payload` report codes; two-slot space check first); the ISO bakes wifi.conf for zero-infra boxes; payload media carry the token (documented credential, LEAN=1 keeps the tokenless dongle). 100+ new sh-test assertions + netboot-wifi bun suite; full `bun test` green. **POL-76 (D71): initramfs multi-NIC boot stall fixed** — a dual-NIC box with one cable unplugged stalled ~179 s in the initramfs (an unplugged NIC held `network-online.target`, because dracut marks every NIC `RequiredForOnline=routable` and `wait-online --any` still blocks on a required carrier-less link). Fixed with the initramfs twin of the rootfs's netplan `optional: true`: a `10-polyptic-netboot.network` marking wired links `RequiredForOnline=no` (Wi-Fi links stay required, so association is still waited for); `--any` timeout tightened as a backstop. Verified offline (new config-invariant test + shell suites); live multi-NIC boot pending. **POL-77 (D72): keyboard-less Wi-Fi failure diagnostics** — a real amd64 box rejected a Wi-Fi config that was provably valid (config + baked scripts + tools byte-identical to source, validated under dash+GNU in-cluster), so the failure lives in the box's live initramfs runtime, which nothing off-box reproduces. Since a wall screen has no keyboard, the reject path now remounts the medium read-write and writes `polyptic/wifi-debug.txt` (new `wifi-diagnostics.sh`): reject reason + `sh -x` validation trace + runtime PATH/tool-resolution + full net state (`ip`/`iw`/`rfkill`/wireless-`/sys`/wlan modules/`dmesg`). Tools shipped into `initrd-wifi` (`rfkill` added to the image; `ip`/`iw`/`rfkill`/`dmesg`/`lsmod` optional). Secrets redacted, best-effort, wired boxes never reach it. Verified by the wifi shell suite (13 new assertions); the live capture is the next real-hardware step — cut a fresh USB, boot the box, pull the stick, read the report. **POL-78 (D73): the real-radio Wi-Fi bug, FOUND + FIXED via POL-77's report** — the first `wifi-debug.txt` capture named it exactly: the initramfs shipped no `dirname`, so `wifi-supplicant-conf.sh`'s `$(dirname "$0")` left `LIB` empty → the sibling `wifi-conf.sh` was unfound → every Wi-Fi config was "rejected". The config, radio (`wlp45s0` iwlwifi, rfkill clear), and tools were all perfect. Fix: pure-shell `${0%/*}` own-dir resolution (no external `dirname`) in both scripts + `dirname`/`head`/`tail` added to the initrd; regression test stubs a broken `dirname` on PATH. This closes the real-radio association gap VMs never covered. **POL-79 (D74): the netboot depot always mirrors the ACTIVE build into `builds/<imageId>/`, healed on FETCH.** A homelab box 404'd forever "cannot fetch the OS image": its local medium pins `root=live:…/dist/image/<arch>/builds/<active-id>/rootfs.squashfs` (POL-63/D67), but the `builds/<id>/` mirror was created ONLY by the server's reconcile (`ImageUpdates.start()`→`retain()` at boot, or `execute()`→`retain()` after a server-triggered *successful* rebuild) — the build scripts write only the arch root (`build-live-image.sh`/`refresh-live-image.sh` → `$OUT_DIR`). So an externally-run rebuild Job or a partially-failed multi-arch hook run left the active build mirror-less, and with no restart the pinned path stayed 404 (the interim fix was a manual hardlink). Fix, server-side: (1) `GET /dist/image/:arch/builds/:imageId/:file` now heals on a miss via `ImageUpdates.ensureActiveBuild(arch)` (hardlink the arch root into `builds/<active-id>/`, EXDEV→copy) then re-stats — the box's own fetch heals the mirror, no restart, and a non-active/pruned id still 404s; (2) `execute()` reconciles unconditionally so a multi-arch run that partly failed still mirrors the arches that published; (3) startup reconcile unchanged. Idempotent, active never pruned. Tests: `image-updates.builds.test.ts` (reproduces the gap + proves the heal/idempotency/active-protection) and a real-server `netboot-builds-heal.e2e.test.ts` (active-at-root-only published post-boot → pinned path serves 200, bogus id 404). Full `bun test` 395/395. NOT verified: a live netboot on hardware. **POL-80 (D75): the OFFLINE/Wi-Fi GRUB splash is baked DETERMINISTICALLY + self-heals onto existing media.** POL-74/D69 baked the POL-47 theme onto the medium with a best-effort BUILD-TIME curl to `$POLYPTIC_BASE/boot/theme.txt`+`logo.png`, which **silently shipped a plain, theme-less medium** when the server wasn't reachable yet (the homelab case: media flashed before DNS resolved). Fix (1): `build-boot-medium.sh` now GENERATES `theme.txt` with `bun` from the SAME `buildBootThemeTxt()` the server serves (new `deploy/render-boot-theme.ts`) and copies the committed `packages/server/assets/boot-logo.png` — byte-identical to the served theme, no network, works in-cluster (rebuild Job) and on macOS; a missing asset degrades to a plain-but-booting menu with a LOUD line (never a hard fail, never a silent plain medium). `offload.sh` inherits it unchanged (copies the theme dir off the medium). Fix (2): `update-poll.sh` self-heals `/polyptic/boot/theme/{theme.txt,logo.png}` on the medium from the depot on every poll (the box HAS network unlike GRUB), so an OLD theme-less stick gets the splash WITHOUT re-flashing — idempotent (content compare, no FAT churn), atomic-ish (tmpfs stage → mv), best-effort/guarded, independent of the A/B slot logic. Tests: `wifi.test.sh` heal/idempotency/drift/no-theme cases (119 sh assertions), `boot-splash.test.ts` generator==served + no-network cases; full `bun test` 397/397. NOT verified: a live GRUB render on hardware. **POL-82 (D76): offline splash theme is a COMMITTED asset** — POL-80's `bun`-at-build-time generation failed in the cluster's bun-less `ubuntu:24.04` medium-bake Jobs (no `bun` on PATH → plain medium, a regression caught in the boot-medium hook log). Now `packages/server/assets/boot-theme.txt` is committed (like `boot-logo.png`) and `build-boot-medium.sh` just `cp`s it — no `bun`, no network; a drift test pins it == `buildBootThemeTxt()`. **POL-67 (D77): the kiosk browser is Google Chrome stable, NATIVE Wayland — reversing D63's surf-only clause on real-hardware evidence.** The first real amd64 wall (fpd-ago, amdgpu) CPU-pegged at idle: surf-under-Xwayland can't get DRI3 there, so every surf software-rendered (`WebKitWebProcess` at 300%+, no `/dev/dri` fd). Chrome (`--ozone-platform=wayland --kiosk --app`, one instance per output via per-connector `--user-data-dir`) goes EGL/GBM straight to the GPU like sway. Installed by `setup` from GOOGLE'S own apt repo (apt+amd64 only; the repo/key persist into the squashfs so the nightly refresh tracks latest stable Chrome automatically); **surf + xwayland/xdotool still install everywhere as the fallback** (arm64 boxes — Google's apt repo publishes no arm64 index yet — arm64 Chrome is announced for 2026 and `setup` PROBES the repo, so those boxes adopt it automatically on the first full rebuild after Google ships — and `POLYPTIC_BROWSER=surf`), the agent picks at runtime and reports `browser` on hello. **Remote DevTools through the console** (supersedes the POL-50 on-wall inspector on Chrome boxes; the surf path remains for fallback walls): Chrome's loopback-only `--remote-debugging-port` is tunnelled over the agent WS on the POL-59 relay posture — the existing per-screen inspect arm now means "arm the tunnel" (no relaunch, nothing on the glass), `DevtoolsRelay` + a session-gated `/api/v1/screens/:id/devtools[/**]` proxy serve Chrome's own bundled DevTools frontend, and its CDP WebSocket upgrade is cookie-gated, armed-rechecked per frame, audited. Console ▸ Machines: the per-screen button reads **DevTools** on Chrome machines and opens the tunnel in a new tab. Player secondary amplifier fixed (it measured ~2× the raw page's CPU): the badge's `backdrop-filter: blur()` is gone and the ident/badge blinks are discrete `visibility` steps (D66-compliant). Image size regresses ~300–400 MB (≈800 MiB, still ≪ the 1.4 GiB casper ISO). Next: real-hardware netboot pass (dongle/offload, Secure Boot ON, incl. true UEFI HTTP Boot) — now also **verifying Chrome-on-GPU** on the wall (a `/dev/dri` fd on Chrome's GPU process, idle CPU on a static page, the DevTools tab end-to-end) and **re-measuring the rebuilt image's size + RAM floor** (Chrome adds ~300–400 MB); confirming the POL-78 Wi-Fi fix associates on a real radio; plus the curated firmware allowlist on real GPUs/NICs and `gfxmode=auto` against a real EDID; then OIDC (6) when wanted. **POL-86 (D78): the wall paints only PROVEN-reachable content** — the PR #72 signal-driven watchdog went blind on its first real boot (TEST 8), so the player now probes every surface URL (`no-cors` fetch = per-URL reachability oracle) before painting, re-proves on doubt, covers media as well as iframes, and streams a `player/diag` trail into the server log so a broken boot is diagnosable from `kubectl logs` alone. Live-verified against a staged TEST 8. Pending on hardware: the real Wi-Fi reboot pass. **POL-87 (D79): the offline-boot `null src bitmap` error is gone at every layer** — the GRUB guard needs BOTH theme.txt and logo.png, every theme writer commits logo-first/theme-last, and update-poll deletes an orphaned theme.txt (incl. zero-byte logo) so already-broken sticks self-repair without a re-flash. Pending on hardware: one Wi-Fi boot to confirm the stick healed. **POL-68 (D80): Machines view decluttered + console lifecycle UI.** Approved cards show exactly three action clusters ([Ident all] · stateful console button · ⋯ Reboot/Revoke/Remove), Online + amber "Shell armed" chips, screen-row Remove as a ✕ icon; "Enable console" → split "Launch console ▾ / Disable console" drives the POL-59 per-machine arm (deliberately NO image-rebuild gate — see D80) and Launch opens a FULL-SCREEN console view. Operator reboots now read "rebooting…" (Presence-tracked, 3-min TTL) and close with "back online"; the feed gains an `accent` severity. Settings ▸ Onboard Screens: the status chip is the row's only status element (spinner "Updating"/"Rebuilding" + tooltip detail), retention footer folded into the ISO-download tooltips. The mock's per-machine CPU/mem/disk stats strip is DEFERRED to the POL-59 monitoring work. Full typecheck + test suite green; UI verified live against an isolated stack (screenshots in docs/pr-screenshots/pol-68/). **POL-60 (D81): kiosk input lockdown** — the sway config disables ALL input devices wholesale (`input * events disabled`, hot-plug covered) so a walk-up keyboard can't Tab around the dashboard or reach a VT; debug boots (`systemd.debug-shell=1`) re-enable keyboard/pointer/touch at startup for the tty9 shell; the x11-i3 fallback gets a best-effort `xinput` sweep that spares XTEST (the POL-50 inspector types through it) + `xinput` in the package sets. Verified against real sway 1.9 in a container (config validates; re-enable payloads accepted at runtime); 387/387 tests green. **POL-25 (D82): mTLS agent identity — the Phase-2b deferral lands.** `AGENT_MTLS_PORT` starts a second, TLS-terminating listener for /agent only, gated by a per-deployment private CA (persisted in the store; `@peculiar/x509`, no openssl): enrolment signs the agent's CSR (key never leaves the box; CN forced to the machine id) and returns the bundle on `server/enrolled.mtls`; the agent persists it 0600 and every reconnect dials `wss://` with the client cert — a wrong/absent cert FAILS THE HANDSHAKE, before app code. The 2b credential stays the per-machine identity (Bun can't expose peer certs on http upgrades yet, so CN-binding per connection waits on the runtime); `AGENT_MTLS_REQUIRE=1` is the enforced end-state (plain channel only enrols + issues, never admits). The server SELF-TESTS its live listener at boot (rogue-CA + no-cert dials must fail, judged by an app-data round-trip — TLS 1.3 'completes' the client handshake before the cert verdict) and exits on a runtime that can't verify (Bun ≤ 1.2's `requestCert` was presence-only, measured); CI/release move to Bun 1.3.14. Helm: `agentMtls.*` (raw-TCP Service port, never behind an HTTP Ingress). Full matrix + enrolment-flow tests, capability-gated. **POL-32 (D83): cached media — wall media survives a network outage.** Every image/video src is downloaded once into an **IndexedDB blob cache** and rendered from a `blob:` object URL (service workers need a secure context; the wall is plain HTTP — IndexedDB isn't), so an outage can't freeze a looping video (it streamed over Range before — it froze mid-frame) or break an image; `/media/<id>` is immutable-by-id → cached forever, external URLs stale-while-revalidate (throttled conditional GET; videos carry playback position across a content swap), failed fetches retry on a 5s→60s backoff (boot-into-outage self-heals), pruning = 30-day age + 4 GiB LRU cap (never evicting what's on the glass). The **last-good slice** is persisted to localStorage (schema-validated on load) and restored BEFORE the WS dials — a normal boot paints last-known content instantly; a reload during an outage shows the wall, not a splash (never ACKed; the first live render — even an authoritative empty one — overwrites). `GET /media/:id` gains `Access-Control-Allow-Origin: *` (the cache's fetch() is CORS-gated in the dev split-origin layout; the route was already public by design). Iframes were already never blanked/reloaded on disconnect — a live page's own network calls are its business. Verified in a real Chromium against the real server + built player: server SIGKILLed mid-playback → video keeps advancing; page reloaded against a dead control plane (static shell only, no WS//media/API) → the wall comes back from localStorage+IndexedDB. 400/400 tests (21 new). **POL-34 (D84): playlists** — carousels of content, first-class in the library (`ContentSource kind:"playlist"`, ordered steps referencing other sources with per-step timing: statics MUST be timed, a video may play out). The server resolves the whole rotation into ONE `PlaylistSurface` (+ a `startedAt` anchor) and the PLAYER advances it locally — fully-timed rotations derive their phase from the clock so wall members stay in sync and reboots rejoin mid-cycle; library edits/deletes ripple through playlists onto the glass; auth tokens stamp per entry (POL-24); console gains a dedicated Playlists sidebar view (list + step-builder modal; Content stays a flat source catalogue). Verified live (image→image→video on a real player) + 27 new tests; 405/405 green. **POL-42 (D85): authored PAGES + the Studio** — new content kind `page` (a `PageDefinition`: %-region element tree over a background, 16:9/9:16) composed in **Console ▸ Content ▸ New page → the Studio** (drag/resize/snap/guides/layers/undo-redo/inspector, the pitch’s v1 editor exactly) and assigned like any source; the definition rides INSIDE the slice as ONE `PageSurface`, so span math + the <150ms DOM-diff path are untouched and a Studio save re-pushes live walls with no reload. Elements v1: embed (library source by id → resolved + POL-24-credential-stamped at SEND time into `PageSurface.data`; raw URLs allowed, credential-free), RSS/Atom feed + Open-Meteo weather (server-side poller `page-data.ts`, assigned-pages-only, last-good on failure, change-driven re-push), ticker (CSS transform loop — the documented D66 carve-out; Chrome/D67 boxes are the target), image, text, clock, shape, QR (client-side SVG), countdown. One shared **`@polyptic/elements`** Vue package renders in BOTH player and Studio canvas (the preview IS the renderer; container-query sizing). No-Gos hold: single-surface screens, no zoom on pages, no pages-in-pages, not a signage CMS. `content_sources` gains nullable `url` + `definition jsonb`. 419/419 tests. **POL-88 (D86): the cluster rebuild/boot-medium Jobs now copy `packages/server/assets` into `/repo`, so the POL-82 committed splash theme actually reaches the medium bake (the v0.2.27 Job log showed the guard still shipping a plain offline menu); pinned by a boot-splash test across the helm seam. **POL-85 (D87): dev-tools quick-launch from the Wall page** — the Inspector's single-screen header gains a ⋯ overflow whose item runs the exact Machines-view flow (chrome = arm the POL-67 tunnel + DevTools in a new tab, surf/unknown = the confirmed on-panel inspector, offline = disabled with the explanatory tooltip), driven by the ack-settled Inspect state machine extracted from ScreenRow into a shared `useScreenInspect` composable (its settle rules now pinned by 10 unit tests); 561/561 green.** Next for pages: a live-wall pass of the ticker on a real software-rendered box. **POL-54 (D88): the /player WS is gated — the server mints a per-screen bearer token into every playerUrl (`server/apply`), the player echoes it on hello, and with auth enabled a missing/wrong/cross-screen token is closed (4401) before any slice (with its POL-24 credential-stamped URLs) leaves the building; sockets are bound to their admitted screen (no more spoofable ack/diag), the secret persists like the mTLS CA so reconnects never die, dev stacks (`AUTH_ENABLED=false`) stay open, and old agents upgrade for free because the URL was always server-minted.** Next for pages: a live-wall pass of the ticker on a real software-rendered box. **POL-70 (D89): HTTPS is the default posture — one `config.publicBaseUrl`/ingress host derives PUBLIC_BASE_URL + the three origin envs, ingress TLS defaults ON, Secure cookies key off the https base automatically (plain-HTTP homelabs degrade with a loud banner, never a refusal), native TLS via TLS_CERT_FILE/TLS_KEY_FILE for bare hosts; the netboot depot stays plain HTTP by contract (D47). Round 2: Let's Encrypt via a vendored, condition-gated cert-manager subchart (one hostname + one email → auto-renewing certs), TLS_MODE=self-signed (persisted server-minted CA, reused across boots) and a Console ▸ Settings ▸ HTTPS card with the CA download + per-OS trust instructions.** **POL-94 (D130 — provisional): the content library becomes an inventory** — search by name/address, kind filters and name/type/most-used sort; a usage fold in `admin/state.sourceStatus` ("Used on 2 screens · 1 video wall") that also makes the delete confirmation name exactly what it will break; and a live per-source health badge fed by the PLAYERS' own POL-86 probes (a new `player/surface-health` sent on state CHANGE only, attributed via a send-time `Surface.sourceId` stamp) — reachable / unreachable / not checked, with a tooltip that says plainly what a reachability probe cannot know (the SOP wall means a 500 or a login page still reads as "loading"). **POL-101 (D126, provisional): panels can be SLEPT — manually (per screen, per box) and on a daily panel-hours window — without re-opening blanking during content hours.** **POL-116 (D105): a pruned pin no longer bricks a Wi-Fi boot.** The offline menu pins `builds/<imageId>/rootfs.squashfs` (D67) and is only re-pinned AFTER a successful boot, so a box powered off across three rebuilds (D54's default retention) came back to a 404 it could never boot past — livenet retrying it forever, 5s apart (real hardware, 2026-07-14). A dracut `netroot` hook now probes the pin and, when the depot really has pruned it, re-points livenet at the ACTIVE build from `manifest.json` (or the unpinned arch root) — loudly: a plain-English splash line and a `pinned-build-missing` boot report in the activity feed. An unreachable depot changes nothing (a network outage must never look like a prune), retention stays a simple count (pin-aware retention was rejected — it means never pruning), and `update-poll` re-pins the medium afterwards, so the fallback fires once. Pending on hardware: one Wi-Fi boot against a depot whose pin has actually been pruned. **POL-109 (D129): media upload is an INGEST PIPELINE, not a byte sink** — `POST /api/v1/media` now probes (duration/dimensions/codecs), generates a poster frame (video) / downscaled thumbnail (image), and REFUSES a container/codec a wall browser cannot decode with a message that names it and says what to do (an AVI is caught at upload, not as a black screen on the glass). The toolchain sits behind a `MediaProber` adapter (`media-probe.ts`; `MEDIA_PROBE_CMD`/`MEDIA_FRAME_CMD`/`MEDIA_PROBE=off`) and a server with NO toolchain ACCEPTS the upload with a warning rather than refusing what it cannot inspect (D129). `ContentSource.media` is derived from the media catalogue (no DB change); video surfaces + playlist entries carry `poster` so the player pre-paints the first frame; the console shows real thumbnails/posters and auto-fills a playlist step's hold time from the probed duration.---Anchor docs (`CLAUDE.md`, `ROADMAP.md`, `DECISIONS.md`), monorepo skeleton (bun workspaces + `tsconfig.base`), and the shared **contract** (`@polyptic/protocol`, zod). The keystone everything builds against. **POL-118 (D106):** the wired GRUB stall is gone — stage 1 DHCPs the EFI cards **one at a time and takes the first lease** (`net_dhcp efinet0`; `$?`=36, GRUB_ERR_NET_NO_CARD, ends the walk) instead of letting bare `net_dhcp`'s all-cards loop wait out a carrier-less port long after the live one answered — the GRUB-stage twin of POL-76/D71, measured under OVMF with two NICs and one wired to nowhere: **18.3 s → 6.3 s** to chain. It also **narrates the wait** in plain English ("Looking for a wired network ..." → "Got an address (10.0.2.15) - fetching the boot menu ..."), and a fourth menu entry — **Watch this screen boot (verbose)** — turns on GRUB's own `debug=net,efinet,http` narration and drops `quiet splash`, so an operator can watch a boot happen. The dual-NIC amd64 box itself is the pending real-hardware check.---Anchor docs (`CLAUDE.md`, `ROADMAP.md`, `DECISIONS.md`), monorepo skeleton (bun workspaces + `tsconfig.base`), and the shared **contract** (`@polyptic/protocol`, zod). The keystone everything builds against. **POL-120 (D107): the player reports the build it is actually running** — the badge/splash/diag said `v0.0.0` on every wall because the player's vite config read `package.json` (permanently 0.0.0) and the Dockerfile declared `ARG POLYPTIC_VERSION` *after* the SPA builds; both halves fixed, pinned by a test asserting the ARG precedes the player build. **POL-123 (D108): the chart brings its own Postgres** — `postgresql.*` in `values.yaml` had always been a LIE (advertised, never implemented: no dependency, no template), so the shipped default `store: postgres` + `postgresql.enabled: false` installed happily and crash-looped the server on `getaddrinfo ENOTFOUND`, and the operator's cluster only ever worked because of an undocumented hand-applied `polyptic-db` manifest. The chart now deploys Postgres itself (a first-party StatefulSet, ON by default, password generated + preserved, data on a claim `helm uninstall` won't touch), REFUSES at template time to render an incoherent database posture, and adopts an existing `polyptic-db` PVC. `helm install polyptic … -f values.yaml` is once again the whole deployment. **POL-121 (D109): a fresh install now builds its own first OS image.** An empty depot used to stay empty for up to a week — the nightly cycle only refreshes an existing image and the weekly full rebuild was the only thing that created one — and the boot-medium hook, finding nothing, quietly handed out the **LEAN** (wired-only, no Wi-Fi) medium. The server now fires the full build ONCE at startup when no image is published, re-bakes the medium as part of it, and says so in the activity feed and on the Onboard Screens card; a store-side latch claimed before the hook spawns means a crash-looping pod can never turn that into a build storm. **POL-119 (D111): cast to a screen** — an operator flips a persistent per-screen Cast toggle in the Inspector, the box runs a supervised UxPlay (AirPlay) receiver advertised under the screen's friendly name, presenters mirror from iPhone/Mac after typing the PIN shown ON the wall; the receiver window fullscreens over the browser via a persistent pid-matched sway window watch, window presence is the live "Casting now" signal (agent status → Presence → console tile badge + Inspector state + player badge glyph), video-only/waylandsink-only/sway-only, apt set `uxplay avahi-daemon gstreamer1.0-plugins-bad` + avahi enabled by setup. Pending: real-hardware/VM pass with a real iPhone (mDNS on the wall L2). **POL-92 (D112, provisional): the fleet stopped being blind — every heartbeat now carries the box's own vitals (CPU/memory/disk/temp/load, per-browser RSS + respawns, the running image id, and the `/dev/dri` GPU tell that catches a software-rendering browser before it cooks the box, D77), the server rings them per machine and ships them in `admin/state`, the Machines view grew the stats strip POL-68 §1 designed (hysteretic overload banner, software-render banner), and `/metrics` exports per-machine labelled gauges (`polyptic_machine_up`, `_cpu_percent`, `_gpu_accelerated`, `_browser_respawns_total`, depot fetches, image build age) with sample alert rules in `deploy/prometheus-alerts.example.yaml`. **POL-107 (D114): multi-operator accounts — `admin` · `operator` · `viewer`, enforced server-side by ONE deny-by-default route-policy table (`packages/server/src/roles.ts`); a viewer reads + applies scenes, an operator authors content/layout, an admin owns machines, settings and accounts. The existing single admin credential is back-filled to `admin` (an upgrade is never a lockout) and Settings ▸ Operators invites the rest.** **POL-104 (D122): batch enrolment tokens + pre-registration** — the one flat bootstrap token baked into every stick becomes a SET of named, expiring, capped, individually revocable tokens (exactly one flagged `bake` — the one `/boot/grub.cfg` writes and `build-boot-medium.sh` lifts back out); the existing flat token is LIFTED verbatim on upgrade (legacy record, no expiry, no cap, still baked) so no medium already flashed is stranded. Revoking blocks new enrolments only — a box already enrolled keeps its per-machine credential and keeps running; rotating leaves the old secret alive for a 24 h grace window. A `pre_registrations` table (machineId/serial/MAC → label, tags, auto-approve), consulted only AFTER the token gate, makes a declared box name and approve itself with zero clicks; pending cards now carry MAC/serial/model/arch/IP + the token they came in on, and gained an Approve-all. **POL-105 (D124):** every box reports the OS image it BOOTED (`agent/hello` + heartbeat vitals, persisted — an OFFLINE box still tells you which build stranded it) and Settings shows the fleet bucketed by build; roll-outs stage behind a **POL-103 tag** — a RING (`tag=canary` → build X, its own urgency) makes `manifest.json?machineId=…` answer that box with a different build from the same depot, everyone else keeps the active build, and **Promote** serves it fleet-wide and retires the ring in one action. Retention stays a dumb count (D105): a ring pinned to a build that gets pruned degrades back onto the fleet build, loudly. **POL-112 (D128):** audio end-to-end — the player's hardcoded `muted` is gone, `muted`+`volume` (0–1) ride on video/playlist surfaces, the Inspector has a Sound card (toggle + slider), audio is remembered per `(screen-or-wall, content)` pair like zoom, new content always arrives **muted**, a combined surface sounds on exactly ONE panel (server-enforced, no echo), and an unmuted autoplay a browser refuses (surf/Xwayland) degrades to muted playback rather than freezing the wall. **POL-95 (D131): the Active badge stopped lying, and applying a scene stopped being a blind leap** — `activeSceneId` is now server-authoritative (persisted in `meta`, pushed in `admin/state`, cleared automatically the moment a manual change diverges the wall from the scene), so a reload and a second operator always agree; the console's optimistic client-side write is gone. **POL-176 (D162): install-to-disk — SHIPPED, in review.** A live-booted box with an internal disk shows INSTALL in the console; the installer (root path unit, agent-relayed live progress) wipes the named disk into an ESP + A/B image slots + ephemeral-encrypted swap + a scratch overlay, fetches a fresh sha256-verified build BEFORE the wipe, and the box boots its squashfs from its own disk thereafter — no RAM copy, no network in the boot loop, `rd.live.overlay.reset=1` statelessness by construction, menuless GRUB with `fallback=netboot` as the automatic recovery. update-poll stages new builds into the inactive slot on every poll and the reboot is operator-first ("update ready — reboot to apply"; nightly window backstop). Supersedes D55's diskless contract; retires the D47/D60 offload flow (fielded offloaded ESPs keep netbooting). **Pending: the first real-hardware installed boot (rides the existing hardware pass).**
**DoD:** workspace resolves; protocol types compile; design + decisions committed.

## Phase 1 — Live vertical slice ✅ (done)
The thinnest end-to-end thing that proves the spine and the **instant** property.
- `server`: in-memory desired-state, Fastify REST + WS hub (`/agent`, `/player`) on :8080.
- `agent`: connects, registers one screen, opens the player via the `dev-open` backend.
- `player`: SolidJS page, connects over WS, renders the slice, **swaps content via keyed DOM diff (no reload)**.
- `deploy`: docker-compose Postgres (Phase 2+, unused by the slice); run everything with `bun run dev`.
**DoD met:** REST change → player render in **~4ms** over WS, **stable-id in-place swap**, no reload. Verified by `scratchpad/harness.ts` (8/8) + typecheck + Vite build. See `docs/DEV.md`. Built in parallel against the locked contract, then cross-reviewed + fixed.

## Phase 2a — Registry (Postgres) + multi-machine + Admin UI + ident ✅ (done)
Real Machine/Output/Screen/Scene registry in **PostgreSQL** (dev via `deploy/docker-compose.yml`), behind a `Store` interface (`PostgresStore` default; `MemoryStore` test double). Multiple machines × screens. A minimal **Admin UI** (`packages/admin`): live machines→screens list with connection status, **rename**, and an **ident** button. **Ident mode** flashes a screen's friendly name (the player overlay is already built). Promote the e2e harness into committed `bun test`.
**DoD:** bring up Postgres + the stack; connect 2 machines; see both machines' screens in the Admin UI; click ident → the player flashes the name; rename a screen → persists across a server restart.

## Phase 2b — Enrollment/claim + durable credential ✅ (done)
Outbound-WSS **enrollment**: agent dials with a one-time bootstrap token → appears **pending** → operator **approves** in the Admin UI → durable per-machine **credential** (server stores only sha256; agent keeps the raw secret). Dev default is open-enrollment (auto-approve, with a boot warning); setting `POLYPTIC_BOOTSTRAP_TOKEN` switches on gating. **mTLS transport landed at POL-25 (D69)** — the credential model was the app-level seam the client certs dropped into: enrolment now also issues a durable per-machine cert and reconnects ride a dedicated TLS listener (`AGENT_MTLS_PORT`).
**DoD met:** a fresh machine shows pending; approving admits its screens (live `server/apply`); an unknown/wrong-token/unapproved machine is rejected + disconnected. Verified by e2e (10 gated tests) + a Postgres restart capstone.

## Phase 3 — Murals (spatial canvas), surfaces, content library, scenes
The big UI phase. Model adopted from the **Console v2** design (D20–D25):
- **Murals (D21):** several named, switchable canvases. A Screen is **unplaced** (tray) or **placed** on one mural at `{x,y,w,h}` — operators drag/snap screens to compose a wall.
- **Combined surfaces / video walls (D22):** combine adjacent screens into one `Surface`; content **spans** it (bezel seams shown); split to undo; "ident all".
- **Content library (D23):** reusable `ContentSource` items (web/dashboard/image/video + auth strategy) dragged onto a screen or surface.
- **Scenes (D24):** save the whole composition (content + layout + grouping) per mural; switch → atomic fan-out across all screens, instantly.
- **Activity feed (D25):** live event stream in the console.
- Contract gains: Mural, Surface, ContentSource, Scene, screen placement (position/size), + the WS/REST to drive them. **Admin UI** rebuilt as the canvas console.
**DoD:** compose a mural by snapping/combining screens; assign content from the library (incl. spanning a surface); save a scene; switch scenes → all screens flip together, instantly.

> **Missing operator flows** (not in Console v2; queued for design → then build): cold-start (nothing connected), the **enrollment/approval** UI (2b's bouncer), first-time *ident→name→place* mapping, a **fleet/machines** view, **content-source** add/edit, **scene management**, and console **settings/sign-in** (admin OIDC, Phase 6).

## Phase 4 — Real device stack + zero-click boot
**Delivery = `apt install polyptic-agent`** (a `.deb`), NOT a mandatory image (D26). Setup logic lives in the agent binary (`polyptic-agent setup`, distro-aware: apt/dnf/pacman → generic across systemd Linux); image + cloud-init/Ansible are optional wrappers. Start from **Ubuntu Server-minimal** (D27); the package wires **greetd** autologin → **sway** (Wayland; `x11-i3` fallback for NVIDIA, D9) → **systemd**-supervised agent + **surf-per-output** (D63 — one kiosk browser). **Make the `DisplayBackend`s real** — replace the Phase-1 sway/x11 stubs with swaymsg-IPC placement + browser launching. Crash hardening (`Restart=always`, no `swayidle`, `dpms on`). Config (control-plane URL + bootstrap token) via debconf or `/etc/polyptic/agent.toml` → it enrols (2b) → approve in the console.
**DoD:** cold power-on → wall shows the active scene with zero interaction; survives EOD smart-plug cut.
> **Test note:** **OrbStack** (headless — no display) verifies the install → systemd → agent → enrolment plumbing + a *headless* sway, fast. For the **visual** cold-boot DoD use a desktop-virtualization VM with a real virtual display — **Parallels** (or UTM) — where sway + the browser actually render. Caveats: a VM gives ~one virtual output, so *multi-output-per-client* placement + the real multi-screen wall stay a real-hardware test; and a virtual GPU may need `WLR_NO_HARDWARE_CURSORS` or the x11/i3 fallback (which usefully exercises that path). On Apple Silicon the guest is arm64 — build the `.deb` for the test VM's arch *and* for the (likely amd64) thin clients.

## Phase 5 — Preview, health, resilience, packaging
`grim` thumbnails (always-on) + on-demand `wayvnc`→noVNC through the control plane. Prometheus metrics, fleet/screen health in the admin UI. Agent caches last-good slice (rides out control-plane outages). Helm chart for any cluster.
**DoD:** see every screen live in the UI; control-plane restart never blanks the wall.

## Phase 6 — Auth, properly
Generic **OIDC** for admin UI/API (any IdP). Per-source auth strategies (`public`/`anonymous`/`reverse-proxy`/`oidc`). ~~mTLS agent identity~~ ✅ shipped (POL-25 / D69).
**DoD:** a sensitive dashboard shows authenticated content on the wall without a human logging in; admin UI is OIDC-gated.

## Phase 7 — Nice-to-haves
Media: image/video/**slideshow** + Office→media conversion (server-side). **Native-app** surfaces (CAD/RTSP/etc.) via the agent's top-level-window placement.
**DoD:** play a looping video + a converted slide deck as scene content; place one native window beside web tiles.
