{
  "name": "@polyptic/player",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Per-screen renderer (Vue 3) — connects over WS, renders its screen's slice (incl. video-wall spans), applies changes via DOM diff (no reload).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.2.9",
    "@fontsource-variable/geist-mono": "^5.2.8",
    "@polyptic/protocol": "workspace:*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.5.0",
    "vite": "^5.4.11",
    "vue-tsc": "^2.2.0"
  }
}
