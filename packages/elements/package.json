{
  "name": "@polyptic/elements",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared page-element Vue components (POL-42) — ONE renderer used by both the player and the console's Studio canvas, so the preview can never drift from the wall.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "vue-tsc --noEmit"
  },
  "dependencies": {
    "@polyptic/protocol": "workspace:*",
    "qrcode-generator": "^1.4.4",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vue-tsc": "^2.2.0"
  }
}
